### 종합 평가
- 전체 점수: 5.5/10
- 한줄 요약: 방향과 보안 설계는 좋지만, 부분 스냅샷이 정상 처리되거나 실패 후 DB가 반쯤 갱신될 수 있어 현재는 병합 차단이 필요합니다.

### 이슈 목록
- [High] apps/pipeline/src/fontagit_pipeline/transform.py:185 — 변환 실패 레코드를 경고만 남기고 제외합니다. 라틴 폰트가 빠져도 이후 동기화가 성공해 기존 폐트를 `draft`로 바꿀 수 있습니다. 전체 동기화에서는 변환 오류를 모아 DB 쓰기 전에 즉시 실패시키세요.
- [High] apps/pipeline/src/fontagit_pipeline/uploader.py:109 — 모든 `upsert_font`가 개별 커밋된 뒤 마지막 동기화 RPC를 호출합니다. stale 6종 초과나 네트워크 오류가 발생하면 DB가 부분 갱신된 채 종료됩니다. 스냅샷 적재와 stale 전환을 하나의 DB 트랜잭션으로 묶으세요.
- [High] apps/pipeline/src/fontagit_pipeline/uploader.py:95 — 최소 100종 검사는 실제 계약인 한글 38종+라틴 상위 100종을 보장하지 못합니다. 100~135종의 불완전 응답도 통과하고, 누락이 5종 이하면 정상 폰트를 비공개로 전환합니다. 한글·라틴 개수를 각각 검증하고 이전 정상 스냅샷 대비 감소율도 확인하세요.
- [Medium] apps/pipeline/src/fontagit_pipeline/__main__.py:95 — 스냅샷 검증과 DB 동기화보다 먼저 `tier-a.json`을 덮어씁니다. 업로드가 실패해도 잘못된 로컬 산출물이 남습니다. 임시 파일로 보관한 뒤 전체 성공 시 최종 파일로 교체하세요.
- [Medium] apps/pipeline/src/fontagit_pipeline/data/korean_names.json:222 — 설계 문서는 Noto Sans KR의 `name_ko`를 “노토 산스 KR”, “본고딕”을 별칭으로 정의하지만 실제 데이터는 “본고딕”을 이름으로 저장합니다. 화면의 대표 이름이 다른 브랜드명으로 표시될 수 있으므로 공식 이름은 재검증하고 통용명은 별칭으로 분리하세요. Noto Serif KR도 함께 확인해야 합니다.
- [Medium] apps/pipeline/src/fontagit_pipeline/models.py:68 — `sources`는 배열 존재만 검사하므로 `[" "]`나 `["curated"]`도 근거 URL로 통과합니다. HTTP(S) URL 형식과 공백을 검증하고 치명적인 잘못된 근거 입력 테스트를 추가하세요.
- [Medium] apps/pipeline/src/fontagit_pipeline/korean_names.py:33 — JSON 최상위가 배열이면 `raw.items()`에서 `AttributeError`가 발생해 `KoreanNamesError`로 변환되지 않고 CLI가 비정상 종료됩니다. 최상위 객체 여부를 먼저 검사하세요.
- [Medium] apps/pipeline/tests/test_uploader.py:108 — 테스트가 RPC 호출 순서만 모킹하며 핵심 DB 안전장치를 검증하지 않습니다. stale 정확히 5종 성공, 6종 실패 후 변경 0건, anon 실행 거부를 실제 PostgreSQL 통합 테스트로 검증하세요.
- [Medium] docs/superpowers/plans/2026-07-16-slice2-alias-search.md:156 — “Implementation Ready” 상태지만 SQL 정규화에 NFC가 없고, `similarity(...) * 0` 때문에 유사도 점수가 항상 같으며, 존재하지 않는 지마켓 데이터를 테스트합니다. 다음 슬라이스 실행 전에 계획을 실제 설계와 일치시키세요.

### 잘한 점
- `SECURITY DEFINER` 함수의 `PUBLIC`·`anon`·`authenticated` 권한을 회수하고 `service_role`만 허용한 점은 안전합니다.
- 빈 값·중복 slug, 100종 미만, stale 5종 초과를 Python과 DB 양쪽에서 방어한 방향은 좋습니다.
- NFC 정규화, 매핑 전수 대조, 폰트별 원자적 upsert와 핵심 경로 테스트를 추가한 점은 좋습니다.