# Review Report: 2026-07-27-compare-canvas-home-merge-design.md

> Generated: 2026-07-27 13:51
> Codex CLI: codex-cli 0.144.5 (model gpt-5.6-sol, reasoning xhigh)

---

## 1. Codex 리뷰 요약

- 전체 점수: 7/10
- 한줄 요약: 구현 방향은 명확하지만, 리다이렉트 전략-폰트 목록 기준-반응형-접근성-검증 기준을 보완해야 안전하게 구현할 수 있다.
- 항목별: 완성도 ⚠️(완료 조건 없음) / 누락 ⚠️(모바일-접근성-예외-배포 검증) / 일관성 ⚠️(301 vs /playground 재사용 충돌) / 실현 가능성 ⚠️(폰트 적용 방식-Cloudflare 검증 미구체화)

주요 지적 12건: (1) Plan 문서 부재 (2) 301과 /playground 재사용 충돌 (3) 폰트 후보 기준 불안정 (4) fontFamily 적용 방식 미명시 (5) slug 실패 시 렌더 스킵 부적절 (6) 입력 예외 처리 없음 (7) 접근성 기준 없음 (8) 반응형 미정의 (9) 리다이렉트는 build만으로 검증 불가 (10) UI 단위 테스트 과다 (11) 삭제 대상 전수 확인 필요 (12) 기준 브랜치 불명확

---

## 2. Claude 크로스 리뷰

### 종합 소견

리다이렉트 302 전환(#2), 배포 환경 검증(#9), 기준 브랜치 정정(#12)은 실질 결함으로 동의한다. 나머지는 방향은 맞으나 기존 코드 패턴(familyOf, aria-label, 반응형 CSS 계승)이 이미 커버하는 부분을 스펙에 명시하지 않아 생긴 지적이 많다.

### 항목별 판정

| # | Codex 피드백 | 판정 | Claude 의견 |
|---|-------------|------|------------|
| 1 | Plan 문서 부재 | 부분 동의 | superpowers 워크플로우상 스펙 승인 후 writing-plans 단계가 예정됨. 완료 조건만 스펙에 추가 반영 |
| 2 | 301 vs /playground 재사용 충돌 | 동의 | #69에서 재사용 예정인데 301은 브라우저에 영구 캐시됨. 302로 변경 |
| 3 | 폰트 후보 기준 불안정 | 부분 동의 | data/fonts.ts 무료 9종은 lib/fonts.ts(next/font)와 쌍으로 관리되는 기존 패턴. 기존 CompareBoard OPTIONS와 동일 필터라 새 위험 아님. familyOf 연결 명시로 해소 |
| 4 | fontFamily 적용 방식 미명시 | 동의 | 기존 유틸 `familyOf(fontKey)` 재사용을 스펙에 명시 |
| 5 | slug 실패 시 렌더 스킵 | 부분 동의 | 후보와 slug가 같은 정적 배열이라 실패 경로 없음. 가드는 TS 타입 안전용임을 문서화. 대체 UI는 과잉(YAGNI) |
| 6 | 입력 예외 처리 없음 | 부분 동의 | 빈 값 fallback(기존 동작 계승)과 overflow 규칙만 명시. maxLength 등 추가 정의는 과잉 |
| 7 | 접근성 기준 없음 | 부분 동의 | 기존 컴포넌트에 aria-label 패턴 존재(비교 문장 입력, N번 폰트 선택). 계승을 명시 |
| 8 | 반응형 미정의 | 부분 동의 | PlaygroundCanvas.module.css의 기존 브레이크포인트 계승을 명시 |
| 9 | 리다이렉트 build 검증 불가 | 동의 | 로컬 Next 빌드는 _redirects를 처리하지 않음. out/ 복사 확인 + 배포 후 curl 검증 추가 |
| 10 | UI 단위 테스트 과다 | 부분 동의 | 이 프로젝트는 컴포넌트 테스트 관례가 있어 신규 1파일은 유지하되 핵심 2케이스로 축소 |
| 11 | 삭제 대상 전수 확인 | 부분 동의 | 설계 시 grep 전수 확인 완료(참조 4곳: sitemap, Header, MobileTabBar, playground/page). 구현 계획에 최종 grep 단계 포함 |
| 12 | 기준 브랜치 불명확 | 동의 | 실증 확인: 기능 PR base는 develop(PR #121, #113, #112), main은 release 전용. 스펙 "main 기준"은 오류 → develop으로 정정 |

### 동의하는 핵심 피드백 Top 3

1. **#2 301→302**: #69에서 /playground를 재사용하는 순간 301 캐시가 사용자를 계속 홈으로 보내는 실제 장애가 된다.
2. **#12 기준 브랜치**: gh 조회로 develop이 기능 base임을 확인. 추가로 #117이 이미 PR #121로 develop에 머지된 사실 발견 — 로컬 미커밋 변경은 잔재 가능성.
3. **#9 배포 검증**: _redirects는 Cloudflare 전용이라 로컬 검증이 불가능. 검증 절차를 스펙 완료 조건에 포함.

### 동의하지 않는 피드백

- #5의 "대체 UI 표시": slug는 셀렉트 후보와 동일한 정적 배열에서만 나오므로 실패 경로가 없다. 에러 UI 추가는 사용 불가 상태를 위한 코드로 YAGNI 위반.

### Codex가 놓친 추가 관점

- #117 잔재: 워킹트리의 미커밋 변경이 이미 머지된 PR #121과 중복일 가능성. 구현 전 diff 대조-정리가 선행 조건.

---

## 3. 최종 권고사항

### 즉시 반영 (Must) — 스펙에 반영

1. 리다이렉트 301 → 302 (#69 재사용 대비)
2. 리다이렉트 검증 절차 추가 (out/_redirects 확인 + 배포 후 curl)
3. 기준 브랜치 main → develop 정정, #117 머지 사실 반영

### 검토 후 반영 (Should) — 스펙에 반영

4. familyOf(fontKey) 적용 방식 명시
5. 빈 값 fallback-overflow 규칙, 접근성-반응형 기존 패턴 계승 명시
6. 신규 테스트 핵심 2케이스로 축소
7. 완료 조건 섹션 추가

### 참고 (Nice to have)

8. slug 가드는 TS 안전용임을 문서화 (반영)
9. 구현 계획에 최종 참조 grep 단계 포함 (writing-plans에서 처리)
