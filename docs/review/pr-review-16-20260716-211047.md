# PR Review Report: #16 — feat: 검색 이연 반영 (오류 상태 구분, URL 동기화, SQL 통합 테스트)

> Generated: 2026-07-16 21:25 KST
> PR URL: https://github.com/jojo3420/fontagit/pull/16
> Base ← Head: main ← feat/search-followups
> Codex Model: gpt-5.6-sol (config 기본값, reasoning xhigh)

---

## 0. PR 요약

- 변경: searchFonts 오류 계약(빈 배열 → throw) + 오류 상태 UI, URL q 동기화(replace), aria-live, `supabase/tests/search_fonts_test.sql`(psql assert 10케이스), 계획 문서 v1.2
- 리뷰 시점 diff 380줄, 이후 fix 커밋 2개 추가

---

## 1. Codex 리뷰 (요약)

- 전체 점수: 7/10 | 머지 권고: ⚠️ Should-fix 수정 후 머지 | **Must-fix: 없음**
- Should-fix 4건 + Medium 5건 (전문은 이 파일 이전 버전의 관점별 표 참조 — 아래 판정표에 항목별 요약)

---

## 2. Claude 크로스 리뷰

### 항목별 판정 (전부 diff/실코드 대조)

| # | Codex 지적 | Claude 판정 | 처리 |
|---|-----------|------------|------|
| H1 | 입력 삭제 시 URL의 q 잔존 (replace가 truthy 분기 안, '/search'는 데드 코드) | **동의 — 실코드 확인** | 1차 fix(5f3336c)가 미해결 → 컨트롤러 직접 수정 `56f0ab7`(분기 밖 이동 + trim) |
| H2 | 실패 시 이전 검색 결과가 오류와 동시 노출 | **동의 — 실코드 확인** | `5f3336c`(catch에 setResults([]) + 렌더 조건 !error) |
| H3 | SQL S5/S6이 부정 케이스 없으면 거짓 통과 | 부분 동의 | 스모크 한계 주석 추가(5f3336c). 데이터 생성-롤백은 "파이프라인 외 dev 직접 삽입 금지" 결정과 충돌 → 이연 |
| H4 | URL 동기화 테스트 부재 | **동의** | `56f0ab7` — vi.hoisted 공유 스파이로 replace 단언 + 성공→실패 연속 검색 회귀 테스트 |
| 관점7 | 오류 로깅 소실 | 동의 | `5f3336c` — throw 전 console.error 복원 |
| M1 | 뒤로가기 시 입력값-URL 불일치 | 동의 → 이연 | 별도 iteration (useSearchParams 구독 재설계 필요) |
| M3 | boolean 4개 대신 단일 상태 머신 | 동의 → 이연 | 리팩토링 성격, 동작 영향 없음 |
| M4 | SQL 테스트의 dev 데이터 의존 | 알려진 트레이드오프 | 헤더에 전제 명시됨 |
| M5 | URL에 원본 query(trim 안 됨) | 동의 | `56f0ab7`에 trim 반영 |

### 재검증 (증거)

- 최종 테스트 96/96 통과, tsc 0 에러 (fix 2건 반영 후)
- H1 검증: replace가 debounce 콜백의 분기 밖 공통 위치 — 입력 삭제 테스트가 `/search` 호출 단언
- 1차 fix의 부실 이행(H1 미적용, H4 중복 테스트)을 컨트롤러 실코드 대조로 적발 후 직접 교정 — 구현자 보고만 믿지 않는 검증의 필요성 재확인

---

## 3. 최종 권고 및 머지 결정

### 머지 결정: ✅ 머지 (squash) — Must-fix 0건, 동의 Should-fix 전건 반영

### 이연 (다음 iteration)
- M1 URL→입력 역방향 동기화(뒤로가기), M3 상태 머신 통합, H3 부정 케이스 데이터 전략(파이프라인 경유 테스트 픽스처)

---

## 4. 다음 단계

- [ ] prod 배포 시 0006 적용 + `supabase/tests/search_fonts_test.sql` 실행
- [ ] 슬라이스3 클릭 집계(0007)

## 5. 메타데이터

- Codex 종료 코드: 0 | diff 380줄(리뷰 시점) | 이 리포트: docs/review/pr-review-16-20260716-211047.md
