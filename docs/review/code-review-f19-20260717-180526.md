### 종합 평가
- 전체 점수: 5/10
- 한줄 총평: 기본 구조와 XSS 방어는 적절하지만, 빈 쿼리 레이스와 React Strict Mode 오류 때문에 자동완성 신뢰성이 부족합니다.
- 권고: Must-fix 필요

### 관점별 리뷰 (표)

| # | 관점 | 평가 | 핵심 지적 |
|---|---|---|---|
| 1 | 정합성 | ⚠️ 부분 충족 | 클라이언트 RPC 구조는 맞지만 커밋 설명과 달리 AbortController가 없습니다. |
| 2 | 레이스 컨디션 | ❌ Must-fix | 쿼리를 지울 때 이전 응답이 다시 표시될 수 있습니다. |
| 3 | IME 조합 | ⚠️ 불완전 | 조합 중 조회가 실행되며 Enter 기본 제출 방어도 부족합니다. |
| 4 | 접근성 | ⚠️ 부분 충족 | 기본 ARIA와 방향키는 있으나 blur, 고정 ID, 스크롤 처리가 미흡합니다. |
| 5 | XSS | ✅ 양호 | React 텍스트 렌더링만 사용해 안전합니다. |
| 6 | 에러-빈 상태 | ❌ 부족 | 로딩, 0건, RPC 실패가 모두 같은 빈 화면입니다. |
| 7 | Silent Failure | ❌ 있음 | 콘솔 기록만 있고 사용자에게 실패가 표시되지 않습니다. |
| 8 | 성능 | ⚠️ 보완 필요 | 타이머 정리는 되지만 실제 RPC 요청은 취소되지 않습니다. |
| 9 | 방어적 입력 | ✅ 대체로 양호 | trim, 최대 길이, RPC 파라미터 전달이 적용됐습니다. |
| 10 | 테스트 | ❌ 핵심 누락 | 레이스, Strict Mode, 실제 IME 제출, 오류 상태 테스트가 없습니다. |
| 11 | 머피의 법칙 | ❌ 미흡 | 빈 쿼리 전환, 503, 포커스 이동 같은 실패 경로가 취약합니다. |

### Critical 이슈 — Must-fix

1. 빈 쿼리에서 이전 응답이 되살아나는 레이스

- 파일:라인: `apps/web/hooks/useDebouncedSuggestions.ts:34-57`
- 문제: 요청 중 검색어를 지우면 이전 요청의 응답이 빈 상태를 덮어쓸 수 있습니다.
- 근거(diff 인용): `if (!trimmedQuery) { ... return; }`이 `const currentSequence = ++sequenceRef.current;`보다 먼저 실행됩니다. 이미 시작된 RPC도 취소되지 않습니다.
- 제안 수정: effect 시작 시 항상 시퀀스를 증가시키고, 빈 쿼리도 이전 요청을 무효화해야 합니다. AbortController 신호도 `searchSuggestions`까지 전달해 실제 RPC를 취소하세요.

2. React Strict Mode에서 자동완성이 멈출 수 있음

- 파일:라인: `apps/web/hooks/useDebouncedSuggestions.ts:32,78-82`
- 문제: Strict Mode의 개발용 effect 재실행 과정에서 `isMountedRef`가 `false`로 고정될 수 있습니다.
- 근거(diff 인용): cleanup에서 `isMountedRef.current = false`로 만들지만 effect 재실행 시 다시 `true`로 설정하지 않습니다.
- 제안 수정: `isMountedRef`를 제거하고 AbortController와 시퀀스로 생명주기를 관리하는 것이 가장 단순합니다. 유지한다면 effect setup마다 반드시 `true`로 복구해야 합니다.

### High 이슈 — Should-fix

1. 새 검색어에 이전 제안을 선택할 수 있음

- 파일:라인: `apps/web/hooks/useDebouncedSuggestions.ts:46`, `apps/web/components/HeaderSearch.tsx:137-154`
- 문제: 새 쿼리를 입력해도 이전 `items`가 200ms와 RPC 대기 시간 동안 계속 표시됩니다. 사용자가 방향키와 Enter로 잘못된 폰트에 이동할 수 있습니다.
- 근거(diff 인용): `setResult((prev) => ({ ...prev, loading: true, error: false }))`가 기존 항목을 유지하고, Header는 `{ items }`만 사용합니다.
- 제안 수정: 쿼리 변경 즉시 `items: []`로 초기화하거나 결과가 어느 쿼리에 대한 것인지 함께 저장해 현재 쿼리와 일치할 때만 표시하세요.

2. IME 조합 방어가 불완전함

- 파일:라인: `apps/web/components/HeaderSearch.tsx:48-69,113-124`
- 문제: 조합 중 Enter에서 사용자 정의 선택만 중단할 뿐, 폼의 기본 제출을 명시적으로 막지 않습니다. 조합 중간 문자열로 RPC도 실행됩니다.
- 근거(diff 인용): `if (e.nativeEvent.isComposing) return;`에는 `preventDefault()`가 없고, `onChange`는 조합 여부와 관계없이 `setQuery()`를 호출합니다.
- 제안 수정: 조합 여부를 ref로 관리하고, 조합 중 Enter는 기본 동작도 차단하세요. 훅에는 `isComposing ? '' : query`를 전달하고 `keyCode === 229` 호환 처리도 검토하세요.

3. blur가 검색 버튼과 키보드 포커스를 끊음

- 파일:라인: `apps/web/components/HeaderSearch.tsx:124-137`
- 문제: 입력창에서 검색 버튼으로 포커스를 옮기는 순간 `open=false`가 되어 폼 자체가 사라질 수 있습니다. Tab 이동과 마우스 클릭 모두 불안정합니다.
- 근거(diff 인용): 입력에 `onBlur={() => setOpen(false)}`가 있고 같은 `open`이 검색 패널과 제안 목록을 함께 제어합니다.
- 제안 수정: 검색 패널 열림과 제안 목록 열림을 별도 상태로 분리하세요. `relatedTarget`이 폼 내부인지 확인한 뒤 외부로 나갈 때만 닫아야 합니다.

4. 로딩-0건-503 실패가 사용자에게 구분되지 않음

- 파일:라인: `apps/web/components/HeaderSearch.tsx:19,137-154`
- 문제: 훅이 제공하는 `loading`과 `error`를 사용하지 않아 RPC 실패가 사용자에게 조용히 숨겨집니다.
- 근거(diff 인용): `const { items } = useDebouncedSuggestions(query);`로 두 상태를 버리고, `items.length > 0`일 때만 목록을 렌더링합니다.
- 제안 수정: “검색 중”, “검색 결과 없음”, “잠시 후 다시 시도”를 구분해 표시하고 상태 메시지에는 `role="status"`를 적용하세요.

### Medium 이슈

1. 핵심 실패 경로 테스트 누락

- 파일:라인: `apps/web/hooks/useDebouncedSuggestions.test.ts:15-64`, `apps/web/components/HeaderSearch.test.tsx:53-86`
- 문제: 오래된 응답의 역전, 쿼리 삭제, Strict Mode, 503, 로딩 상태를 검증하지 않습니다. IME 테스트도 실제 폼 제출 여부까지 확인하지 않습니다.
- 근거(diff 인용): 훅 테스트는 호출 횟수만 검사하며 Header 훅 mock은 항상 결과를 반환합니다.
- 제안 수정: 핵심 테스트를 3개 정도 추가하세요. 우선순위는 빈 쿼리 레이스, Strict Mode 재실행, 조합 중 Enter 제출 방지입니다.

2. 테스트의 결과 타입 캐스팅이 잘못됨

- 파일:라인: `apps/web/hooks/useDebouncedSuggestions.test.ts:30-32`
- 문제: 검색 결과 객체를 함수의 첫 번째 매개변수 타입인 `string`으로 이중 캐스팅해 타입 오류를 숨깁니다.
- 근거(diff 인용): `as unknown as Parameters<typeof searchModule.searchSuggestions>[0]`
- 제안 수정: `SearchResult` 또는 `Awaited<ReturnType<typeof searchSuggestions>>[number]` 타입을 사용하세요.

3. listbox ID 충돌과 잘못된 ARIA 참조 가능성

- 파일:라인: `apps/web/components/HeaderSearch.tsx:20,127-131`
- 문제: HeaderSearch가 두 번 렌더링되면 동일 ID가 중복됩니다. 목록이 렌더링되지 않은 상태에서도 `aria-controls`가 존재하지 않는 요소를 가리킵니다.
- 근거(diff 인용): `const listboxId = 'header-suggest-listbox';`
- 제안 수정: React `useId()`로 고유 ID를 만들고, 목록이 표시될 때만 `aria-controls`와 `aria-activedescendant`를 설정하세요.

4. 키보드 활성 항목이 화면 밖으로 이동할 수 있음

- 파일:라인: `apps/web/components/HeaderSearch.tsx:48-62`, `apps/web/components/SearchSuggestions.module.css:12-17`
- 문제: 목록은 스크롤되지만 방향키로 이동한 활성 항목을 자동으로 화면 안으로 가져오지 않습니다.
- 근거(diff 인용): `activeIndex`만 변경하며 `scrollIntoView()` 처리가 없습니다.
- 제안 수정: 활성 항목 ref를 두고 인덱스 변경 시 `scrollIntoView({ block: 'nearest' })`를 호출하세요.

5. 영문 검색 결과의 강조가 불명확함

- 파일:라인: `apps/web/components/SearchSuggestions.tsx:17-35,58-59`
- 문제: 한글 이름이 있으면 영문 이름을 표시하지 않습니다. 영문 검색으로 찾은 결과에도 강조가 없을 수 있습니다.
- 근거(diff 인용): `const name = item.nameKo || item.nameEn;` 및 대소문자를 구분하는 `text.indexOf(trimmedQuery)`
- 제안 수정: 한글과 영문 이름을 함께 표시하고, 강조 비교는 대소문자를 무시해 처리하세요.

### 긍정적 관찰

- 정적 export 제약에 맞게 Supabase RPC를 클라이언트에서 직접 호출했습니다.
- `dangerouslySetInnerHTML` 없이 React 텍스트 노드와 `<mark>`를 사용해 XSS에 안전합니다.
- RPC 입력을 문자열 조합이 아닌 파라미터로 전달합니다.
- 공백 제거와 최대 쿼리 길이 검사가 있습니다.
- 기본적인 listbox/option 역할, `aria-selected`, 방향키-Enter-Escape 처리가 구현됐습니다.
- 제안 항목은 `onMouseDown`에서 기본 동작을 막아 마우스 선택 시 blur 순서 문제를 줄였습니다.
- 타이머 cleanup과 비어 있지 않은 새 쿼리 사이의 시퀀스 방어는 구현되어 있습니다.

---

## Claude 교차 검증 (증거 기반, git show로 실제 코드 대조)

> 대상 커밋 d719397(F-19)은 이미 develop 병합됨 → 아래는 후속 수정 항목. 머지 차단 개념 없음.

| Codex 지적 | 파일:라인 | Codex | Claude 판정 | 근거(실제 코드) |
|-----------|---------|-------|------------|----------------|
| 빈 쿼리 stale 레이스 | useDebouncedSuggestions.ts:37-48 | Must | **동의 → High(실재)** | early-return이 `++sequenceRef`(48) 앞(37). RPC in-flight 중 입력 삭제 시 이전 응답이 빈 상태 덮어씀. prod 영향 |
| Strict Mode isMountedRef 고정 | :32,78-82 | Must | **동의 → High(dev전용)** | cleanup만 false, effect 재설정 시 true 복구 없음 → dev 이중실행 후 자동완성 멈춤. prod(런타임 Strict 없음) 무관 |
| 새 쿼리에 이전 items 노출 | HeaderSearch.tsx:~143, hook:46 | High | 동의 → High | `setResult(prev=>{...loading:true})`가 prev.items 유지 → 200ms+RPC 동안 이전 결과로 오이동 가능 |
| loading/error 무시(Silent Failure) | HeaderSearch: `const { items } = ...` | High | 동의 → Should | 훅의 loading/error 버림 → RPC 503이 사용자에 무음. role=status로 로딩/0건/실패 구분 필요 |
| blur가 폼 닫음 | HeaderSearch onBlur | High | 동의 → Should | input onBlur→open=false가 패널+목록 동시 제어. relatedTarget 검사로 분리 |
| IME preventDefault/조합 중 RPC | HeaderSearch handleKeyDown | High | 부분동의 → Medium | 드롭다운은 `!isComposing`로 숨김. 조합 중 RPC 호출은 낭비(표시 영향 X). Enter 기본제출 방어 보강 권장 |
| 테스트 누락(레이스/Strict/503) | *.test | Medium | 동의 → Medium | 실패경로 테스트 부재 |
| 테스트 타입 이중 캐스팅 | hook.test:30-32 | Medium | 동의 → Nice | `as unknown as ...string` → SearchResult 타입 사용 |
| listbox 고정 ID | HeaderSearch:listboxId | Medium | 동의 → Nice | `useId()`로 고유화 |
| scrollIntoView 누락, 영문 강조 | 각 | Medium | 동의 → Nice | UX 개선 |

### 종합
- 환각 없음. Codex file:line 전부 실제 코드와 일치. 리뷰 품질 높음.
- 구조/보안은 양호(XSS 안전, 파라미터 RPC, ARIA 기본, 입력 검증). 반면 **신뢰성/에러 처리에 실재 결함**(레이스 2건, dev 멈춤, 무음 실패, stale 선택).
- 권고 우선순위: (1) 훅 재작성 - `++sequenceRef`를 effect 최상단으로 + 빈 쿼리도 무효화 + AbortController로 실제 RPC 취소 + isMountedRef 제거, (2) HeaderSearch가 loading/error 노출 + 새 쿼리 시 items 초기화, (3) blur relatedTarget 분리, (4) 실패경로 테스트.
