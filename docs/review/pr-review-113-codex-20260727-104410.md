### 종합 평가
- 전체 점수: 7/10
- 한줄 총평: 실행 증거는 강하지만, 청크 manifest의 참조 무결성 검증이 코드에 없어 PR 설명과 핵심 안정성 주장이 어긋납니다.
- 머지 권고: Should-fix 수정 후 머지

### 관점별 리뷰 (표)
| # | 관점 | 평가 | 핵심 지적 |
|---|------|------|---------|
| 1 | 정합성 | 보통 | PR 설명의 “합집합=전체/evidence 참조 무결성 검증”이 실제 코드에 없음 |
| 2 | 영향도 분석 | 양호 | CLI 인자 추가와 호출 흐름은 대체로 연결됨 |
| 3 | SOLID/SRP | 보통 | `audit-chain.sh`가 실행, 복구, 검증, 로깅을 모두 떠안음 |
| 4 | 선제 위험 | 보통 | 청크 적용 중 일부 성공 후 실패하면 부분 적용 상태가 남음 |
| 5 | 입력 방어 | 보통 | `--chunk-size` 양수 검증은 있으나 CLI 단계에서 즉시 막지는 않음 |
| 6 | 보안 | 양호 | 명백한 인젝션/시크릿 노출은 보이지 않음 |
| 7 | Silent Failure | 개선 | 성공 로그 보강은 좋지만, 청크 누락 검증이 약함 |
| 8 | 테스트 | 부족 | 핵심 무결성/다중 청크/누락 evidence 테스트가 없음 |
| 9 | 스타일/가독성 | 양호 | Python 코드는 읽기 쉬움. Shell은 길어 유지보수 부담 있음 |
| 10 | 머피의 법칙 | 보통 | 재개 옵션은 있으나 부분 적용/오염된 출력 디렉터리 방어가 약함 |

### Critical 이슈 — Must-fix (머지 차단)
없음.

### High 이슈 — Should-fix (머지 전 수정 권장)

`apps/pipeline/src/fontagit_pipeline/audit_manifest.py:748` / 청크 분할 후 참조 무결성 검증이 없음 / 근거(diff 인용):
```python
filtered_snapshots: list[dict[str, object]] = []
...
filtered_findings: list[dict[str, object]] = []
...
chunks.append(...)
```
문제: entry가 참조하는 `evidence_ids`나 `finding_ids`가 `evidence_bundle`에서 빠져도 에러가 나지 않습니다. 그러면 청크 manifest는 저장되지만, 나중에 적용/감사 추적에서 증거가 비는 조용한 실패가 됩니다. PR 설명의 “합집합=전체-evidence 참조 무결성 검증 포함”과도 불일치합니다.  
제안 수정: 각 청크에서 `referenced_evidence_ids - actual_snapshot_ids`, `referenced_finding_ids - actual_finding_ids`를 계산해 하나라도 남으면 `ManifestError`를 발생시키세요. 전체 청크 합집합이 원본 entries/evidence 참조와 같은지도 테스트로 고정해야 합니다.

`apps/pipeline/tests/test_audit_manifest.py:318` / 테스트가 핵심 위험을 못 잡음 / 근거(diff 인용):
```python
def test_split_manifest_into_chunks_basic() -> None:
...
chunk_size=1로 1개 엔트리 bundle
```
문제: 신규 테스트 4개가 거의 모두 “1개 entry → 1개 chunk”만 검증합니다. 실제 장애 원인은 1,073건 대량 분할입니다. 현재 테스트로는 다중 청크 순서, 청크별 evidence 누락, 전체 entries 합집합 보존, reverse manifest 정확성을 못 잡습니다.  
제안 수정: 최소 3개 entry를 만들어 `chunk_size=2`로 2개 청크를 만들고, 전체 entry 수 합계, 각 청크 evidence 참조, reverse before/after 교체, 누락 evidence 시 `ManifestError`를 검증하세요.

`scripts/audit-chain.sh:251` / 청크 순차 적용은 부분 성공 후 실패 시 DB가 중간 상태로 남음 / 근거(diff 인용):
```bash
for chunk_dir in "$manifest_dir"/chunk-[0-9][0-9][0-9]; do
...
font-audit-manifest apply ...
...
exit "$step_exit"
```
문제: 11개 중 6개 적용 후 7번째가 실패하면 앞 6개는 이미 DB에 반영됩니다. 재실행 시 “이미 적용된 항목은 자동 제외”라고 주석은 말하지만, 이 보장은 이 스크립트 안에서 확인되지 않습니다.  
제안 수정: 실패 메시지에 “부분 적용 상태”를 명시하고, 재실행 전에 현재 적용 수를 확인하는 검증 단계를 넣으세요. 가능하면 `apply` RPC가 멱등인지 테스트/문서로 고정해야 합니다.

### Medium 이슈 (다음 iteration)

`apps/pipeline/src/fontagit_pipeline/__main__.py:1353` / `--chunk-size` 검증이 늦음 / 문제: 음수 입력 시 `split_manifest_into_chunks`에서야 실패합니다. / 제안 수정: argparse에 양수 정수 validator를 붙이면 초급 사용자도 바로 이해 가능한 에러를 받습니다.

`scripts/audit-chain.sh:90` / 출력 디렉터리 재사용 방어 부족 / 문제: 사용자가 기존 산출물 디렉터리를 넘기면 오래된 `chunk-*`가 섞일 수 있습니다. / 제안 수정: `manifest_dir`가 이미 있고 비어 있지 않으면 중단하거나 `--force`를 요구하세요.

`scripts/audit-chain.sh:192` / Docker 이미지 존재 확인 없음 / 문제: `fontagit-pipeline:local`이 없으면 4단계에서 뒤늦게 실패합니다. / 제안 수정: 시작 시 `docker image inspect fontagit-pipeline:local`로 빠르게 확인하세요.

`apps/web/next.config.ts:6` / PR 범위와 직접 관련이 약함 / 문제: Turbopack root 변경은 audit-chain 코드 병합 PR의 핵심과 성격이 다릅니다. / 제안 수정: 실제로 lockfile 핫픽스와 묶인 필수 변경이면 PR 설명에 별도 근거를 남기세요.

### 긍정적 관찰

성공 로그 보강은 좋습니다. `auto-approve`와 `manifest apply`가 성공했는지 숫자로 남기면 장시간 배치 작업에서 운영자가 헷갈릴 여지가 줄어듭니다.

`in-list` 배치를 40으로 낮춘 변경은 현실적인 운영 수정입니다. prod 게이트웨이 URL 길이 문제를 코드 주석으로 남긴 점도 다음 작업자에게 도움이 됩니다.

청크별 `forward.sha256`/`reverse.sha256`를 따로 저장하는 구조는 방향이 맞습니다. 여기에 참조 무결성 검증만 추가하면 대량 적용용 코드로 훨씬 단단해집니다.