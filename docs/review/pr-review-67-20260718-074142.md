### 종합 평가: 점수 6/10, 기본적인 XSS·시크릿 방어는 괜찮지만 광고 실패 처리와 sitemap 갱신·검증, 개인정보 고지가 운영 수준에 못 미침, 머지 권고(Should-fix 후)

### Critical(Must-fix, 머지 차단)

없음. 제공된 diff에서 즉시 침해나 빌드 실패가 확정되는 문제는 발견하지 못했습니다.

### High(Should-fix)

1. `apps/web/components/AdFitUnit.tsx:35-63`, `apps/web/lib/analytics/constants.ts:36-38` / 광고 스크립트 실패가 조용히 묻힘 / `data-ad-onfail`만 믿고 있어 네트워크 오류, CSP(콘텐츠 보안 정책) 차단, 스크립트 실행 오류에는 콜백이 실행되지 않습니다. 그러면 빈 광고 영역이 계속 남고 장애도 관측되지 않습니다. `"//t1.kakaocdn.net/..."`도 프로토콜 의존 주소입니다. / `https://` 절대 주소를 사용하고 `script.onerror`, 제한 시간, 오류 로그를 추가하세요. 로더는 페이지당 한 번만 적재하고 Kakao 공식 도메인만 CSP에 허용했는지 배포 환경에서 확인해야 합니다.

2. `apps/web/app/privacy/page.tsx:63-71,87-106`, `apps/web/components/AdFitUnit.tsx:50-57` / 실제 광고 처리와 개인정보 고지가 충분히 맞지 않음 / 광고 단위가 설정되면 Kakao 스크립트가 즉시 실행되지만 고지는 “쿠키를 사용할 수 있다” 수준입니다. 수집 항목·목적·보유 기간·거부 방법·Kakao 정책 링크가 없고, Google 맞춤 광고는 실제 설정과 무관하게 단정합니다. `FontAgit 운영팀`도 책임자를 식별하기 어렵습니다. / AdFit 계약과 실제 전송 데이터를 기준으로 문구를 확정하고 Kakao·Google 정책 및 거부 링크를 넣으세요. 상호·사업자번호·메일이 실제 운영 정보인지 확인하고, 동의가 필요한 설정이라면 동의 전에는 스크립트를 로드하지 않아야 합니다.

3. `apps/web/app/sitemap.ts:4-29`, `apps/web/lib/db/fonts.ts:77-87` / sitemap이 오래되거나 일부만 생성될 수 있음 / `force-static`이라 DB 내용은 빌드 시점에 고정됩니다. 이후 파이프라인이 폰트를 발행해도 웹 재배포 전까지 sitemap에 반영되지 않습니다. 또한 Supabase 서버측 행 제한에 걸리면 별도 오류 없이 일부 slug만 반환될 수 있습니다. / prod 폰트 발행 뒤 웹 재배포를 필수 단계로 묶거나 sitemap을 재검증 가능한 주기로 갱신하세요. DB 조회는 `.range()` 페이지네이션과 안정적인 정렬을 사용해야 합니다.

4. `apps/web/app/sitemap.ts:9-18`, `apps/pipeline/src/fontagit_pipeline/noonnu_seed.py:53-72` / DB slug를 URL 경로에 그대로 삽입 / Tier B는 영문명이 없으면 한글 slug를 만들 수 있지만 sitemap은 `${slug}`를 그대로 연결합니다. `/`, `%`, `?`, `#`가 섞인 잘못된 데이터도 방어하지 않습니다. 현재 검증기도 원본 URL의 올바른 퍼센트 인코딩을 확인하지 않습니다. / slug가 단일 경로 조각인지 검증하고 `encodeURIComponent(slug)`로 URL을 만드세요. 잘못된 slug는 sitemap 생성을 실패시키고 원인을 출력하는 편이 안전합니다.

5. `apps/web/app/fonts/[slug]/page.tsx:42` / 공개 상태 판정이 fail-open임 / `const isPublished = (font.status ?? "published") === "published"` 때문에 DB 조회나 변환 코드가 `status`를 누락하면 draft·hold 폰트도 색인 가능 상태가 됩니다. / `font.status === "published"`만 공개로 인정하고, `status`를 필수 타입으로 만들거나 DB 변환 단계에서 검증하세요. 누락·알 수 없는 상태는 반드시 `noindex`로 처리해야 합니다.

6. `apps/web/package.json:9-13`, `apps/web/scripts/verify-seo-output.node-test.mjs:1-51` / SEO 검증이 실제 배포 차단 장치로 연결되지 않음 / 기본 `test`는 `vitest run`뿐이며 Node 테스트와 `verify:seo`는 별도 명령입니다. 이 diff만 보면 CI나 배포 과정에서 실행된다는 보장이 없습니다. 검증도 폰트·컬렉션이 한 개 이상인지만 확인하므로 대량 누락을 통과시킵니다. / `node --test`와 `verify:seo`를 CI 필수 단계로 연결하고, DB 공개 건수와 sitemap 건수가 일치하는지도 확인하세요.

### Medium

1. `apps/web/components/AdFitUnit.module.css:1-13`, `apps/web/components/AdFitUnit.tsx:69-80` / 광고 레이아웃이 좁은 화면에서 깨질 수 있음 / 320px 고정 광고인데 너비 제한이나 중앙 정렬이 없고, `.label`은 `position:absolute`지만 부모에 `position:relative`가 없습니다. 실패 후 영역을 제거하면 “CLS 방지” 설명과 달리 화면이 위로 움직입니다. / 부모에 `position:relative`와 정렬·폭 정책을 추가하고, 광고 실패 시 공간 유지와 접기 중 하나를 명확히 선택하세요.

2. `apps/web/app/about/page.tsx:7`, `apps/web/app/fonts/page.tsx:9`, `apps/web/app/collections/[slug]/page.tsx:34` / canonical 생성 방식이 혼재함 / 정적 페이지는 상대 주소, 컬렉션 상세는 `getSiteUrl()` 절대 주소를 사용합니다. 상대 주소는 루트 `metadataBase` 설정에 의존하며 현재 SEO 산출물 검증은 canonical을 검사하지 않습니다. / 한 가지 방식으로 통일하고 실제 빌드된 HTML에서 canonical origin과 후행 슬래시를 검증하세요.

3. `apps/web/app/about/page.tsx:6-23`, `apps/web/lib/seo.ts:7` / 서비스 설명이 서로 충돌함 / 소개 페이지는 “국내외 폰트”로 확대했지만 전역 설명은 계속 “한국 웹폰트”입니다. 실제 데이터가 국내 중심이면 “국내외”도 과장일 수 있습니다. / 현재 공개 폰트 범위를 확인한 뒤 전역 설명과 소개 문구를 같은 기준으로 맞추세요.

4. `apps/pipeline/src/fontagit_pipeline/noonnu_enrich.py:447-450,482-505` / 자동 발행과 제안 저장이 원자적이지 않음 / 폰트를 먼저 `published`로 변경한 뒤 제안 저장이 실패하면, 다음 실행은 이미 published라는 이유로 건너뜁니다. 감사 기록이 영구 누락될 수 있습니다. / DB 함수(RPC)로 두 변경을 한 트랜잭션에서 처리하는 것이 가장 안전합니다. 최소한 제안 저장 실패 시 발행 상태를 되돌리고 별도 실패 큐에 남겨야 합니다.

### 긍정적 관찰

- `NEXT_PUBLIC_ADFIT_UNIT_*`는 브라우저에 공개되는 광고 단위 ID이므로 시크릿 노출로 보지 않습니다. 서버용 Supabase secret key와도 분리되어 있습니다.
- AdFit ID를 정규식으로 제한하고 React 속성으로 전달해 직접적인 HTML·스크립트 삽입 XSS 경로는 보이지 않습니다.
- sitemap을 `published` 폰트로 제한하고 hold·검색·없는 페이지에 `noindex`를 적용한 방향은 맞습니다.
- Tier B의 눈누 URL 허용 목록, SSRF 방어, `license_proposals`의 RLS(행 단위 접근 제어) 잠금은 적절합니다.