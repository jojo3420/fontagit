### 종합 평가
- 전체 점수: 9/10
- 한줄 총평: 눈누 official_url/license_source_url 오염 정정 파이프라인을 순수 함수 분리, 방어적 게이트 설계, 실측 검증 및 회귀 테스트 확충을 통해 모범적으로 완성한 PR입니다.
- 머지 권고: 즉시 머지 가능

---

### 관점별 리뷰 (표)
| # | 관점 | 평가 | 핵심 지적 |
|---|------|------|---------|
| 1 | 정합성 | PASS | PR 설명의 4가지 실행 차단 결함 해결 및 0027 마이그레이션/파이썬 코드가 정확히 일치함 |
| 2 | 영향도 분석 | PASS | `fetch_scan_html` 하위 호환 위임 및 `auto_applicable_only` 기본값 도입으로 기존 호출처 부작용 없음 |
| 3 | SOLID (SRP) | PASS | `noonnu_url_ingest.py` 순수 함수 독립으로 크롤-판정-감사적재 책임 분리 우수 |
| 4 | 선제적 위험 식별 | WARN | 스캔 중 예외 발생 시 `font_audit_runs`가 `'running'` 상태로 DB에 고립(orphan)될 수 있음 |
| 5 | 방어적 입력 처리 | PASS | CLI 인자 검증(`--store-findings` dev 한정, `--retry-failed` 조건) 및 URL 식별자 추출 예외 처리 철저 |
| 6 | 보안 (OWASP) | PASS | SQL Injection 방지(PL/pgSQL jsonb 스키마 검증), 시크릿 노출 없음, `_audit_manifest_service_role` 검증 유지 |
| 7 | Silent Failure 방지 | PASS | `_ingest_record` DB 저장 예외 전파 보장. 요약 집계 타입 캐스팅 시 안전한 fallback 처리 |
| 8 | 테스트 | PASS | Ingest/Approve CLI/AuditStore/0027 연동 테스트 3건 추가 (489개 테스트 통과) |
| 9 | 코드 스타일/가독성 | PASS | 한국어 도메인 주석, Type Hinting, Dataclass 활용 등 가독성과 도출맥락 설명이 훌륭함 |
| 10 | 머피의 법칙 | WARN | DB 적재 실패 시 `state.jsonl` 기록 전 중단되어 재시도는 안전하나, 이전 run에 부분 스냅샷 잔재 남음 |
| 11 | SQL 근거 검증 (0027) | PASS | `official_url` 우회 대상을 `metadata` + `reference` + 지정 `evidence_role`로 제한하여 legal 필드 우회 차단 |

---

### Critical 이슈 - Must-fix (머지 차단)
*해당 사항 없음 (dev/prod 실측 검증 완료 및 코드상 머지 차단 결함 없음)*

---

### High 이슈 - Should-fix (머지 전 수정 권장)

#### 1. 스캔 예외 발생 시 `font_audit_runs` 상태 고립 (Orphaned Run Status)
- **파일**: [__main__.py](file:///Users/joel.silver/Workspace/gitroom/python/fontagit/apps/pipeline/src/fontagit_pipeline/__main__.py#L280-L325)
- **문제**: `--store-findings` 옵션으로 실행 중 `scan_targets` 내부에서 `ScanAbortedError`나 네트워크/DB 예외가 발생하면 `complete_run`이 호출되지 않아 `font_audit_runs` 테이블의 run 상태가 `'running'`으로 영구 남아있게 됩니다.
- **근거**:
  ```python
  # __main__.py:280
  run_id = store.start_run(...)
  # ...
  records = scan_targets(...) # 여기서 예외 발생 시 complete_run 미도달
  ```
  `__main__.py:1533`에서 `run_status != "completed"`를 검사하므로, 고립된 run은 승인할 수 없어 안전하긴 하지만 DB에 불필요한 incomplete run 데이터가 남습니다.
- **제안**: `try ... except / finally` 블록으로 스캔 과정을 감싸 예외 중단 시 run 상태를 `'failed'` 또는 `'aborted'`로 마감하는 에러 핸들링을 추가하는 것을 권장합니다.

---

### Medium 이슈 (다음 iteration)

#### 1. Reference evidence role 교차 허용 구조 (0027 SQL)
- **파일**: [0027_manifest_noonnu_url_evidence.sql](file:///Users/joel.silver/Workspace/gitroom/python/fontagit/supabase/migrations/0027_manifest_noonnu_url_evidence.sql#L180-L195)
- **문제**: `0027` 마이그레이션의 reference 우회 조건문에서 `font-file-script`와 `noonnu-official-url-anchor`가 `in ('font-file-script','noonnu-official-url-anchor')`로 일괄 묶여 있습니다. 이로 인해 이론상 `noonnu-official-url-anchor` 스냅샷으로 `foundry`나 `download_url` 등의 finding 근거 검증도 통과할 수 있습니다.
- **제안**: 현재 허용 필드가 전부 legal이 아닌 reference metadata 필드(`foundry`, `download_url`, `official_url` 등)라 실질적 위험은 낮으나, 향후 근거 등급을 정밀하게 통제해야 한다면 필드별로 매핑되는 `evidence_role`을 1:1로 한정하는 방식을 검토할 수 있습니다.

#### 2. `expected_findings` 계산 시 더미 UUID 생성
- **파일**: [__main__.py](file:///Users/joel.silver/Workspace/gitroom/python/fontagit/apps/pipeline/src/fontagit_pipeline/__main__.py#L314-L316)
- **문제**: 단순 예상 건수 로깅 목적으로 루프를 돌며 `build_finding_drafts(record, uuid4())`를 호출하여 매번 무의미한 `uuid4()` 객체를 생성합니다.
- **제안**: 건수 집계 목적이라면 고정 더미 UUID(`UUID(int=0)`)를 전달하거나 단순 건수 계산 리팩토링을 고려해볼 수 있습니다.

---

### 긍정적 관찰
1. **실측 기반 결함 교정**: dev 실행 중 발견된 4가지 차단 결함(0017 제약 위반, 0026 게이트 거부, run 마감 실패, 승인 범위 초과)을 임시 조치 대신 0027 SQL 마이그레이션과 3건의 회귀 테스트로 근본 해결한 점이 돋보입니다.
2. **SRP 준수 및 테스트 용이성**: `noonnu_url_ingest.py`를 pure function 모듈로 설계하여 DB/네트워크 의존성 없이 변환 logic과 해시 결정성을 100% 단위 테스트 가능하게 만든 구조가 매우 우수합니다.
3. **안전한 사람 검수 게이트**: `auto_applicable_only` 기본값을 도입하여 자동 적용 대상(`auto_fix_safe`)과 사람 개별 검수 대상(`manual_review`, `nullify`)을 명확히 분리함으로써 실수로 인한 bulk approval 위험을 철저히 막았습니다.
