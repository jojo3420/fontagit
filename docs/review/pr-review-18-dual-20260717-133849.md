# Dual PR Review Report: #18 — feat: 클릭 rate limiting DB 최후방어 (슬라이스3 후속, 0008)
> Generated: 2026-07-17 13:38:49 KST
> PR URL: https://github.com/jojo3420/fontagit/pull/18
> Base ← Head: main ← feat/click-rate-limiting
> Reviewers: Codex (gpt-5.5, xhigh) + Antigravity agy (Gemini 3.5 Flash High)
> Mode: **Degraded — Codex 단독** (agy가 리뷰 대신 `--dangerously-skip-permissions` 플래그 설명 + 메타 질문을 반환 → 유효 리뷰 실패)

주의: agy 미참여로 "두 모델 합의"는 없다. 아래 판정은 Codex 리뷰를 Claude가 PR diff/실코드와 대조해 검증한 결과다.

---

## 0. PR 요약
- 변경 규모: +약 138 lines(코드 2파일) + 설계/계획/리뷰 문서. 4커밋.
- 핵심 변경: `record_click`을 0008로 교체(advisory lock 직렬화 + 폰트별 10초 20건 상한), SQL 통합 테스트 신설.

## 1. 모델별 리뷰 원문
- Codex: `docs/review/pr-review-18-codex-20260717-133849.md` (8/10, 🟡 Should-fix 후 머지, Must-fix 0)
- agy: 유효 리뷰 실패(플래그 설명 반환). `docs/review/pr-review-18-agy-20260717-133849.md`

## 2. Claude 통합 크로스 리뷰

### 종합 소견
Codex의 Must-fix는 0건이고 나도 diff에서 머지 차단 결함을 찾지 못했다. 다만 Codex의 Should-fix 2건은 실제 개선점이다: R4 테스트가 함수 정의 문자열만 검사해 가짜 통과가 가능하고(실코드 확인), blocking advisory lock은 봇 폭주 시 연결 대기열 리스크가 있어 위협 모델의 "리소스 보호" 절반을 약화시킨다.

### 항목별 판정
| # | 지적 | 파일:라인 | 출처 | 심각도 | Claude 판정 | 근거 |
|---|------|---------|------|--------|------------|------|
| 1 | R4 테스트 가짜 통과 | click_rate_limit_test.sql:78 | Codex | High | **동의 → Should-fix** | 실코드 확인: `position('pg_advisory_xact_lock' in v_def)`는 함수 주석(0008:31)에도 문자열이 있어, 실제 lock 실행문을 지워도 통과. 실행문 증명 못 함 |
| 2 | blocking lock 폭주 시 연결 대기 | 0008:32 | Codex | High | **동의 → Should-fix** | 실코드 확인: `perform pg_advisory_xact_lock`(blocking). 봇이 같은 폰트 동시 대량 요청 시 lock 대기로 DB/PostgREST 연결 점유. insert는 막아도 연결 자원 소모 → 리소스 보호 약화 |
| 3 | lock key namespace 없음 | 0008:32 | Codex | Medium | **부분 동의 → Nice** | grep 확인: advisory lock은 0008이 유일 사용처라 충돌 현재 실재 없음. 향후 다른 lock 추가 대비 2-key는 저비용 방어 |
| 4 | 설계 문서 상태 불일치 | design.md:6 | Codex | Medium | **동의 → Nice** | 확인: "상태: 설계 확정 대기"인데 구현 완료. 정합성 위해 갱신 |
| 5 | progress 리뷰 표현 과장 | progress.md | Codex | Medium | **부분 동의 → Nice** | progress의 "최종 리뷰(Opus) YES"는 실제 있었던 최종 브랜치 리뷰 결과(사실)라 과장 아님. 단 Codex가 설계단계 dual(Degraded)과 혼동. 근거 파일 추적성만 개선 여지 |

### 모델 합의도 분석
- 합의 지적: 0건 (agy 미참여 — Degraded)
- Codex 단독: 5건 (Claude diff 대조 검증)
- Claude 자체 발견 Critical: 0건

### 동의한 핵심 이슈 (Top 2)
1. **#2 blocking lock 리소스 리스크** — `pg_try_advisory_xact_lock`으로 바꿔 획득 실패 시 즉시 return하면 대기 없이 리소스 보호. rate limit 목적(과다 기록 차단)엔 "경합 시 더 적게 기록"이 오히려 안전하고 fire-and-forget과도 일치. 이 함수엔 try 방식이 blocking보다 적합.
2. **#1 R4 강화** — `position('perform pg_advisory_xact_lock' in v_def)` 등 실행문 확인 + lock이 count보다 앞인지 검사.

### 비동의/패스 없음 (전부 유효, 심각도만 조정)

## 3. 최종 권고 및 머지 결정

### 머지 결정: ⏸ 사용자 확인 대기 (Must-fix 0 → 머지 가능)

### Must-fix (머지 차단): 0건

### Should-fix (권고)
- S1: R4 테스트를 실행문 확인으로 강화(click_rate_limit_test.sql:78).
- S2: blocking `pg_advisory_xact_lock` → `pg_try_advisory_xact_lock`(획득 실패 시 return)으로 리소스 보호 강화(0008:32). 트레이드오프: 경합 시 정확히 20건 카운트는 완화되나 rate limit 목적엔 안전.

### Nice-to-have
- N1: lock key를 2-key namespace로(`hashtext('fontagit.record_click'), hashtext(p_slug)`).
- N2: 설계 문서 상태 "구현 완료, PR 리뷰 중"으로 갱신.
- N3: progress의 최종 리뷰 근거 추적성 명확화.

## 4. 다음 단계
- Must-fix 0이라 머지 가능. Should-fix 2건(특히 S2)은 실제 개선이라 이 PR 반영 권장.
- 반영 시: 0008 수정 + R4 강화 + dev psql 재실측 → 재푸시.

## 5. 메타데이터
- Codex 종료 코드: 0 / agy 종료 코드: 0(유효 리뷰 실패 — 플래그 설명 반환)
- PR diff 크기: 818줄
- Codex 리뷰: `docs/review/pr-review-18-codex-20260717-133849.md`
- agy 리뷰: `docs/review/pr-review-18-agy-20260717-133849.md`(무효)
- 이 통합 리포트: `docs/review/pr-review-18-dual-20260717-133849.md`
