### 종합 평가
- 전체 점수: 4/10
- 한줄 총평: 빌드 오류와 무한스크롤 재연결 결함 때문에 현재 상태로는 핵심 기능이 정상 동작하지 않습니다.
- 머지 권고: Must-fix 해결 전 불가

### 관점별 리뷰 (표)

| # | 관점 | 평가 | 핵심 지적 |
|---|---|---|---|
| 1 | 정합성 | 미흡 | 정렬 변경 후 무한스크롤이 멈출 수 있어 PR 설명과 다릅니다. |
| 2 | 영향도 | 주의 | 전역 `next/navigation` 모킹이 기존 테스트를 무력화할 수 있습니다. |
| 3 | SRP | 주의 | 검색 제어 로직이 Hero에 다시 구현되어 헤더와 재차 달라질 위험이 있습니다. |
| 4 | 선제 위험 | 미흡 | sentinel이 다시 생성돼도 Observer가 재연결되지 않는 경로가 있습니다. |
| 5 | 방어적 입력 | 보통 | 검색어 정리·인코딩은 적절하지만 빈 자동완성의 키보드 처리가 불안정합니다. |
| 6 | 보안 | 양호 | 검색어 인코딩을 적용했고 직접 HTML 삽입이나 시크릿 노출은 없습니다. |
| 7 | Silent Failure | 주의 | 자동완성 항목이 없는데도 존재하지 않는 항목을 활성화할 수 있습니다. |
| 8 | 테스트 | 미흡 | 새 상태 전환과 Observer 실패 경로를 검증하는 테스트가 없습니다. |
| 9 | 코드 스타일 | 실패 | `FormEvent` 타입을 가져오지 않아 TypeScript 빌드가 실패합니다. |
| 10 | 머피의 법칙 | 실패 | 목록을 끝까지 본 뒤 정렬하면 36개에서 더 로드되지 않을 수 있습니다. |

### Critical 이슈 - Must-fix (머지 차단)

1. `apps/web/components/Hero.tsx:74`  
문제: `FormEvent`를 import하지 않아 TypeScript 컴파일 오류가 발생합니다.  
근거(diff 인용): `const handleSubmitSearch = (e?: FormEvent<HTMLFormElement>) => {`이 추가됐지만 React import에는 `FormEvent`가 없습니다.  
제안 수정: React에서 `FormEvent`를 타입으로 가져오세요. `React.KeyboardEvent`도 같은 방식으로 `KeyboardEvent` 타입을 명시적으로 가져오면 더 안전합니다.

2. `apps/web/components/ClientFontsList.tsx:43-58, 97-99`  
문제: sentinel이 다시 생성돼도 `IntersectionObserver`가 연결되지 않을 수 있습니다.  
근거(diff 인용): effect 의존성이 `[sorted.length]`뿐이고 sentinel은 `displayedFonts.length < sorted.length`일 때만 렌더링됩니다. 전체 목록을 로드해 sentinel이 사라진 뒤 정렬하면 `sorted.length`는 그대로입니다. 표시 개수는 36으로 초기화되어 새 sentinel이 생기지만 effect는 재실행되지 않습니다.  
제안 수정: effect가 `filterKey`와 표시 개수 변화에도 sentinel을 다시 확인하도록 변경하세요. sentinel이 사라질 때도 즉시 `disconnect()`되도록 수명 주기를 맞춰야 합니다.

### High 이슈 - Should-fix

1. `apps/web/components/ClientFontsList.tsx`, `apps/web/components/Hero.tsx`  
문제: 이번 PR의 핵심 상태 전환을 검증하는 테스트가 없습니다.  
근거(diff 인용): 테스트 파일 변경 없이 `vitest.setup.ts`의 모킹만 추가됐습니다.  
제안 수정: 최소한 다음 두 경로를 검증하세요.

   - 목록 끝까지 로드 → 정렬 변경 → 다시 다음 페이지가 로드되는지
   - 한글 조합 중 Enter는 제출하지 않고, 일반 Enter는 `/search?q=`로 이동하는지

2. `apps/web/vitest.setup.ts:25-37`  
문제: `next/navigation`을 전역에서 고정 모킹해 URL과 이동 동작을 검사하는 기존 테스트가 거짓 통과할 수 있습니다.  
근거(diff 인용): `useSearchParams: () => new URLSearchParams()`와 매 호출마다 새 `push: vi.fn()`을 반환합니다. 테스트에서 실제 전달된 검색 조건이나 `push` 호출을 확인하기 어렵습니다.  
제안 수정: 공유 가능한 `push` spy와 변경 가능한 search params를 제공하거나, 필요한 테스트 파일 안에서만 모킹하세요.

### Medium 이슈

1. `apps/web/components/Hero.tsx:36-64`  
자동완성 결과가 0개일 때 ArrowDown은 `activeIndex`를 `0`으로 만듭니다. 이 경우 `aria-activedescendant`가 존재하지 않는 항목을 가리킵니다. 항목이 없으면 인덱스를 `-1`로 유지하고 목록 변경 시 범위를 다시 맞추세요.

2. `apps/web/components/Hero.tsx:13-80`  
헤더와 공유하는 것은 자동완성 표시 컴포넌트와 데이터 훅뿐입니다. 제출, 키보드, 한글 조합, 선택 로직은 Hero에 별도로 구현되어 향후 다시 달라질 수 있습니다. 검색 제어 로직을 공통 훅으로 묶는 편이 안전합니다.

3. `apps/web/components/ClientFontFilters.tsx:20-24`  
렌더링할 때마다 1,240개 폰트를 카테고리별로 네 번 순회합니다. 현재 규모에서는 치명적이지 않지만, 빌드 단계에서 `{고딕: n, ...}` 형태의 메타만 계산해 전달하면 책임과 데이터 크기가 더 명확해집니다.

### 긍정적 관찰

- 렌더 중 상태 조정은 `filterKey !== prevKey`로 제한되어 있어 현재 코드만 보면 무한 렌더링은 발생하지 않습니다. 필터 변경마다 추가 렌더링 1회는 발생합니다.
- `Math.min(..., sorted.length)`으로 표시 개수가 전체 길이를 넘지 않게 막았습니다.
- Observer 정리 함수로 `disconnect()`를 사용한 방향은 맞습니다. 다만 재연결 의존성 보완이 필요합니다.
- 검색어에 `trim()`과 `encodeURIComponent()`를 적용했습니다.
- 한글 입력 조합 상태와 외부 클릭 이벤트 정리를 처리했습니다.
- `useSearchParams`를 사용하는 클라이언트 컴포넌트가 `Suspense` 안에 있어 SSG `output: "export"` 구성과 맞습니다.