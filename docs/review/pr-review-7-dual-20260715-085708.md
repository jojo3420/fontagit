# Dual PR Review Report: #7 — feat: Slice 0 data pipeline with license-gated Supabase upload
> Generated: 2026-07-15
> PR URL: https://github.com/jojo3420/fontagit/pull/7
> Base ← Head: main ← develop
> Reviewers: Codex (gpt-5.5, xhigh) + Antigravity agy (Gemini 3.5 Flash High)
> Mode: **Degraded — Codex 단일 모델** (agy 인증 만료로 실패, exit=1)

---

## 0. PR 요약
- 제목: feat: Slice 0 data pipeline with license-gated Supabase upload
- 작성자: jojo3420
- 변경 규모(코드 파일): 약 +1048 lines (파이프라인 src/tests + supabase 마이그레이션)
- 핵심: 구글폰트 수집 → 라이선스 판별 → Supabase fontagit 스키마 멱등 업로드, OFL/Apache/UFL 자동 공개.

---

## 1. 모델별 리뷰 상태
- Codex: ✅ 완료 (exit 0) — `docs/review/pr-review-7-codex-20260715-085708.md`
- agy: ❌ 실패 (exit 1) — Google OAuth 인증 만료("Authentication required ... authentication timed out"). 재리뷰하려면 `agy` 대화형 로그인 필요.

---

## 2. Claude 통합 크로스 리뷰 (Codex 단독 diff 대조)

### 항목별 판정
| # | 지적 | 위치 | 출처 | Codex 심각도 | Claude 판정 | 근거 |
|---|------|------|------|--------------|-------------|------|
| 1 | 라이선스 조회 실패 시 빈 map → 전 레코드 draft → upsert가 기존 published 덮음 | `licenses.py` fetch_license_map + `__main__.py` upload | Codex | Critical | **동의 → Must-fix** | fetch_license_map은 HTTPError 시 `{}` 반환. 빈 map이면 to_record가 전부 verified=False/status=draft. upload_records upsert(on_conflict=slug)가 기존 published row의 status를 draft로 갱신. GITHUB_TOKEN 부재로 rate limit 빈발 → 재실행 시 공개 폰트 전량 비공개 |
| 2 | published=검증 라이선스 규칙을 DB/모델이 강제 안 함 | `models.py` validator + `0001_...sql` chk/RLS | Codex | Critical | **하향 → Should-fix** | 현재 유일 쓰기 경로 to_record가 verified일 때만 published로 설정 → 실제 잘못된 데이터는 안 들어감. 방어 심화(defense-in-depth)로 권장이나 머지 차단 사유 아님 |
| 3 | Supabase 설정 하나만 빠지면 조용히 업로드 스킵 | `__main__.py` `if url and secret` | Codex | High | 동의 → Should-fix | url/secret 하나만 있으면 else 분기로 무경고 스킵. 운영 설정 실수 은폐 가능 |
| 4 | 업로드 비원자적(fonts 성공 후 aliases 실패 시 중간상태) | `uploader.py` upload_records | Codex | High | 동의 → Should-fix | 멱등 upsert라 재실행 수렴은 되나, 실패 지점 로그/처리 부재 |
| 5 | Supabase 응답 무검증 `data[0]["id"]` | `uploader.py` upload_records | Codex | High | 동의 → Should-fix | 빈 data면 IndexError로 중단(부분 업로드). slug 식별 로그 없음 |
| 6 | grant(authenticated) vs RLS policy(anon만) 불일치 | `0001_...sql` grant/policy | Codex | High | 동의 → Low/Medium | authenticated는 grant 있으나 policy 없어 실제 read 불가. 기능 결함 아닌 정리 대상 |
| 7 | updated_at upsert 미갱신 / collection_items가 draft font 노출 / stale alias / alias 정규화 범위 / status-tier Literal 미사용 | 여러 | Codex | Medium | 동의 → 다음 iteration | 개선 권장, 비차단 |

### 모델 합의도
- 합의(Both): 0건 (agy Degraded)
- Codex 단독: 위 전부. Claude가 diff로 개별 재판정.
- Claude 자체 발견 추가 Critical: 없음.

### 환각 점검
- Codex 인용 파일:라인은 실제 diff에 존재. #1의 데이터 손실 경로는 diff상 로직으로 재현 가능(환각 아님). 심각도만 #2에서 하향.

---

## 3. 최종 권고 및 머지 결정

### 머지 결정: ❌ 머지 차단 (Must-fix 1건)

### ❌ Must-fix (합집합)
1. `licenses.py` / `__main__.py`: 라이선스 조회 실패와 "정상적으로 빈 결과"를 구분하라. Supabase 업로드가 켜진 상태에서 fetch_license_map이 실패하면 업로드하지 말고 exit 3(기존 published 보존). 또는 draft 강등 시 라이선스 미확인이 조회 실패 때문인지 검증.

### 🟡 Should-fix (다음 PR 권장, 비차단)
- published/verified/license_type 규칙을 DB CHECK + FontRecord validator에 이중 강제(#2)
- Supabase env 부분 누락 시 무경고 스킵 대신 설정 오류 처리(#3)
- 업로드 실패 시 slug 식별 로그 + 부분 실패 가시화(#4, #5)

### 💡 Nice-to-have / 다음 iteration
- grant/policy 정리(#6), updated_at 트리거, collection_items published 조건, stale alias 정책, Literal 타입(#7)

---

## 4. 다음 단계
- [ ] Must-fix #1 수정(라이선스 조회 실패 시 업로드 중단) → 재푸시 → `/request-pr-dual 7` 재실행
- [ ] agy 리뷰 원하면 `agy` 대화형 로그인 후 재실행(현재 Degraded)

---

## 5. 메타데이터
- Codex 종료 코드: 0 / agy 종료 코드: 1 (인증 실패)
- 코드 diff 크기: 1048줄 (uv.lock/문서 제외 발췌)
- Codex 리뷰: `docs/review/pr-review-7-codex-20260715-085708.md`
- agy 리뷰: 실패(빈 결과)
- 이 통합 리포트: `docs/review/pr-review-7-dual-20260715-085708.md`
