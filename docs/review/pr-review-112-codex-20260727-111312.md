### 종합 평가
- 전체 점수: 7/10
- 한줄 총평: 핵심 설계는 좋지만, “미확인”과 “미제공”을 섞어 표시하는 치명적 UX 오류가 있습니다.
- 머지 권고: Must-fix 해결 전 머지 불가

### 관점별 리뷰 (표)
| # | 관점 | 평가 | 핵심 지적 |
|---|------|------|---------|
| 1 | 정합성 | 보통 | `굵기 미확인` 상태에서 “웹 견본 미제공”이라고 잘못 표시될 수 있음 |
| 2 | 영향도 분석 | 양호 | 상세 페이지 호출부는 반영됨. 단, `variants` 조회 포함 여부는 diff만으론 확인 불가 |
| 3 | SOLID / SRP | 양호 | 변환 유틸, 로더, 표시 컴포넌트 분리는 적절함 |
| 4 | 위험 식별 | 보통 | 폰트 로딩 실패 후 재시도 불가 가능성 있음 |
| 5 | 방어적 입력 처리 | 부족 | `700abc` 같은 잘못된 variant가 `700`으로 통과함 |
| 6 | 보안 | 보통 | `officialUrl` 프로토콜 검증 없이 외부 링크로 사용 |
| 7 | Silent Failure | 보통 | 실패는 화면에 보이나, 실패 링크 캐시/재시도 처리가 약함 |
| 8 | 테스트 | 보통 | 핵심 유틸 테스트는 있음. 상태 분기 테스트는 부족 |
| 9 | 스타일/접근성 | 양호 | 구조는 읽기 쉬움. 스켈레톤 접근성은 보완 여지 |
| 10 | 머피의 법칙 | 부족 | 브라우저 FontFace range, 네트워크 실패 재시도 케이스가 약함 |

### Critical 이슈 — Must-fix (머지 차단)

- `apps/web/components/WeightSpecimenSection.tsx:47-104`
  / 문제: `combos.length === 0`이면 항상 “이 폰트는 웹 견본을 제공하지 않습니다”를 보여줍니다. 그래서 Tier A인데 `variants`가 없거나 굵기 정보가 미확인인 폰트도 “미제공”으로 잘못 표시됩니다.
  / 근거(diff 인용):
  ```tsx
  const confirmed = font.confirmedWeights ?? null;
  const hasRows = combos.length > 0;
  ...
  ) : (
    <p className={styles.rowFallback}>
      이 폰트는 웹 견본을 제공하지 않습니다
  ```
  / 제안 수정: `confirmedWeights === null` 또는 Tier A `variants` 미확인은 “굵기 정보 미확인”만 표시하세요. “웹 견본 미제공”은 Tier B/C처럼 정책상 외부 요청을 하지 않는 경우에만 보여줘야 합니다.

### High 이슈 — Should-fix (머지 전 수정 권장)

- `apps/web/lib/weightLabels.ts:37-47`
  / 문제: `parseInt`가 부분 문자열을 허용합니다. 예를 들어 `"700abc"`가 `700`으로 통과합니다.
  / 근거(diff 인용):
  ```ts
  const weight = weightStr ? parseInt(weightStr, 10) : 400;
  if (!Number.isInteger(weight) || weight < 1 || weight > 1000) continue;
  ```
  / 제안 수정: 정규식으로 허용 형태를 먼저 검증하세요. 예: `regular`, `italic`, `/^\d+$/`, `/^\d+italic$/`만 통과.

- `apps/web/components/DetailSpecimenPanel.tsx:55-65`
  / 문제: variable font의 `FontFace.weight`가 `"100 900"` 같은 범위 문자열이면 실제 로드됐어도 실패로 판정될 수 있습니다.
  / 근거(diff 인용):
  ```ts
  face.weight === String(weight) ||
  face.weight.split(" ").includes(String(weight))
  ```
  / 제안 수정: `"100 900"`이면 요청 weight가 범위 안에 있는지 검사하세요.

- `apps/web/components/DetailSpecimenPanel.tsx:24-45`
  / 문제: stylesheet 로드 실패 시 `<link>`는 DOM에 남습니다. 이후 같은 URL을 다시 로드할 때 기존 링크를 보고 성공 처리할 수 있습니다.
  / 근거(diff 인용):
  ```ts
  link.onerror = () => {
    stylesheetPromises.delete(url);
    reject(new Error("stylesheet load failed"));
  };
  ```
  / 제안 수정: `onerror`에서 실패한 link를 제거하거나 실패 상태를 표시해 재시도 가능하게 만드세요.

### Medium 이슈 (다음 iteration)

- `apps/web/components/WeightSpecimenSection.tsx:96-100`
  / 문제: `officialUrl`을 그대로 `href`에 넣습니다. DB 값이 오염되면 `javascript:` 같은 위험한 URL이 들어갈 수 있습니다.
  / 근거(diff 인용):
  ```tsx
  <a href={font.officialUrl} target="_blank" rel="noopener noreferrer">
  ```
  / 제안 수정: `http:` / `https:`만 허용하는 URL 정규화 함수를 거치세요.

- `apps/web/app/fonts/[slug]/page.tsx:122`
  / 문제: `confirmedWeights`가 빈 배열이면 truthy라서 `0가지 굵기`로 보일 수 있습니다.
  / 근거(diff 인용):
  ```tsx
  font.confirmedWeights ? `${font.confirmedWeights.length}가지 굵기` : "굵기 정보 미확인"
  ```
  / 제안 수정: `font.confirmedWeights?.length ? ... : "굵기 정보 미확인"`로 처리하세요.

- `apps/web/components/SpecimenBox.tsx:20-39`
  / 문제: `text`만 넘기고 `onTextChange`를 안 넘기면 입력창이 바뀌지 않는 controlled 상태가 됩니다.
  / 근거(diff 인용):
  ```ts
  const isControlled = controlledText !== undefined;
  const setText = (next: string) => {
    if (!isControlled) setInnerText(next);
    onTextChange?.(next);
  };
  ```
  / 제안 수정: 개발 모드에서 경고하거나, `text`와 `onTextChange`를 함께 받는 타입으로 분리하세요.

### 긍정적 관찰

- `availableWeights`와 `confirmedWeights`를 분리한 판단은 좋습니다. 기존 UI 회귀를 줄이면서 새 화면의 의미를 명확히 나눴습니다.
- `normalizeVariants`, `resolveItalicSupport`, `resolveDetailFontPreview`를 순수 함수로 뺀 점은 테스트하기 쉽고 유지보수에도 유리합니다.
- Tier B/C에서 외부 요청을 만들지 않는 정책은 명확합니다.
- `font-synthesis: none`을 견본 행에 적용한 것은 “가짜 굵기/이탤릭” 방지 목적과 잘 맞습니다.
- 로드 실패를 행 단위로 보여주는 방식은 사용자에게 숨기지 않는 좋은 처리입니다.