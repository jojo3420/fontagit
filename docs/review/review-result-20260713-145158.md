# Review Report: 2026-07-13-fontagit-web-phase3-4.md
> Generated: 2026-07-13 14:52
> Codex CLI: codex-cli 0.144.1 (model gpt-5.6-sol, reasoning xhigh)

---

## 1. Codex 리뷰 요약

### 종합 평가
- 전체 점수: 7/10
- 한줄 요약: 대부분 바로 구현할 수 있지만, OG 대체안-다크모드 초기화-검증 절차의 모순은 구현 전에 보완해야 합니다.

### 항목별 리뷰
| 관점 | 평가 | 상세 |
|------|------|------|
| 완성도 | 주의 | 코드/경로는 구체적이나 Task 9 대체안-마감 문서 작업이 불완전 |
| 누락 항목 | 위험 | OG 한글 렌더링, 실제 메타 태그 검사, 다크모드 초기화 조건, 모바일 시각 비교 누락 |
| 일관성 | 위험 | Task 9가 인라인/색상/보이스 규칙 위반, Task별 검증 원칙과 실제 명령 불일치 |
| 실현 가능성 | 주의 | 주요 화면 구현 가능, usePathname OK. OG-테마 초기화는 조건 확인 필요 |

### Codex 구체적 피드백 (14건)
1. Task 9: ImageResponse는 export에서 정적 생성 가능-generateStaticParams 맞음. 단 빌드 성공만으론 부족 — PNG 실제 생성/og:image 태그/1200x630/한글 깨짐 확인 필요
2. Task 9: "미지원 시 PNG 대체"에 실행 방법 없음 — `public/og/fonts/{slug}.png` + `generateMetadata` 연결까지 명시해야
3. Task 9: ImageResponse가 인라인 스타일-하드코딩 색상 사용 → Global Constraints 충돌, 예외 명시 필요. "안전하게 연결"은 보증 표현 위반
4. Task 9: `fontFamily:"sans-serif"`만으로 한글 정상 출력 단정 불가 — 한글 폰트 임베드 또는 PNG 직접 검사
5. Task 8: 컴포넌트 자체는 mismatch 회피. 단 인라인 스크립트가 data-theme를 바꾸므로 suppressHydrationWarning 확인 + Task 8에 layout.tsx 포함. 클릭은 DOM 속성 기준 계산 권장
6. Task 7: usePathname 정적 export OK. 단 탭 터치 영역 44px 미만 가능 → 세로 전체로 확대
7. Task 3: 모듈 최상위 assert 유효-순환참조 없음. 단 신규 테스트가 검증 함수 예외를 확인 안 하고 조건 재검사 — 순수 함수 분리 후 negative 테스트
8. Task 5: 입력칸+버튼 있으니 div는 접근성 부적절 — form + preventDefault 권장, 유지 시 role="form"+안내
9. Task 2: change() 정상. 테스트가 첫 견본만 확인 — 3개 견본-font-family 실제 변화 확인
10. Task 1: 그리드는 무료만인데 제목 "전체 폰트에서 보기" + 개수도 HERO 포함해 어긋남
11. 전체: "tsc+test+build+Playwright 자립" 선언과 실제 검증 명령 불일치
12. 전체: baseline 스크린샷은 디자인 일치 검증 아님 — 핵심 화면 1280/390 원본 대조 승인 단계 추가
13. Task 4/9: 컬렉션 상세 generateMetadata 권장. Self-Review에 Task 9가 컬렉션 헬퍼 쓴다는 오기
14. 마감: progress 갱신을 독립 Task로 분리. zsh 글로빙 — `[slug]` 경로 따옴표 필요

### Codex 개선 제안 (우선순위)
- 최우선: Task 9 OG 정적 판정+한글 검사+PNG 대체 절차 / Task 8 layout+suppressHydrationWarning
- 높음: 검증 명령 통일 / 무결성 순수함수 분리+negative 테스트
- 보통: 등록 화면 시맨틱 form / 원본 대조+진행 문서 별도 Task

---

## 2. Claude 크로스 리뷰

### 종합 소견
Codex 리뷰는 사실 기반이며 특히 Task 9(OG)의 위험을 정확히 짚었습니다. 14건 중 10건 동의, 3건 부분 동의(전제 일부가 이미 처리됨), 1건은 실제 코드와 대조해 보완했습니다. 가장 치명적인 건 #4(한글 OG 깨짐) — next/og의 satori 렌더러는 한글 글리프를 기본 제공하지 않아 폰트를 명시 전달하지 않으면 거의 확실히 네모(tofu)로 렌더됩니다. #14 zsh 글로빙은 커밋 명령 자체를 실패시킵니다.

### 항목별 판정

| # | Codex 피드백 | 판정 | Claude 의견 |
|---|-------------|------|------------|
| 4 | Task 9 한글 폰트 위험 | 동의(최우선) | satori는 시스템 폰트 접근 불가. `sans-serif`로는 한글 tofu 확정적. 빌드 타임 `fs.readFile`로 Pretendard woff을 읽어 `ImageResponse`의 `fonts` 옵션에 전달해야. 기본 OG("당신의 폰트 아지트")-폰트별 OG(nameKo) 둘 다 영향. icon("A")만 무관 |
| 14 | zsh 글로빙 따옴표 | 동의(Must) | Task 9 커밋 `git add apps/web/app/fonts/[slug]/opengraph-image.tsx`는 zsh에서 `[slug]` glob 해석 → "no matches" 실패. 경로 따옴표 필수. Task 1/2 커밋의 `apps/web/e2e/*-snapshots/`도 미존재 시 nullglob 이슈 → `2>/dev/null` 이미 있어 OK |
| 3 | Task 9 규칙 위반 | 동의(Must) | satori는 CSS Modules/변수 불가 → 인라인+하드코딩이 기술적 필연. Global Constraints에 "OG/아이콘 생성 코드는 인라인 스타일+토큰값 복제 예외" 명시 누락은 문서 결함. "안전하게 연결"은 보증 표현 위반 → "공식 페이지에서 라이선스 확인" 등으로 수정 |
| 1 | Task 9 검증 부족 | 동의 | 빌드+ls만으론 부족. og:image 메타 태그가 PNG를 가리키는지 + 생성 PNG 육안(한글) 확인 추가 |
| 10 | Task 1 문구 불일치 | 동의 | GRID=무료&!pretendard=8종, 제목 "전체 폰트"는 유료 sandoll 제외라 부정확. "무료 폰트에서 보기"로 수정. 개수 표기도 "대표 1 + N종"으로 명확화 |
| 13 | Self-Review 오기 | 동의 | 내 Self-Review "Task 4/9에서 동일 시그니처 소비"는 오기 — Task 9는 getAllSlugs/getFontBySlug만 사용. 수정. 컬렉션 상세 generateMetadata 추가는 타당 |
| 2 | Task 9 fallback 불완전 | 부분 동의 | "미지원 시 PNG 대체"는 조건부라 우선순위 중간이나, 절차가 추상적인 건 사실. 단 #1에서 Codex 스스로 "ImageResponse는 export에서 정적 생성 가능"이라 했으므로 fallback 발동 가능성은 낮음. 절차는 간단히 보강 |
| 5 | Task 5 폼 시맨틱 | 부분 동의 | div 결정은 "비동작 목업" 의도였으나 입력칸+버튼이 있으면 form이 접근성상 옳음. `<form onSubmit={e=>e.preventDefault()}>`(use client 최소 래퍼)로 시맨틱+비동작 양립. "폼 비동작" 결정은 유지되며 시맨틱만 개선 |
| 7 | Task 7 터치 영역 | 부분 동의 | usePathname은 문제없음(Codex도 동의). 탭 링크를 `display:flex;height:100%;align-items:center`로 세로 확대는 WCAG 타깃 크기 개선으로 타당 |
| 8 | Task 8 초기화 | 부분 동의 | suppressHydrationWarning은 이미 `layout.tsx`에 존재(Phase 1-2) → Codex 전제 일부 이미 해결. Task 8 수정 파일에 layout 포함 불필요(변경 없음). 단 문서에 "이미 존재함" 명시 + toggle을 DOM 속성 기준으로 계산(초기 클릭 경쟁 방지)은 유효 개선 |
| 11 | 검증 원칙 불일치 | 부분 동의 | 선언("전부 자립")과 실제(로직=vitest, 화면=build+스모크)가 다른 건 사실. 단 전부 full 세트는 과함. "선언 문구를 실제에 맞게 정정 + 각 Task 최소 tsc+build 공통"이 옳은 해법 |
| 12 | 95% 시각 검증 | 동의 | baseline 스크린샷은 회귀용이지 원본 대조 아님. 최종 검증에 "핵심 신규 화면 1280/390 원본 육안 대조" 승인 단계 추가 |
| 6 | Task 3 negative 테스트 | 부분 동의 | 빌드 타임 assert가 이미 방어선. 단 negative 테스트가 없으면 검증 로직 회귀를 못 잡음. 검증부를 `checkIntegrity(fonts, collections, keys)` 순수 함수로 분리 후 잘못된 입력에 throw 확인은 가치 있음(Should) |
| 9 | Task 2 테스트 강화 | 부분 동의 | 스모크 목적(회귀 감지)엔 현재도 최소 충분. 3견본-font-family 확인 추가는 개선(Nice) |

### Claude가 보완한 관점 (Codex 미포착)
- **Task 1 CSS 죽은 코드:** `.heroSpecimen`의 `font-family: var(--font-hero, "Pretendard Variable")`는 정의되지 않은 변수 fallback인데, 실제 렌더는 인라인 `style={{fontFamily}}`가 이겨 무의미하다. CSS에서 font-family 줄을 제거하고 색/크기만 두어야 혼란이 없다.

### 동의하지 않는 부분
- 없음(전면 반박 항목 없음). #8만 "layout 수정 포함"은 불필요 — 해당 파일은 변경 없이 이미 조건 충족.

---

## 3. 최종 권고사항

### 즉시 반영 (Must — 구현 착수 전)
1. **Task 9 한글 OG(#4):** 빌드 타임 `fs.readFile`로 Pretendard(또는 재배포 가능 한글 폰트) woff을 읽어 `ImageResponse`의 `fonts:[{name,data,weight}]`로 전달. 기본 OG + 폰트별 OG 모두 적용.
2. **Task 9 zsh 글로빙(#14):** 커밋 명령의 `apps/web/app/fonts/[slug]/opengraph-image.tsx`를 따옴표로 감싸기.
3. **Task 9 규칙 예외-보이스(#3):** Global Constraints에 "OG/아이콘 코드는 인라인 스타일+토큰값 하드코딩 예외" 추가. "안전하게 연결" → "공식 페이지에서 라이선스 확인"으로 카피 수정.
4. **Task 9 검증(#1):** 검증 스텝에 og:image 메타 태그 확인 + 생성 PNG 한글 육안 확인 추가.
5. **Task 1 문구(#10) + CSS 죽은코드(Claude):** "전체 폰트" → "무료 폰트에서 보기", 개수 표기 명확화, `.heroSpecimen` font-family 줄 제거.
6. **Self-Review 오기(#13):** Task 9 헬퍼 언급 정정.

### 검토 후 반영 (Should)
- Task 5 등록 폼을 `<form onSubmit preventDefault>`(use client 최소 래퍼)로 전환(#5)
- Task 8 toggle을 DOM 속성 기준 계산 + "suppressHydrationWarning 기존재" 문서 명시(#8)
- Task 7 탭 링크 세로 확대(터치 44px)(#7)
- 검증 문구를 실제에 맞게 정정 + 각 Task 최소 tsc+build 공통(#11)
- 최종 검증에 95% 원본 육안 대조 단계 추가(#12)
- progress 갱신을 별도 최종 Task로 분리(#14)

### 참고 (Nice to have)
- Task 3 검증부 순수 함수 분리 + negative 테스트(#6)
- Task 2 3견본-font-family 변화 테스트 강화(#9)
- 컬렉션 상세 generateMetadata(#6/13)
