### 종합 평가
- 전체 점수: 7/10
- 한줄 총평: Next.js 정적 Export 환경을 고려한 아키텍처 설계와 `useDeferredValue`를 활용한 실시간 반응성 확보는 우수하나, 큐레이션 확장 시의 크래시 위험, 무의미한 리렌더링 연산 반복, 그리고 실제 도메인 타입 규격과 동떨어진 테스트 mock 데이터 구조는 머지 전 개선이 필수적입니다.
- 머지 권고: **Should-fix 수정 후 머지** (Must-fix로 분류된 Critical 이슈들이 해결된 후 승인을 권장합니다)

---

### 관점별 리뷰 (표)
| # | 관점 | 평가 | 핵심 지적 |
|---|---|---|---|
| 1 | 정합성 | Pass | 요구사항에 기술된 5개 섹션 자동 매핑 및 추천 큐레이션 우선 노출, 타입 캔버스 상태 전파가 올바르게 작동함. |
| 2 | 영향도 | Pass | Static HTML export 시 `useSearchParams`로 인한 deopt를 막기 위해 `Suspense` 바운더리를 페이지 단에 적절히 적용함. |
| 3 | SOLID (SRP) | Pass | 섹션 판단(`sectionOf`) 및 큐레이션 정렬(`orderByCuration`) 등의 유틸리티를 순수 함수로 격리하여 단일 책임 원칙을 준수함. |
| 4 | 선제적 위험 | Warning | 캔버스 입력(키스트로크)마다 무거운 그룹화 및 정렬 연산이 캐싱(`useMemo`) 없이 매번 호출되어 대형 폰트셋 구동 시 UI 버벅임 유발. |
| 5 | 방어적 입력 처리 | Warning | 1) `availableWeights`가 빈 배열일 때 `body` 대신 `headline`으로 잘못 매핑됨. 2) 잘못된 `?section=invalid` 쿼리 입력 시 빈 화면 노출. |
| 6 | 보안 (OWASP) | Pass | 클라이언트 사이드 상태 전파 위주로 구성되어 있어 인젝션 등의 OWASP 취약점 위험도는 낮음. |
| 7 | Silent Failure 방지 | Warning | 타입 캔버스 내 초기화 버튼에 `type="button"`이 유실되어, 상위 양식(Form)이 존재할 시 의도치 않은 서브밋이 발생할 위험이 있음. |
| 8 | 테스트 | Fail | `SectionOverview.test.tsx`의 mock 데이터가 도메인 인터페이스 규격에 맞지 않아(`category: "body"`, `availableWeights` 누락) 잠재적인 테스트 크래시 요인 포함. |
| 9 | 코드 스타일/가독성 | Warning | `TypeCanvasBar.tsx` 컴포넌트에서 디자인 시스템 토큰 대신 하드코딩된 스타일(`background-color: #f5f5f5`)을 사용함. |
| 10 | 머피의 법칙 | Warning | 큐레이션 확장 시 `SECTION_CURATION` 맵에 키(key)가 정의되지 않은 신규 섹션이 생성될 경우 런타임 에러(`TypeError`)로 크래시 발생. |

---

### Critical 이슈 - Must-fix (머지 차단)

#### 1. `orderByCuration` 호출 시 미정의된 섹션 큐레이션으로 인한 런타임 크래시
- **파일:라인** : [SectionOverview.tsx](file:///Users/joel.silver/Workspace/gitroom/python/fontagit/apps/web/components/SectionOverview.tsx#L38) 및 [sections.ts](file:///Users/joel.silver/Workspace/gitroom/python/fontagit/apps/web/lib/sections.ts#L84)
- **문제** : `SECTIONS`를 임의로 확장했을 때 `SECTION_CURATION[section.slug]`가 정의되어 있지 않다면 `recommendedSlugs`가 `undefined`로 전달되며, 이 경우 `recommendedSlugs.map()`을 실행할 때 `TypeError: Cannot read properties of undefined`가 발생하며 페이지 전체가 다운됩니다.
- **근거** :
  ```diff
  // SectionOverview.tsx
  {SECTIONS.map((section) => {
    const fonts = groups[section.slug];
+   const all = orderByCuration(fonts, SECTION_CURATION[section.slug]);
  ```
  ```diff
  // sections.ts
  export function orderByCuration(fonts: Font[], recommendedSlugs: string[]): Font[] {
+   const rank = new Map(recommendedSlugs.map((s, i) => [s, i]));
  ```
- **제안 수정** :
  `orderByCuration`의 매개변수에 기본값을 부여하거나 호출 시 빈 배열을 기본 전달하도록 방어 코드를 작성합니다.
  ```typescript
  // sections.ts
  export function orderByCuration(fonts: Font[], recommendedSlugs: string[] = []): Font[]
  ```
  또는 `SectionOverview.tsx`에서:
  ```typescript
  const all = orderByCuration(fonts, SECTION_CURATION[section.slug] || []);
  ```

#### 2. `SectionOverview.test.tsx` 테스트 Mock 데이터의 도메인 타입(`Font`) 규격 불일치
- **파일:라인** : [SectionOverview.test.tsx](file:///Users/joel.silver/Workspace/gitroom/python/fontagit/apps/web/components/SectionOverview.test.tsx#L34-L52)
- **문제** : 테스트 코드 내 mock 폰트 목록 생성 시 `category` 필드값으로 도메인 규격 외의 값인 `"body"`를 지정하고 있고, 필수 필드인 `availableWeights`를 누락시켰습니다. 추후 테스트 코드에 카테고리별 분류 검증이나 가중치 비교 로직을 추가하는 순간 `availableWeights` 탐색 중 바로 `TypeError`가 발생하게 됩니다.
- **근거** :
  ```diff
  const mockFonts: Font[] = Array.from({ length: 20 }, (_, i) => ({
    // ...
+   category: "body",
+   section: "body", // Font 타입에 정의되지 않은 잘못된 속성
    // availableWeights 누락
  }));
  ```
- **제안 수정** :
  도메인 명세(`Category` = `"고딕" | "명조" | "손글씨" | "장식"`)에 맞추고 필수 값을 보장해 줍니다.
  ```typescript
  const mockFonts: Font[] = Array.from({ length: 20 }, (_, i) => ({
    // ... 다른 속성들
    category: "고딕",
    availableWeights: [400],
  } as Font));
  ```

---

### High 이슈 - Should-fix (머지 전 수정 권장)

#### 1. 대량 리렌더링 유발 시 무거운 연산의 반복 (성능 병목)
- **파일:라인** : [SectionOverview.tsx](file:///Users/joel.silver/Workspace/gitroom/python/fontagit/apps/web/components/SectionOverview.tsx#L27-L49)
- **문제** : 타입 캔버스에 텍스트를 타이핑하여 `previewText`가 변경될 때마다 `SectionOverview`가 항상 리렌더링됩니다. 이때 변하지 않는 정적 데이터인 `fonts` 배열에 대해 `groupFontsBySection(fonts)`와 `orderByCuration`을 매 키스트로크마다 불필요하게 반복 계산합니다.
- **근거** :
  ```diff
  export function SectionOverview({ fonts, topN = DEFAULT_TOP_N, previewText }: ...) {
+   const groups = groupFontsBySection(fonts);
+   // ... 매 렌더링마다 5개 섹션별 orderByCuration 연산 수행
  ```
- **제안 수정** :
  `fonts`가 변경될 때만 연산하도록 `useMemo` 캐싱 처리를 도입하여 키스트로크 오버헤드를 대폭 절감합니다.
  ```typescript
  const groupedAndOrdered = useMemo(() => {
    const groups = groupFontsBySection(fonts);
    return SECTIONS.map((section) => ({
      section,
      curatedFonts: orderByCuration(groups[section.slug] || [], SECTION_CURATION[section.slug] || [])
    }));
  }, [fonts]);
  ```

#### 2. `FontCard.tsx` 내의 불필요한 문자열 연산 및 비어있지 않은 `previewText` 렌더 시의 레이아웃 안정성
- **파일:라인** : [FontCard.tsx](file:///Users/joel.silver/Workspace/gitroom/python/fontagit/apps/web/components/FontCard.tsx#L14-L24)
- **문제** : 
  1) `previewText`가 존재하여 견본 텍스트를 대체하는 경우에도, 사용되지 않는 기존 디폴트 견본 문자열의 `split`, `slice`, `join` 연산이 매 리렌더마다 실행됩니다.
  2) `previewText`가 극단적으로 긴 텍스트일 때 카드 레이아웃이 상하로 무제한 늘어나 visual grid 균일성을 파괴할 우려가 있습니다.
- **근거** :
  ```diff
  const custom = previewText?.trim();
+ const words = (custom || getSpecimenText(font, false)).split(" ");
+ const line1 = words.slice(0, 2).join(" ");
+ const line2 = words.slice(2, 4).join(" ");
  ```
- **제안 수정** :
  `custom` 변수의 유무에 따라 조기에 분기 처리하여 렌더링 효율을 높이고 스타일을 제어합니다.
  ```typescript
  const custom = previewText?.trim();
  let specimenContent;

  if (custom) {
    specimenContent = custom;
  } else {
    const words = getSpecimenText(font, false).split(" ");
    const line1 = words.slice(0, 2).join(" ");
    const line2 = words.slice(2, 4).join(" ");
    specimenContent = <>{line1}<br />{line2}</>;
  }
  ```

#### 3. `TypeCanvasBar.tsx` 하드코딩된 스타일 및 웹 표준 마크업 개선
- **파일:라인** : [TypeCanvasBar.module.css](file:///Users/joel.silver/Workspace/gitroom/python/fontagit/apps/web/components/TypeCanvasBar.module.css#L4) 및 [TypeCanvasBar.tsx](file:///Users/joel.silver/Workspace/gitroom/python/fontagit/apps/web/components/TypeCanvasBar.tsx#L39)
- **문제** :
  1) `#f5f5f5`와 같이 프로젝트 디자인 토큰을 무시한 하드코딩 색상이 지정되어 있어 다크모드 등 테마 대응 시 오작동할 우려가 있습니다.
  2) `<button>` 요소에 `type="button"`이 생략되어 폼 내부 사용 시 뜻하지 않은 Submit 동작을 유발할 수 있습니다.
- **근거** :
  ```diff
  .container {
    display: flex;
    gap: 0.5rem;
    padding: 1rem;
-   background-color: #f5f5f5;
+   background-color: var(--surface); /* 혹은 프로젝트 전역 변수 */
  ```
  ```diff
- <button onClick={handleReset} className={styles.resetButton}>초기화</button>
+ <button type="button" onClick={handleReset} className={styles.resetButton}>초기화</button>
  ```

---

### Medium 이슈 (다음 iteration)

#### 1. `sectionOf` 함수 내 굵기 정보 결락 시 방어 처리 강화를 통한 오매핑 방지
- **파일:라인** : [sections.ts](file:///Users/joel.silver/Workspace/gitroom/python/fontagit/apps/web/lib/sections.ts#L57)
- **문제** : `availableWeights`가 빈 배열(`[]`)로 넘어왔을 때 `some` 연산 결과는 `false`가 됩니다. 컨텍스트 명세에 의하면 빈 배열일 때 `[400]` 기본값을 보장해야 하므로 '본문-긴 글(body)'로 분류되어야 하나, 구현상으로는 `hasLightWeight`가 `false`가 되어 `'headline'`으로 오분류됩니다.
- **제안 수정** :
  ```typescript
  const weights = availableWeights.length === 0 ? [400] : availableWeights;
  const hasLightWeight = weights.some((w) => w < 700);
  ```

#### 2. `FontsViewWrapper` 와 `ClientFontsList` 간 `useSearchParams()` 중복 호출 제거
- **문제** : 동일 렌더 트리 내 부모 컴포넌트인 `FontsViewWrapper`와 하위 `ClientFontsList` 양쪽에서 `useSearchParams` 훅을 중복 호출해 데이터를 개별 파싱하고 있습니다.
- **제안** : 부모 `FontsViewWrapper`에서 쿼리 데이터를 일괄 가공한 뒤 하위 컴포넌트로 전달(Props Drilling)하여 훅 호출 횟수를 줄이고 응집도를 높이는 설계를 고려할 수 있습니다.

---

### 긍정적 관찰
- **Next.js 정적 빌드 대응** : `output: 'export'` 환경에서 빌드가 차단되는 문제를 피하기 위해 `useSearchParams` 컴포넌트를 `Suspense` 경계로 정확하게 감싼 대처가 탁월합니다.
- **최적의 사용자 경험 설계** : 타이핑 시 병목을 회피하기 위해 `useDeferredValue`를 최적의 계층 구조(`SectionedFontsView` -> `SectionOverview` -> `FontCard`)로 설계하여 부드러운 실시간 갱신 환경을 제공합니다.
- **테스트 케이스 확보** : 뷰 분기, 필터 결합, 용도별 분류 등 복잡한 조건 분기들에 대해 전방위적인 단위 테스트 및 통합 테스트 구조를 설계하여 기능 신뢰도를 한 차원 끌어올렸습니다.
