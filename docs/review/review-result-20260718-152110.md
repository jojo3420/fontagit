# Review Report: 2026-07-18-compare-home-merge-design.md
> Generated: 2026-07-18 15:23
> Codex CLI: codex-cli 0.144.5 (model gpt-5.6-sol, reasoning=xhigh)

---

## 1. Codex 리뷰 요약

- 전체 점수: 7/10
- 한줄 요약: 구현 방향은 명확하나 지연 로딩 실제 방식, 앵커 동작, 성능 합격 기준이 부족해 그대로 구현하면 성능 회귀/모바일 접근 문제 소지.

핵심 지적 12건: (1) IntersectionObserver만으론 JS 번들 분리 안 됨 → next/dynamic 필요 (2) #compare는 초기 서버 HTML에 상주해야 앵커 동작 (3) section 소유 위치 불명확(서버 권장) (4) placeholder 높이만으론 CLS 방지 부족, 반응형 규격 필요 (5) IO 1회 관찰 후 유지 + 미지원 fallback (6) 모바일 탭 제거 시 발견성 약화 (7) 앵커 3경우 검증 (8) Lighthouse 수치/환경/횟수 명문화, INP는 lab 한계 (9) 404 vs 301 재확정 (10) not-found 수정은 전역 404에 부작용 (11) /compare 링크 잔존 0 완료조건 (12) 실배포 서버 404 상태코드 확인.

---

## 2. Claude 크로스 리뷰

### 종합 소견
codex 지적 대부분이 사실 기반이고 원본/실제 코드에서 확인됨. 특히 6-10번은 worktree 코드로 실증됨. 9번만 사용자 확정 사항이라 결정 유지(근거만 기록). 전반적으로 스펙 개정 필요.

### 항목별 판정

| # | Codex 피드백 | 판정 | Claude 의견(근거) |
|---|------|------|------|
| 1 | IO만으론 번들 분리 안 됨, next/dynamic 필요 | 동의 | 정적 `import`면 CompareBoard+`data/fonts`가 초기 청크에 포함. `next/dynamic(…, {ssr:false})`가 실제 코드분할. CWV 개선의 핵심 |
| 2 | #compare 서버 HTML 상주 | 동의 | 앵커(직접/크로스페이지)가 안정 동작하려면 섹션 컨테이너가 SSG HTML에 존재 필수 |
| 3 | section 소유 서버 | 동의 | 서버 `page.tsx`가 `<section id=compare>`+placeholder 소유, 내부 board만 클라이언트 dynamic |
| 4 | 반응형 placeholder 높이 | 동의 | 모바일 1열/데스크톱 3열로 높이 상이. min-height 반응형 규격화 |
| 5 | IO 1회 관찰+유지+fallback | 부분 동의 | next/dynamic 채택 시 IO는 선택(과설계 주의). 뷰포트까지 지연하려면 dynamic+IO 결합. 채택 시 unobserve+미지원 즉시렌더 |
| 6 | 모바일 발견성 약화 | 동의(실증) | e2e가 "모바일 헤더 tool 링크 숨김" 강제 → 탭 제거 시 모바일 비교 진입점 완전 소멸. 결정 5 재확정 필요 |
| 7 | 앵커 3경우 검증 | 동의 | 홈 내 클릭/타 페이지 이동/주소창 직접입력 3경우 e2e |
| 8 | Lighthouse 수치 명문화 | 동의 | #25 실측(LCP 2.1s, CLS 0, TBT 40ms)을 명시 기준으로. INP는 field지표라 TBT를 lab proxy로 |
| 9 | 404 vs 301 재확정 | 부분 동의 | 사용자 확정(404, 트레이드오프 인지). 결정 뒤집지 않음. 단 스펙에 선택 근거 기록 |
| 10 | not-found 전역 부작용 | 동의(실증) | not-found.tsx는 이미 "홈으로" 일반 안내 존재. 비교 전용 문구는 전역 404에 부적절 → 결정 7 철회 |
| 11 | /compare 링크 잔존 0 완료조건 | 동의 | grep으로 활성 /compare 링크 0 완료조건 명문화 |
| 12 | 실배포 404 상태코드 | 동의 | Cloudflare Pages 실제 404 응답 확인(빌드 산출물만으론 불충분) |

### 동의하는 핵심 피드백 Top 3
1. **#1 next/dynamic** — 지연 렌더의 목적(CWV)을 IO만으론 달성 못 함. dynamic 코드분할이 필수.
2. **#6 모바일 발견성(실증)** — 헤더 숨김 + 탭 제거 = 모바일 진입점 소멸. 사용자 재확정 필요.
3. **#10 not-found 부작용(실증)** — 전역 404 오염. 결정 7 철회.

### 동의하지 않는/조정 피드백
- **#9 404→301 재확정**: 사용자가 트레이드오프를 보고 404를 명시 선택. 로드맵/결정 뒤집지 않음 원칙. 근거 기록으로 대체.
- **#5 IO 강제**: next/dynamic만으로 초기 번들서 분리되므로 IO는 필수 아님. 뷰포트 지연이 추가로 필요할 때만 결합(단순화 우선).

### Codex가 놓친 추가 관점
- **모바일 헤더 숨김은 e2e로 강제됨**: 단순 "발견성 저하"가 아니라 진입점 소멸. 심각도 상향.
- **output:'export'에서 next/dynamic ssr:false 동작 확인 필요**: AGENTS.md "이 Next.js는 다르다" → 구현 전 `node_modules/next/dist/docs` 확인.
- **data/fonts 번들 이득**: dynamic 분할 시 CompareBoard가 import하는 fonts 데이터도 함께 초기 번들서 제외 → 추가 이득.

---

## 3. 최종 권고사항

### 즉시 반영 (Must)
1. 지연 방식을 `next/dynamic(() => import('./CompareBoard'), {ssr:false})` 코드분할로 명시(IO는 선택 보조). export 호환 문서 확인.
2. 서버 `page.tsx`가 `<section id="compare">`+placeholder 소유, 내부 board만 클라이언트 dynamic. #compare는 SSG HTML 상주.
3. 결정 7 철회: not-found.tsx 미변경(이미 홈 안내 존재).
4. 결정 5 재확정: 모바일 비교 진입점(사용자 결정 필요).

### 검토 후 반영 (Should)
5. 반응형 placeholder min-height 규격(모바일/데스크톱) — CLS 방지.
6. 성능 완료조건 수치화: LCP≤2.5s, CLS≤0.02, TBT≤100ms(#25 실측 여유), 측정 환경/횟수 명시. INP는 TBT proxy.
7. 앵커 3경우 e2e + /compare 실제 404 상태코드 확인(실배포).
8. /compare 활성 링크 잔존 0 완료조건(grep: 네비/메타/구조화데이터/테스트/문서).

### 참고 (Nice to have)
9. 스펙에 404 선택 근거(최근 배포, 인덱싱 축적 적음) 명문화.
