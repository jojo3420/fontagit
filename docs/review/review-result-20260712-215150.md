# Review Report: 2026-07-12-fontagit-web-foundation-and-core.md (구현 계획서)

> Generated: 2026-07-12 21:55
> Codex CLI: codex-cli 0.144.1 (model gpt-5.6-sol, reasoning effort xhigh)

---

## 1. Codex 리뷰 요약

- 전체 점수: 6.5/10
- 한줄 요약: Next 16 정적 출력 구조는 맞지만, 태스크별 실행 시 빌드 순서/잠금 파일/Playwright 부재/모바일 레이아웃에서 문제가 난다.

항목별: 완성도 WARN(Playwright/95% 기준 부재), 누락오류 FAIL(Task1 빌드 순서, lockfile, [slug] 명령), 일관성 WARN(인라인 스타일 vs CSS Modules, fontKey string), 실현가능성 WARN(Google Fonts 빌드 의존성, 모바일).

구체 지적 20건: (1)Task1 Step5 순서상 실패 (2)lockfile 커밋 누락 (3)[slug] zsh glob (4)동적 라우트 정상[긍정] (5)정적 404 검증 방식 (6)Playwright 실체 없음 (7)TOP10인데 6행 (8)인라인 스타일 vs CSS Modules (9)390px 모바일 오버플로 (10)트렌드 필터 모순 (11)비동작 FilterChip button (12)fontKey string 타입안전 부재 (13)무결성 검사 부족 (14)유료 견본 가짜 굵기/오정보 (15)Pretendard Variable 경로 부정확 (16)Google 폰트 옵션 "에러나면 고치기" 불안정 (17)미사용 Noto 제거 (18)테마 스크립트 값 검증 (19)단순 UI 단위테스트 과다 (20)95% 합격 기준 부재.

---

## 2. Claude 크로스 리뷰

### 종합 소견
Codex 리뷰는 계획서를 서브에이전트가 그대로 실행한다는 전제에서 매우 유효하다. 특히 실행 자체를 깨는 정확성 버그(1, 3, 10, 12, 14, 15)와 목표에 명시했으나 실체 없는 Playwright(6)는 반드시 고쳐야 한다. 환각/오독은 없었고, 반박할 항목은 없다(11/19만 부분 동의).

### 항목별 판정

| # | Codex 피드백 | 판정 | Claude 의견 |
|---|-------------|------|------------|
| 1 | Task1 Step5 빌드 순서 실패 | 동의(Must) | globals.css가 tokens.css(Task2) 참조. Task2를 Task1에 병합 |
| 2 | lockfile 커밋 누락 | 동의(Must) | pnpm install 산출 lockfile 커밋 추가 |
| 3 | [slug] zsh glob | 동의(Must) | `git add -- 'apps/.../[slug]/page.tsx'` 따옴표 |
| 4 | 동적 라우트 정상 | 동의(긍정) | 조치 불필요 |
| 5 | 정적 404 검증 | 동의(Should) | out/를 정적 서버로 띄워 검증하도록 명시 |
| 6 | Playwright 실체 없음 | 동의(Must) | 설정+smoke.spec.ts 태스크 신설 |
| 7 | TOP10인데 6행 | 동의(High) | 데이터 10행으로 확장 |
| 8 | 인라인 스타일 vs CSS Modules | 동의(High) | 페이지 스타일을 page.module.css로 |
| 9 | 390px 모바일 오버플로 | 동의(High) | Header/패딩/상세 그리드 반응형 |
| 10 | 트렌드 필터 모순 | 동의(Must) | 필터칩 제거 또는 실제 탭. 이번엔 칩 제거+두 표 |
| 11 | FilterChip button | 부분동의(Minor) | span은 포커스 상실. type=button+aria-pressed 유지 |
| 12 | fontKey string | 동의(Must) | FontKey 유니언 타입 도입 |
| 13 | 무결성 검사 부족 | 부분동의(Should) | 중복/미존fontKey/3개초과/유료대안/자기참조 추가 |
| 14 | 유료 견본 가짜 굵기/오정보 | 동의(Must) | "대체 견본" 라벨 + 로드된 굵기만 렌더 |
| 15 | Pretendard Variable 경로 | 동의(Must) | variable/pretendardvariable.css 확인됨, 그걸로 교체 |
| 16 | Google 폰트 옵션 불안정 | 부분동의(Should) | "에러나면 고치기" 제거, subsets:latin+preload:false 확정 |
| 17 | 미사용 Noto 제거 | 동의(Should) | 데이터에서 notoSansKr 미사용 확인, 제거 |
| 18 | 테마 스크립트 값 검증 | 동의(Should) | dark/light만 허용 |
| 19 | 단순 UI 단위테스트 과다 | 부분동의 | TierChip 테스트 제거, LicenseBadge(3분기)는 유지 |
| 20 | 95% 합격 기준 부재 | 동의(Must) | 뷰포트/기준이미지/허용오차를 계획에 명시 |

### 동의하는 핵심 피드백 (Top 3)
1. Playwright 실체(6)+95% 기준(20): 목표에 있으나 실행 산출물이 없다. 서브에이전트가 "검증"을 건너뛰게 된다.
2. 실행 파괴 버그(1 Task 순서, 3 zsh glob, 12 fontFamily undefined, 14 가짜 굵기): 그대로 두면 태스크가 실패하거나 오정보를 낸다.
3. 모바일/CSS Modules(8, 9): 계획의 Global Constraints(CSS Modules)와 실제 인라인 스타일이 자기모순. 390px에서 깨진다.

### 동의하지 않는 피드백
없음. 부분 동의(11, 13, 16, 19)는 방향은 타당하되 강도만 조정.

### Codex가 놓친 추가 관점
- 없음. 계획 내부 일관성 위주로 잘 짚었다. 다만 Playwright는 이 저장소에 MCP+e2e-runner가 이미 있어, 신규 스모크 스펙과 역할 정리가 필요하다.

---

## 3. 최종 권고사항

### 즉시 반영 (Must)
Task1+2 병합, lockfile 커밋, [slug] 따옴표, 트렌드 필터 모순 해소, FontKey 유니언, 유료 견본 정직성(라벨+굵기), Pretendard Variable 경로, Playwright 설정+smoke.spec.ts+95% 기준.

### 검토 후 반영 (Should)
정적 404 검증(out/ 서빙), TOP10 10행, 페이지 CSS Modules화 + 모바일 반응형, 무결성 강화, 미사용 Noto 제거, 폰트 옵션 확정, 테마 값 검증, TierChip 단위테스트 제거.

### 참고 (Nice to have)
FilterChip 상호작용 방식(현행 유지 가능).
