### 종합 평가

- 점수 3/10
- 한줄 총평: 구조는 좋지만, 잘못된 라이선스를 확정·발행하거나 prod 확인을 우회할 수 있습니다.
- 머지 권고: Must-fix 전 불가

### Critical(Must-fix, 머지 차단)

- `apps/pipeline/src/fontagit_pipeline/noonnu_enrich.py:165-202`, `tests/fixtures/noonnu_conditional.html:99-108` / 문제: 금지 라이선스를 OFL로 오판합니다. / 근거: HTML 어디든 `OFL` 글자가 있으면 `return "OFL"`이고, 이후 `allow_modify="allowed"`로 저장합니다. 하지만 fixture에는 `수정·복제·배포 및 유료 판매 모두 금지`라고 명시돼 있습니다. 이 폰트도 `auto_safe`로 발행됩니다. / 제안: 페이지 전체 키워드 검색을 제거하고 공식 라이선스 영역만 파싱하십시오. 허용·금지 표현이 충돌하면 무조건 `needs_review`로 보내는 회귀 테스트가 필요합니다.

- `apps/pipeline/src/fontagit_pipeline/noonnu_enrich.py:70-80` / 문제: 유료 가격이 0원으로 잘려 자동 발행될 수 있습니다. / 근거: `price = int(float(offers["price"]))` 때문에 `"0.5"`와 `"-0.1"`이 `0`이 됩니다. 이후 게이트의 `price == 0`을 통과합니다. / 제안: `Decimal`로 원래 값을 보존해 정확히 0인지 비교하고, 음수·소수·비정상 통화는 `needs_review`로 보내십시오.

- `apps/pipeline/src/fontagit_pipeline/noonnu_enrich.py:104-153` / 문제: 허용표 중복 행이나 엉뚱한 첫 번째 표가 자동 발행 근거로 사용될 수 있습니다. / 근거: `soup.find("table")`로 첫 표만 선택하고, 중복 카테고리는 `result[category_key] = status`로 덮어씁니다. 마지막 값이 `allowed`이면 6개 키 검사도 통과합니다. / 제안: 헤더와 주변 라이선스 영역으로 정확한 표를 지정하고, 카테고리 중복·누락·알 수 없는 상태는 즉시 파싱 실패로 처리하십시오.

- `apps/pipeline/src/fontagit_pipeline/noonnu_review.py:95-150` / 문제: 반려·이미 처리된·오래된 제안도 검증 없이 `published`로 바꿀 수 있습니다. / 근거: 승인 시 `review_status`, `parse_status`, 필수 라이선스 값, 공식 URL을 확인하지 않고 곧바로 `"license_verified": True`, `"status": "published"`를 설정합니다. 제안도 `font_id`가 아닌 복제된 `slug`로 연결합니다. / 제안: `review_status='proposed'` 조건부 갱신, `proposal.font_id` 기반 폰트 조회, 필수필드 검증을 한 DB 트랜잭션으로 묶으십시오.

- `apps/pipeline/src/fontagit_pipeline/noonnu_review.py:121-126` / 문제: 사람 승인 경로 자체가 실패할 가능성이 높습니다. / 근거: diff 전반에서 fonts 기본키는 `id`인데 `select("font_id,official_url")`를 호출합니다. 마이그레이션도 `font_id ... references fontagit.fonts(id)`로 정의합니다. / 제안: `select("id,official_url")`로 고치고 `id == proposal["font_id"]`를 확인하는 통합 테스트를 추가하십시오.

- `apps/pipeline/src/fontagit_pipeline/noonnu_publish.py:42-90` / 문제: prod 이중 확인을 우회할 수 있습니다. / 근거: `publish_to_prod(..., dry_run=False)`를 직접 호출하면 `--confirm`과 대화형 `yes` 없이 즉시 prod에 씁니다. 확인은 `__main__.py`에만 있습니다. / 제안: 실제 쓰기 함수가 CLI에서 생성한 일회성 확인 토큰이나 확인된 스냅샷을 요구하도록 설계해 직접 호출 우회를 막으십시오.

### High(Should-fix)

- `apps/pipeline/src/fontagit_pipeline/noonnu_enrich.py:394-440`, `noonnu_review.py:135-150` / 문제: 폰트와 제안 상태 변경이 트랜잭션이 아닙니다. / 근거: 제안을 `auto_published`로 저장한 뒤 폰트 갱신이 실패할 수 있고, 사람 승인도 폰트를 먼저 발행한 뒤 제안 상태를 변경합니다. / 제안: DB 함수(RPC) 하나에서 조건 확인과 두 테이블 갱신을 원자적으로 처리하십시오.

- `apps/pipeline/src/fontagit_pipeline/noonnu_enrich.py:371-390` / 문제: 서버 내부 주소 요청 공격(SSRF)에 노출됩니다. / 근거: seed의 `source_page`를 호스트 검증 없이 요청하고 `follow_redirects=True`를 사용합니다. 눈누 robots 정책도 다른 호스트 접근을 막아주지 않습니다. / 제안: `https://noonnu.cc/font_page/<숫자>`만 허용하고, 리다이렉트마다 호스트·스킴·사설 IP를 다시 검사하십시오.

- `apps/pipeline/src/fontagit_pipeline/noonnu_enrich.py:157-161` / 문제: 스타일 추출 범위가 `@font-face`가 아닌 HTML 전체입니다. / 근거: `re.findall(r"font-weight...")`가 화면 UI용 CSS까지 폰트 굵기로 수집합니다. / 제안: CSS를 파싱해 `@font-face` 블록 안의 `font-weight`와 `font-style`만 읽으십시오.

- `apps/pipeline/src/fontagit_pipeline/noonnu_enrich.py:365-443` / 문제: DB나 네트워크가 전부 실패해도 명령이 성공 코드 0으로 끝날 수 있습니다. / 근거: 대부분의 예외를 `skipped += 1`로 처리한 뒤 정상 튜플을 반환합니다. / 제안: 정책상 스킵과 시스템 오류를 분리하고, 시스템 오류가 발생하거나 전 건 실패하면 비정상 종료하십시오.

- `apps/pipeline/src/fontagit_pipeline/noonnu_publish.py:56-90`, `__main__.py:354-385` / 문제: 확인한 대상과 실제 발행 대상이 달라질 수 있습니다. / 근거: dry-run으로 개수만 보여준 뒤 실제 실행에서 dev를 다시 조회합니다. 그 사이 추가된 행도 확인 없이 발행됩니다. 또한 같은 slug의 prod 데이터를 무조건 덮어씁니다. / 제안: 확인 시 행 목록과 해시를 고정하고 그 스냅샷만 발행하십시오. prod의 더 최신 데이터는 덮어쓰지 않도록 버전 비교도 필요합니다.

- `apps/pipeline/src/fontagit_pipeline/noonnu_review.py:157-176` / 문제: 표본 감사가 항상 앞부분만 봅니다. / 근거: 정렬·무작위·순환 기준 없이 `response.data[:sample_count]`를 반환하며 Tier B 필터도 없습니다. / 제안: Tier B로 제한하고, 매 실행마다 다른 표본이 선택되도록 무작위 또는 순환 샘플링을 적용하십시오.

- `apps/pipeline/tests/test_noonnu_enrich.py:1-293`, `test_noonnu_review.py:1-180` / 문제: 위험한 실제 경로 테스트가 없습니다. / 근거: approve/reject/unpublish, prod dry-run·이중 확인, DB 트랜잭션, 소수 가격, 중복 표, 금지 문구 속 `OFL`을 검증하지 않습니다. / 제안: 핵심 경로별 해피 패스 1개와 치명적 예외 1~2개만 추가하십시오.

### Medium(다음 iteration)

- `apps/pipeline/src/fontagit_pipeline/noonnu_review.py:95-150` / 문제: 누가 승인했는지 기록하지 않습니다. / 근거: `reviewer_note`와 시간만 있고 검수자 식별자가 없습니다. / 제안: 실행자 ID 또는 운영자 계정을 필수 감사 정보로 저장하십시오.

- `apps/pipeline/src/fontagit_pipeline/__main__.py:416-455` / 문제: CLI 입력 검증이 약합니다. / 근거: approve/reject/unpublish의 `--slug`는 선택값이고, `--pct` 범위와 `--limit`의 0·음수를 막지 않습니다. / 제안: 액션별 필수 인자와 `1~100`, 양수 범위를 argparse 단계에서 검증하십시오.

- `supabase/migrations/0016_noonnu_enrich.sql:38-54` / 문제: 제안 권한 값에 DB CHECK가 없습니다. / 근거: `proposed_embedding`, `proposed_redistribute`, `proposed_modify`가 임의 문자열을 허용합니다. / 제안: fonts와 동일하게 `allowed/conditional/denied` 또는 NULL만 허용하십시오.

- `apps/pipeline/src/fontagit_pipeline/noonnu_enrich.py:347-445` / 문제: 파싱·HTTP·robots·DB·발행이 한 함수에 몰려 있습니다. / 근거: `enrich_fonts`가 모든 책임을 직접 수행합니다. / 제안: 파서, 수집기, 저장소, 발행 서비스를 분리해 실패 원인과 테스트 경계를 명확히 하십시오.

### 긍정적 관찰

- 자동 게이트의 상업용 4개 항목은 `print`, `website`, `packaging`, `video`로 정확하며 임베딩은 제외했습니다.
- `parse_status`는 마이그레이션의 `parsed/partial/failed`와 일치합니다.
- `_font_update`는 `license_proposals` upsert 전에 `pop()`으로 제거됩니다.
- DB CHECK로 `published` 상태에 `license_verified=true`를 요구합니다.
- import와 enrich가 `derive_noonnu_slug`를 공유하고 한글 슬러그를 보존합니다.
- 눈누 산문은 저장하지 않고, 권한 상태와 자체 작성한 짧은 설명만 저장합니다.
- 기본 dry-run과 `--confirm`·`yes` 확인 구조, robots 준수와 요청 간 지연을 넣은 방향은 적절합니다.