### 종합 평가
- 전체 점수: 8/10
- 한줄 총평: 핵심 race는 잘 막았지만, 테스트 R4가 너무 약하고 blocking lock의 운영 리스크가 남아 있습니다.
- 머지 권고: 🟡 Should-fix 수정 후 머지

### 관점별 리뷰 (표)
| # | 관점 | 평가 | 핵심 지적 |
|---|------|------|---------|
| 1 | 정합성 | 양호 | PR 설명과 0008 구현은 대체로 일치. 다만 일부 문서 상태/리뷰 표현은 과장 또는 오래된 상태로 보임 |
| 2 | 호출처 영향 | 양호 | `record_click(p_slug text) returns void` 유지. 브라우저 `clicks.ts` 영향은 없어 보임 |
| 3 | race condition | 양호 | lock → count → insert 순서라 TOCTOU는 닫힘. 단 blocking lock이라 폭주 시 DB 연결 대기 리스크 있음 |
| 4 | 보안 | 양호 | `SECURITY DEFINER` + 고정 `search_path` + 정적 SQL이라 인젝션 위험은 낮음 |
| 5 | Silent Failure | 양호 | rate limit 초과만 조용히 무시하고, DB 오류까지 삼키지는 않음 |
| 6 | 테스트 | 주의 | R1~R3은 유효. R4는 comment만 남아도 통과할 수 있어 증명력이 약함 |
| 7 | 프로덕션 준비 | 주의 | 0008 신규 마이그레이션 전략은 좋음. Kong 미적용 상태에서는 DB가 최후방어만 담당 |
| 8 | 머피의 법칙 | 주의 | 대량 동시 요청 시 insert는 막지만 lock 대기열로 리소스가 묶일 수 있음 |

### Critical 이슈 — Must-fix (머지 차단)
없음.

### High 이슈 — Should-fix (머지 전 수정 권장)

1. `supabase/tests/click_rate_limit_test.sql:71` / R4 테스트가 가짜로 통과할 수 있음 / 근거(diff 인용):
```sql
if position('pg_advisory_xact_lock' in v_def) = 0 then
```
문제: 함수 본문에 주석으로 `pg_advisory_xact_lock` 문자열만 남아도 테스트가 통과합니다. 실제 lock 호출을 증명하지 못합니다.  
제안 수정: `position('perform pg_advisory_xact_lock' in lower(v_def))`처럼 실제 실행문을 확인하세요. 더 좋게는 lock 위치가 `select count(*)`보다 앞인지도 검사하세요.

2. `supabase/migrations/0008_record_click_rate_limit.sql:31` / blocking advisory lock이 폭주 요청을 DB 연결 대기열로 만들 수 있음 / 근거(diff 인용):
```sql
perform pg_advisory_xact_lock(hashtext(p_slug));
```
문제: race는 막지만, 봇이 같은 폰트로 동시 요청을 많이 보내면 요청들이 조용히 버려지지 않고 lock을 기다립니다. insert 폭증은 막아도 DB/PostgREST 연결을 오래 잡을 수 있습니다.  
제안 수정: 리소스 보호를 더 중시하면 `pg_try_advisory_xact_lock(...)`로 바꾸고 lock 획득 실패 시 `return`하세요. 정확히 20건까지 세는 것이 더 중요하면 현재 방식을 유지하되, prod에서는 Kong IP 제한과 함께 배포해야 한다고 배포 조건에 명시하세요.

### Medium 이슈 (다음 iteration)

1. `supabase/migrations/0008_record_click_rate_limit.sql:31` / advisory lock key에 namespace가 없음  
`hashtext(p_slug)` 단독 사용은 다른 기능의 advisory lock과 우연히 충돌할 수 있습니다. 낮은 확률이지만, 운영 코드라면 `pg_advisory_xact_lock(hashtext('fontagit.record_click'), hashtext(p_slug))`처럼 namespace를 나누는 편이 더 안전합니다.

2. `docs/superpowers/specs/2026-07-17-click-rate-limiting-design.md:6` / 문서 상태가 구현 상태와 안 맞음  
문서에는 `상태: 설계 확정 대기`라고 되어 있지만 PR은 이미 구현과 테스트까지 포함합니다. 나중에 읽는 사람이 헷갈립니다. `구현 완료, PR 리뷰 중` 정도로 맞추는 게 좋습니다.

3. `docs/progress.md` / 리뷰 결과 표현이 다소 과장됨  
diff 안의 dual review는 `Degraded — Codex 단독`인데, progress에는 “최종 리뷰(Opus) Ready to merge YES”처럼 강한 표현이 있습니다. 실제 근거 파일이 PR에 없다면 표현을 낮추는 게 맞습니다.

### 긍정적 관찰

- 0007을 수정하지 않고 0008 신규 마이그레이션으로 간 점은 좋습니다.
- 함수 시그니처를 유지해서 프론트 호출부 영향이 작습니다.
- `count >= c_max`라서 20건 허용, 21번째 무시 경계가 맞습니다.
- `SECURITY DEFINER`와 `search_path = fontagit, pg_temp` 유지가 적절합니다.
- rate limit 초과를 오류로 던지지 않는 것은 기존 fire-and-forget 계약과 맞습니다.
- R1/R2/R3 테스트는 핵심 동작을 짧고 명확하게 검증합니다.