### 종합 평가
- 전체 점수: 5/10
- 작은 코드 변경의 방향은 맞지만, 빈 태그가 기존 데이터를 지울 수 있고 실행 계획에도 서로 충돌하는 내용이 있습니다.
- 머지 권고: Must-fix 해결 전 불가

### 관점별 리뷰 (표)

| # | 관점 | 평가 | 핵심 지적 |
|---|---|---|---|
| 1 | 정합성 | Critical | “published 폰트 미수정” 제약과 published 데이터 갱신 목표가 정면충돌합니다. |
| 2 | 영향도 | High | 함수 시그니처 변경은 없지만, 태그가 SHA·finding·manifest까지 전파되는 영향이 충분히 검증되지 않았습니다. |
| 3 | SOLID/SRP | Medium | 공통 evidence 수집 함수가 눈누 전용 객체 구조를 직접 알게 됩니다. |
| 4 | 선제적 위험 | High | 태그 순서 변화, 중복 실행, 감사 이후 DB 변경에 따른 오래된 manifest 덮어쓰기 위험이 있습니다. |
| 5 | 방어적 입력 | Critical | `tags=[]`, 공백, 중복, 과다 태그를 그대로 evidence에 넣습니다. |
| 6 | 보안 | Medium | 직접적인 OWASP 취약점은 없지만 외부 HTML 입력의 길이·개수 제한이 확인되지 않습니다. |
| 7 | Silent Failure | High | 태그 파싱 실패가 정상적인 빈 배열로 오인될 수 있습니다. |
| 8 | 테스트 | High | snapshot만 검사하며 실제 tags finding 생성과 빈 태그 방어는 검증하지 않습니다. |
| 9 | 코드 스타일 | Medium | `parsed` 초기화가 보이지 않아 분기 불변조건에 의존합니다. |
| 10 | 머피의 법칙 | High | 외부 HTML이 바뀐 날 전체 1,110종을 실행하면 대량 빈 태그 제안이 만들어질 수 있습니다. |

### Critical 이슈 - Must-fix (머지 차단)

1. `apps/pipeline/src/fontagit_pipeline/audit_runner.py:669-670` / 빈 태그가 기존 태그 삭제 제안으로 바뀔 수 있음 / `extracted["tags"] = list(parsed.tags)`는 `parsed.tags == []`도 명시적인 값으로 전달합니다. downstream 비교가 현재 DB 태그와 비교하면 기존 태그를 `[]`로 바꾸는 finding이 생성될 수 있습니다. HTML 구조 변경도 “태그가 없는 정상 페이지”로 오인됩니다. / 정비 작업은 삭제가 목적이 아니므로 유효한 태그가 있을 때만 `extracted["tags"]`를 넣으세요. 빈 결과는 별도의 `tags_extraction_status` 또는 실패 finding으로 기록해야 합니다.

2. `docs/superpowers/plans/2026-07-22-collections-phase0-data-enrichment.md:5,16, Task 3 Step 4` / 실행 목표와 안전장치가 서로 충돌함 / 문서는 “published 폰트는 파이프라인이 건드리지 않는다”고 하면서, Task 3에서는 published 폰트의 tags·weights 수가 크게 증가해야 한다고 합니다. 현재 문장 그대로라면 완료 조건을 달성할 수 없습니다. / “published 상태 자체를 변경하지 않는다”는 뜻인지, “published 행을 전혀 UPDATE하지 않는다”는 뜻인지 명확히 고치고 RPC의 실제 정책과 맞추세요.

3. `docs/superpowers/plans/...:Architecture`, `docs/superpowers/specs/2026-07-22-collections-expansion-design.md:§4, §10` / 구현 기준 문서가 서로 다른 경로를 지시함 / plan은 “유일한 코드 갭은 `_collect_metadata_evidence()`”라고 하지만 spec은 `noonnu_enrich.py`의 `extract_tags()`를 신규 구현 대상으로 명시합니다. / `audit_noonnu → audit_runner`와 `noonnu_enrich` 중 어느 경로가 공식 데이터 계약인지 하나로 확정하고 나머지 문서를 수정하세요.

### High 이슈 - Should-fix

1. `apps/pipeline/src/fontagit_pipeline/audit_runner.py:669` / `parsed` 스코프가 암묵적 불변조건에 의존함 / `parsed is not None`은 변수가 이미 할당된 경우에만 안전합니다. `source_kind == "noonnu"`인데 `parsed` 할당을 건너뛰는 분기가 생기면 `UnboundLocalError`가 발생합니다. / 함수 초기에 `parsed = None`으로 명시적으로 초기화하고, 파싱 실패 분기 테스트를 추가하세요.

2. `apps/pipeline/tests/test_audit_runner.py:267-297` / 핵심 목적을 끝까지 검증하지 않음 / 테스트는 `snapshot.extracted["tags"]`만 확인합니다. plan이 주장하는 `compare_metadata → tags FindingDraft` 연결은 검증하지 않습니다. / 현재 테스트에서 report의 tags finding, 현재값, 제안값까지 함께 확인하세요.

3. `apps/pipeline/tests/test_audit_runner.py:267-297` / 치명적인 예외 테스트가 없음 / 빈 태그나 파싱 실패가 기존 태그 삭제로 이어지지 않는지 검증하지 않습니다. / 테스트는 최대 3개면 충분합니다: 정상 태그, 빈·공백 태그, 파싱 실패 또는 비눈누 경로.

4. `docs/superpowers/plans/...:Task 2 Step 2-4` / 대량 크롤링 안전장치가 실행 전에 확정되지 않음 / fallback, 재시도, rate limit을 “실행하면서 확인하고 미비하면 중단”하도록 미룹니다. / 1,110종 실행 전에 timeout, 요청 간격, 최대 재시도, 구조변경 판정, 실패 URL 산출물 형식을 plan에 확정하세요.

5. `docs/superpowers/plans/...:Task 3 Step 1` / manifest 생성 절차가 실행 가능한 수준이 아님 / “CLI가 있으면 사용, 없으면 짧은 스크립트”는 구현 계획이 아니라 미결정 사항입니다. / 실제 존재하는 CLI 명령과 입력 파일을 확정하세요.

6. `docs/superpowers/plans/...:Task 3` / 오래된 manifest의 덮어쓰기 경쟁 조건이 빠짐 / 감사와 사람 검수 사이에 DB 태그가 변경되면 이전 current 값을 기반으로 최신 값을 덮을 수 있습니다. / RPC가 현재값 또는 revision을 비교해 충돌을 거부하는지 명시하고, 없다면 낙관적 잠금 검사를 추가하세요.

7. `docs/superpowers/plans/...:Task 4 Step 1` / 태그 출처 구분 목표가 실제 데이터에 없음 / 현재 변경은 `tags`만 넣으며 “눈누 원본·자동추론·사람검수”를 구분할 명시적 필드를 추가하지 않습니다. / evidence 또는 finding에 `tag_source`, 원본 URL, 추출기 버전을 보존하세요.

### Medium 이슈

1. `apps/pipeline/src/fontagit_pipeline/audit_runner.py:669-670` / 태그 정규화 계약이 없음 / 공백, 중복, 비정상적으로 긴 태그와 과도한 개수가 그대로 들어갈 수 있습니다. / 저장용 태그에는 trim·빈값 제거·중복 제거·길이와 개수 제한을 적용하되 원본 evidence는 별도로 보존하세요.

2. `apps/pipeline/src/fontagit_pipeline/audit_runner.py:671 이후` / 태그 배열 순서가 SHA를 바꿈 / `sort_keys=True`는 딕셔너리 키만 정렬합니다. 같은 태그라도 HTML 순서가 바뀌면 새 snapshot으로 인식됩니다. / 태그가 집합 의미라면 정렬·중복 제거 후 해시하거나, 순서를 의미 있는 계약으로 명시하세요.

3. `apps/pipeline/src/fontagit_pipeline/audit_runner.py:669` / 공통 수집기에 소스별 예외가 누적될 가능성 / 향후 다른 소스가 추가되면 `source_kind` 조건문이 늘어납니다. / 각 소스 parser가 표준 evidence DTO를 반환하도록 만드는 편이 장기적으로 깔끔합니다.

4. `docs/superpowers/specs/...:§2` / 1,000행 표본으로 전체 품질을 추론함 / 정렬되지 않은 첫 1,000행은 대표 표본이 아닐 수 있습니다. / Task 0에서 exact count를 얻은 뒤 설계 수치와 목표율을 갱신하세요.

5. `docs/review/review-result-agy-20260722-153145.md:1-7` / 유효한 리뷰 결과가 아닌 작업 예정 문장만 저장됨 / dual 문서가 실패 사실을 밝힌 점은 좋지만 이 파일 자체는 검토 증거가 아닙니다. / 제거하거나 실패 로그임을 파일 제목과 본문에 명시하세요.

### 긍정적 관찰

- `_collect_metadata_evidence()`의 시그니처를 변경하지 않아 직접 호출처 수정 누락은 없습니다.
- `source_kind == "noonnu"` 조건으로 다른 소스의 evidence가 오염되는 것을 막았습니다.
- 태그가 SHA 계산 전에 포함되어 snapshot 무결성 검증 대상이 됩니다.
- 테스트가 private 함수만 직접 호출하지 않고 `run_metadata_audit()` 경로를 통과하는 점은 좋습니다.
- `auto_applicable=False`, RPC 전용 쓰기, dev 우선 적용 원칙은 대량 데이터 변경에 적절합니다.
- agy 리뷰 실패를 성공으로 포장하지 않고 Degraded 상태로 밝힌 점은 투명합니다.
- 새 코드에서 SQL 실행, 명령 실행, HTML 직접 렌더링은 추가되지 않아 즉각적인 SQL 삽입이나 원격 코드 실행 취약점은 보이지 않습니다.