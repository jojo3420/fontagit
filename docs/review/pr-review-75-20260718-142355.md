# PR Review Report: #75 — fix: 한글 폰트 견본을 한글 팬그램으로 표시
> PR URL: https://github.com/jojo3420/fontagit/pull/75
> Base ← Head: main ← fix/korean-font-specimen
> Codex Model: gpt-5.6-sol (reasoning=xhigh)

## 0. PR 요약
- 변경 규모: 7 files, +126 / -12
- 핵심: `isKoreanFont`를 `subsets.includes("korean") || sourceTier==="B"`로 확장, specimen 함수 시그니처를 `Pick<Font,"subsets"|"sourceTier">` 기반으로 변경, 호출부 4곳 수정. 눈누(Tier B) 한글 폰트의 영문 팬그램 오표시 수정.

## 1. Codex 리뷰 (요약)
- 점수 8.5/10, 머지 권고: 즉시 머지 가능. Critical 0, High 0, Medium 3.
- Medium: (1) sourceTier로 언어 추론 결합, (2) subsets null 시 SSG 크래시 우려, (3) getSpecimenText 테스트 수/toContain 느슨.

## 2. Claude 크로스 리뷰

| # | Codex 지적 | 파일:라인 | 심각도 | Claude 판정 | 근거 |
|---|-----------|---------|--------|------------|------|
| 1 | sourceTier로 언어 추론(출처-언어 결합) | specimen.ts:8-9 | Medium | 동의 → Nice-to-have | 설계상 트레이드오프. 사용자가 데이터 백필 대신 택한 방식. 후속(subsets 백필/supportsKorean)으로 이미 트래킹 |
| 2 | subsets null → `.includes()` 크래시 | specimen.ts:9 | Medium | 비동의 → 패스 | 삼중 가드로 null 불가: 스키마 `text[] not null default '{}'`(0001:13), 매퍼 `row.subsets ?? []`(mappers.ts:40), 타입 `subsets: string[]`(font.ts:39) |
| 3 | 테스트 수 초과 + toContain 느슨 | specimen.test.ts | Medium | 동의 → Nice-to-have | 스타일 가이드 권고. getDefaultSpecimenText는 이미 toBe 정확검증. 차기 정리 가능, 비차단 |

### 동의한 핵심 이슈
- Must-fix: 없음
- Should-fix: 없음

### 비동의/패스
- Medium 2 (null 크래시): 스키마-매퍼-타입 삼중 가드로 실제 발생 불가. Codex가 매퍼(mappers.ts:40)를 보지 못한 데서 온 과대 추정.

## 3. 최종 권고 및 머지 결정
- 머지 결정: MERGE_POSSIBLE (Must-fix 0)
- Nice-to-have(비차단, 후속): sourceTier 결합 해소를 위한 subsets 데이터 백필 또는 supportsKorean 필드; 견본 테스트 toBe 정리

## 4. 다음 단계
- [ ] 사용자 승인 후 머지 → main 최신화 → deploy로 /fonts 실화면 확인
- [ ] (후속 별건) 눈누 subsets 백필 + 파이프라인 수정

## 5. 메타데이터
- Codex 종료 코드: 0 | PR diff: 222줄
