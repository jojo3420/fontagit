# PR Review Report: #127 — feat: 캔버스-비교 화면을 홈 통합 보드로 합침 (#118)

> Generated: 2026-07-27 15:35
> PR URL: https://github.com/jojo3420/fontagit/pull/127
> Base ← Head: main ← feature/118-home-compare-canvas
> Codex: codex-cli 0.144.5, reasoning xhigh

## 0. PR 요약

- 변경 규모: PR diff 1,934줄(+ 스냅샷 바이너리 18개)
- 핵심 변경: /playground 캔버스 + 홈 비교 섹션 → 홈 통합 보드 CompareCanvas, 구화면-네비-sitemap-SEO-E2E 정리, /playground 302 리다이렉트

## 1. Codex 리뷰 (원문)

원문은 이 파일의 이전 버전(git history) 참조. 요지: 6/10, Must-fix 2건(브랜치 base 불일치, 404 스냅샷 오염), Should-fix 3건(E2E 검증 강도, toBeHidden, 상세 링크 aria 이름), Medium 3건(그리드 칸 수 동적, silent fallback, 입력 길이 무제한).

## 2. Claude 크로스 리뷰

### 항목별 판정

| # | Codex 지적 | Codex 심각도 | Claude 판정 | 근거 |
|---|-----------|-------------|------------|------|
| 1 | PR base가 계획(develop)과 다름 + 무관 문서 혼입 | Must-fix | 비동의 → 패스 | 계획 이후 사용자가 "main 기준" 명시 지시(세션 기록). 문서 혼입은 사용자 본인의 docs 전용 커밋으로 PR 본문에 고지됨. 계획 문서의 base 표기가 stale인 것은 사실 |
| 2 | 상세 스냅샷 4개가 404 화면으로 저장 | Must-fix | **동의 → Must-fix** | git hash-object 대조로 확증: 404와 상세 스냅샷 해시 동일(6bdd41e/76bc9fc). aa27d60에서 origin/main 기준선으로 복구 완료 |
| 3 | E2E가 첫 견본만 검증 | Should-fix | 동의(다음 iteration) | 사실. 검증 강화 시 e2e 풀런 필요해 후속 처리 |
| 4 | toBeHidden은 숨김도 통과 | Should-fix | 동의(다음 iteration) | 링크는 실제 JSX에서 삭제됨(기능 정상). 검증 강도 문제만 존재 |
| 5 | 상세 링크 aria 이름 9개 동일 | Should-fix | 동의(다음 iteration) | 타당한 접근성 개선 |
| 6 | 그리드 칸 수가 무료 폰트 수에 종속 | Medium | 부분 동의 | 설계가 "무료 9종(대표 제외 8칸)"을 의도적으로 데이터 파생으로 정의. 폰트 추가 시 자동 확장은 설계 논의 필요 |
| 7 | 폰트 조회 실패 silent fallback | Medium | 비동의 → 패스 | 후보와 slug가 동일 정적 배열이라 실패 경로 없음(TS 안전 가드). 설계 문서에 명시됨 |
| 8 | 입력 길이 무제한 | Medium | 동의(다음 iteration) | 로컬 상태 전용이라 보안 위험은 없으나 UX 방어 여지 있음 |

### 머지 결정: ✅ 머지 진행 (사용자 사전 승인)

- Must-fix 동의 1건(스냅샷 오염)은 aa27d60으로 해결 완료. 나머지 Must-fix 1건은 맥락 오해로 패스
- Must-fix: 0건(잔여) / Should-fix: 3건(후속) / Medium: 2건(후속) / 패스: 2건

## 3. 다음 단계 (후속 iteration)

- [ ] E2E 상세 페이지 404 근본 원인 조사(이 브랜치 무관, e2e 환경 기존 문제) — 해결 후 상세 스냅샷 정상 기준선 재생성
- [ ] E2E 견본 검증 강화(9개 확인) + toHaveCount(0) 전환 + 상세 링크 aria-label에 폰트명 포함
- [ ] 견본 입력 maxLength 검토
- [ ] 스펙-계획 문서의 base 브랜치 표기(develop) 갱신 여부 검토
