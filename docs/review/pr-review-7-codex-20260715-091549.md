### 종합 평가
- 전체 점수: 6/10
- 한줄 총평: Supabase 데이터 손실은 막았지만, 로컬 JSON은 여전히 라이선스 실패 시 전부 `draft`로 덮어써질 수 있습니다.
- 머지 권고: Must-fix 해결 전 불가

### 관점별 리뷰 (표)
| # | 관점 | 평가 | 핵심 지적 |
|---|---|---|---|
| 1 | 정합성 | 보통 | DB 업로드 차단은 맞지만 로컬 출력 데이터는 여전히 강등됨 |
| 2 | 영향도 | 위험 | 실패한 라이선스 조회 1번이 전체 레코드 상태를 바꿀 수 있음 |
| 3 | SOLID(SRP) | 보통 | `main()`이 수집, 라이선스, 저장, 업로드 정책을 모두 가짐 |
| 4 | 선제적 위험 | 부족 | Supabase 업로드가 레코드별 부분 성공 후 실패할 수 있음 |
| 5 | 방어적 입력 | 보통 | `FontRecord` 검증은 강화됐지만 `status` 전체 enum 검증은 모델에 없음 |
| 6 | 보안 | 양호 | `httpx` INFO 로그 억제, service key 분리 방향은 좋음 |
| 7 | Silent Failure | 보통 | 업로드 응답 빈 값은 잡지만 alias upsert 실패 정리는 부족 |
| 8 | 테스트 커버 | 보통 | 핵심 실패 경로 일부 추가, 하지만 “로컬 JSON 보존” 테스트 없음 |
| 9 | 스타일/가독성 | 보통 | 대체로 읽히지만 `main()` 흐름이 길어짐 |
| 10 | 머피의 법칙 | 부족 | 외부 API 실패, 부분 업로드, 스키마 불일치에 취약 |

### Critical 이슈 - Must-fix (머지 차단)

`apps/pipeline/src/fontagit_pipeline/__main__.py:72` / 라이선스 조회 실패 시 로컬 JSON 데이터가 여전히 손상될 수 있음 / 근거(diff 인용):  
```python
except LicenseFetchError:
    logger.warning("라이선스 조회 실패 — 전 레코드 draft 처리, 업로드는 보류")
    license_map = {}
    license_ok = False
...
doc = build_document(fonts, license_map, generated_at)
...
write_output(doc, _OUTPUT_PATH)
```
문제는 `license_map = {}` 때문에 모든 레코드가 `draft`가 된 문서를 만든 뒤 저장한다는 점입니다. Supabase 업로드는 막았지만, 로컬 JSON 파일은 기존 `published` 상태를 잃을 수 있습니다. 이전 지적이 “라이선스 조회 실패 시 데이터 손실”이었다면 절반만 고친 상태입니다.  
제안 수정: 라이선스 조회 실패 시 `build_document()`와 `write_output()`도 하지 말고 즉시 `return 3` 하세요. 정말 로컬 JSON을 남기고 싶다면 기존 파일과 다른 별도 경로, 예: `fonts.draft.json`로 저장해야 합니다.

`apps/pipeline/src/fontagit_pipeline/uploader.py:57` / Supabase 업로드가 원자적이지 않아 부분 반영될 수 있음 / 근거(diff 인용):  
```python
for rec in records:
    table.table("fonts").upsert(...).execute()
    ...
    table.table("aliases").upsert(...).execute()
```
중간 레코드에서 실패하면 앞 레코드는 이미 DB에 반영됩니다. 재실행 가능한 upsert라서 완전 치명적이지는 않지만, alias 실패 시 fonts만 갱신되는 불일치가 생깁니다.  
제안 수정: Supabase RPC 함수로 transaction 처리하거나, 최소한 `fonts` 전체 upsert와 `aliases` 전체 upsert를 배치로 분리하고 실패 로그에 실패 slug를 남기세요.

### High 이슈 - Should-fix

`apps/pipeline/src/fontagit_pipeline/models.py:42` / 모델의 `status` 값 검증이 DB보다 약함 / 근거(diff 인용):  
```python
status: str = "draft"
```
DB에는 `draft/published/archived` 제약이 있는데 Python 모델은 아무 문자열이나 받습니다. 테스트에도 `status="active"`가 허용됩니다. 이러면 로컬에서는 통과하고 DB 업로드에서 실패합니다.  
제안 수정: `Literal["draft", "published", "archived"]`로 제한하세요. 테스트의 `active`도 제거해야 합니다.

`apps/pipeline/src/fontagit_pipeline/models.py:82` / 테스트가 실제 허용 라이선스와 다른 값을 허용함 / 근거(diff 인용):  
```python
license_type="OFL 1.1"
license_verified=True
status="active"
```
운영 로직은 `OFL`, `Apache-2.0`, `UFL`만 published 허용인데 테스트는 `OFL 1.1`과 `active`를 정상 케이스로 둡니다. 테스트가 잘못된 사용을 정상처럼 가르칩니다.  
제안 수정: `license_type="OFL"`, `status="draft"` 또는 `"published"`로 바꾸세요.

`apps/pipeline/src/fontagit_pipeline/uploader.py:71` / 기존 alias 정리 없음 / 근거(diff 인용):  
```python
table.table("aliases").upsert(alias_rows, on_conflict="font_id,alias_norm").execute()
```
새 alias는 추가되지만, 더 이상 쓰지 않는 과거 alias는 삭제되지 않습니다. 폰트명이 바뀌거나 alias 생성 규칙이 바뀌면 검색 결과가 오래된 별칭을 계속 포함할 수 있습니다.  
제안 수정: 해당 `font_id`의 기존 alias를 삭제 후 재삽입하거나, `source`/`active` 컬럼을 두고 비활성화하세요.

### Medium 이슈

`apps/pipeline/src/fontagit_pipeline/licenses.py:42` / `entry["path"]` 직접 접근은 GitHub 응답 이상 시 취약함 / 근거(diff 인용):  
```python
result[entry["path"]] = license_type
```
외부 API 응답이 깨지면 `KeyError`가 납니다. 현재 `fetch_license_map()`은 `httpx.HTTPError`만 잡아서 이 경우 `LicenseFetchError`가 아니라 예상 밖 예외가 됩니다.  
제안 수정: `path = entry.get("path")`로 방어하고, JSON 구조 오류도 `LicenseFetchError`로 감싸세요.

`apps/pipeline/src/fontagit_pipeline/transform.py:121` / `license`와 `license_type` 의미가 겹침 / 근거(diff 인용):  
```python
license=None,
license_type=license_type,
```
`license`는 계속 `None`이고 실제 값은 `license_type`에만 들어갑니다. 필드가 둘이면 나중에 어느 쪽을 써야 하는지 혼란이 생깁니다.  
제안 수정: 당장 필요 없으면 `license` 필드를 제거하거나, `license_type` 하나로 통일하세요.

`apps/pipeline/src/fontagit_pipeline/__main__.py:92` / `published` 계산 후 실제 업로드에는 전체 레코드를 보냄 / 근거(diff 인용):  
```python
published = [r for r in doc.fonts if r.status == "published"]
uploaded = upload_records(doc.fonts, ...)
```
로그는 공개 개수를 따로 보여주지만 실제로는 `draft`도 업로드합니다. 의도라면 괜찮지만 “license-gated upload”라는 제목과는 약간 어긋납니다.  
제안 수정: draft도 DB에 저장하는 정책인지 명확히 주석이나 함수명에 남기세요.

### 긍정적 관찰

- 라이선스 조회 실패 시 Supabase 업로드를 막은 점은 이전 문제의 핵심 방향을 잘 잡았습니다.
- `FontRecord`에서 `published` 조건을 검증해 DB 제약과 일부 맞춘 점은 좋습니다.
- Supabase 설정이 한쪽만 있을 때 실패 처리한 점은 안전합니다.
- `httpx` 로그 레벨을 낮춰 API 키 노출 가능성을 줄인 점은 적절합니다.
- 라이선스 파싱, 변환, 업로더 테스트가 추가되어 회귀 방지 기반은 생겼습니다.