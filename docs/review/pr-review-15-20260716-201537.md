# PR Review Report: #15 — feat: 알리아스 검색 (슬라이스2, F-04)

> Generated: 2026-07-16 20:30 KST
> PR URL: https://github.com/jojo3420/fontagit/pull/15
> Base ← Head: main ← feat/search-f04
> Codex Model: gpt-5.6-sol (config 기본값, reasoning xhigh)

---

## 0. PR 요약

- 제목: feat: 알리아스 검색 (슬라이스2, F-04)
- 작성자: jojo3420
- 변경 규모: 마이그레이션 1 + 웹 6파일 + 테스트 3 + 문서 (리뷰 시점 diff 1,020줄)
- 핵심 변경: `search_fonts` RPC(정확 100 > 부분 50 > trgm 0~50, published만 20건) + `normalize_search`(NFC→공백제거→lower SSoT) + `/search` 페이지 + Header 연결

---

## 1. Codex 리뷰 (요약)

- 전체 점수: 6.5/10 | 머지 권고: ⚠️ Should-fix 수정 후 머지
- **Must-fix (Critical): 없음**
- Should-fix (High) 5건, Medium 7건 — 전문은 이 파일의 이전 버전(git에는 미추적) 대신 아래 크로스 리뷰 표에 항목별로 옮김

---

## 2. Claude 크로스 리뷰

### 항목별 판정

| # | Codex 지적 | 위치 | Codex | Claude 판정 | 근거 |
|---|-----------|------|-------|------------|------|
| H1 | 요청 중 입력 삭제 시 무한 로딩 | page.tsx | High | **동의 → 수정** | 코드 확인: 빈 쿼리 분기에 setLoading(false) 부재 — 실버그 |
| H2 | RPC 실패를 "결과 없음"으로 표시 | search.ts | High | 부분 동의 → **이연** | 계획 v1.1 계약이 "오류 → 빈 배열 + 로깅" 명시(plan-mandated). UI 오류 상태 구분은 다음 iteration |
| H3 | 서버측 입력 제한 부재 + LIKE 와일드카드 | 0006 SQL | High | **동의 → 수정** | anon 공개 RPC라 클라 제한 우회 가능. '%'가 전체 매칭 확인 |
| H4 | pg_trgm public 설치 가정 | 0006 SQL | High | **동의 → 수정** | 기존 확장이 타 스키마면 IF NOT EXISTS가 위치 안 바꿈 — prod 리스크 |
| H5 | SQL 점수 로직 자동 테스트 부재 | 테스트 | High | 비동의 → **이연** | 계획이 SQL Editor 수동 검증 명시 + dev 실측 완료 + DB 테스트 인프라 부재(신규 구축은 스코프 밖) |
| M1 | URL q 변경 미반영 | page.tsx | Medium | 동의 → 이연 | 뒤로가기 엣지 — 다음 iteration |
| M2 | `data as` 강제 단언 | search.ts | Medium | 동의 → 이연 | 런타임 검증은 supabase 생성 타입 도입 시 |
| M3 | 동점 정렬 불안정 | 0006 SQL | Medium | **동의 → 수정** | name_en, slug tie-breaker 추가 (저비용) |
| M4 | 상관 서브쿼리 성능 | 0006 SQL | Medium | 동의 → 이연 | 현재 130종 규모 무의미, 데이터 증가 시 재검토 |
| M5 | input label/스크린리더 안내 부재 | page.tsx | Medium | **동의 → 수정(부분)** | aria-label 추가. 상태 라이브 리전은 이연 |
| M6 | 문서 category vs category_ko | 계획 문서 | Medium | **동의 → 수정** | 표기 정정 + 매핑 주석 |
| M7 | progress.md stale | docs | Medium | 동의 → 해소 | progress 갱신 커밋 de2e649 |

### 수정 커밋 및 재검증 (증거)

- Fix 커밋: `0eeae9b` — H1, H3, H4, M3, M5(부분), M6 일괄 반영. 회귀 테스트 1개 추가(요청 중 입력 삭제 → 로딩 해제)
- 웹: 93/93 테스트 통과, `tsc --noEmit` 0 에러
- dev 실측(재적용 후): 기존 4케이스 유지(본고딕 100 / 나눔 5건 / 본고딩 17 / '본 고딕' 100) + `search_fonts('%')`=0건, `'_'`=0건, 101자=0건 — 와일드카드-길이 방어 동작 확인. 확장 스키마 가드 DO 블록 통과

### Codex가 놓친 관점

- 없음 (자체 최종 whole-branch 리뷰(Opus)와 교차: 보안 경계-계약 정합-정규화 SSoT 모두 양호 판정 일치)

---

## 3. 최종 권고 및 머지 결정

### 머지 결정: ✅ 머지 (squash) — Must-fix 0건, 동의한 Should-fix 반영 완료

### ⚠️ Should-fix 이연 (다음 PR)

- H2: 검색 오류 상태 UI 구분("검색에 실패했습니다" — 0건과 장애 구분)
- H5: DB 통합 테스트(점수-필터 자동 검증) — 테스트 인프라 도입과 함께

### 💡 Nice-to-have (이연)

- M1 URL q 동기화, M2 런타임 타입 검증, M4 aliases 집계 쿼리 비교, M5 잔여(상태 라이브 리전)

### 🚫 패스 처리

- 없음 (전 항목 동의 또는 부분 동의)

---

## 4. 다음 단계

- [x] 머지 후 로컬 브랜치 정리 대상: `feat/search-f04`, `feat/search-alias-f04`(구)
- [ ] prod 배포 시 마이그레이션 0006 적용 (prod 쓰기는 사용자 확인 필수)
- [ ] 슬라이스3 클릭 집계(0007) 착수

---

## 5. 메타데이터

- Codex 종료 코드: 0 (1차 실행은 codex CLI 자체 행으로 timeout 124 — 바이너리 복사 우회로 해결, progress-003.md 시행착오 기록 참조)
- PR diff 크기: 1,020줄 (리뷰 시점)
- 이 리포트: `docs/review/pr-review-15-20260716-201537.md`
