# PR Review Report: #102 — feat: automated metadata approval and cross-target audit manifest
> Generated: 2026-07-23 18:45 (KST)
> PR URL: https://github.com/jojo3420/fontagit/pull/102
> Base ← Head: develop ← feature/collections-phase0-auto-approve
> Codex Model: codex config 기본값 (reasoning=xhigh)

---

## 0. PR 요약
- 제목: feat: automated metadata approval and cross-target audit manifest
- 작성자: jojo3420
- 변경 규모: +1,618 -180 across 12 files (리뷰 반영 후 커밋 21개)
- 핵심 변경: metadata(tags/weights) 무인 승인 CLI + 감사 manifest의 dev/prod 교차 적용 경로. 라이브 파일럿으로 dev/prod 각 47종 적용-재검증 완료.

---

## 1. Codex 리뷰 (원문 요지)
- 전체 점수 5.5/10, 머지 권고 ❌ Must-fix 해결 전 머지 불가
- Critical 2: (C1) 자동 승인이 run 상태/게이트/증거를 확인 안 함 (C2) finding 값과 증거 추출값 미대조
- High 5: (H1) reviewed_by 임의 지정 (H2) snapshot run_id 검증 제거 (H3) 성공 로그 조용함 (H4) prod 조회 N+1 (H5) SQL RPC 통합테스트 부재
- Medium 6: 투영 sha 비결정성, bootstrap-apply SRP, dev 기준선의 prod 상수 검증, applied→approved 정규화 위치, 다중 증거 거부 계약, CLI 도움말 문구
- 원문 전체: git 히스토리의 본 파일 이전 버전 참조

---

## 2. Claude 크로스 리뷰

### 종합 소견
Codex 지적 대부분이 실코드와 일치했고, 특히 C1/C2는 dev 실데이터로 재현-실증됐다. 단 C1의 "auto_applicable=true 강제"와 H2의 "run 등가 비교 복원"은 이 PR의 명시적 설계 결정(무인 승인 거버넌스, evidence run-agnostic 계약)과 충돌해 비동의. 동의 항목은 머지 전 전부 수정 반영했다(커밋 672fcad + 5b086e4, 276 passed).

### 항목별 판정
| # | Codex 지적 | Claude 판정 | 처리 |
|---|-----------|------------|------|
| C1 | 자동 승인이 run 상태/게이트 미확인 | 동의(실증: broken 50% run도 status=completed → 수동 우회 가능) | ✅ 수정: run status=completed + broken 10% 게이트 재확인. auto_applicable 강제는 비동의(무인 승인 거버넌스 결정과 충돌) |
| C2 | finding 값-증거 미대조 | 동의(실증: proposed==extracted 정확 일치 → 저렴한 검증 가능) | ✅ 수정: 승인 전 derive_proposed_value(작성기와 동일 파생)로 대조, 불일치 시 실패+exit 3. SQL 계층 중복 검증은 후속 |
| H1 | reviewed_by 임의 지정 | 동의 | ✅ 수정: auto 고정 |
| H2 | snapshot run_id 검증 제거 | 부분 동의 | ✅ UUID 형식 검증 2곳 복원. run 등가 비교/origin_run_id 분리는 비동의(run-agnostic 계약이 의도, 스키마 변경 대비 이득 낮음) |
| H3 | 성공 로그 조용함 | 동의(실측: apply 조용한 성공이 재시도 혼선 유발) | ✅ 수정: basicConfig+요약 로그 |
| H4 | prod 조회 N+1 | 동의(1,110 확장 시 실질) | ⏭ 후속: 확장 작업과 함께 일괄 조회로 |
| H5 | SQL RPC 통합테스트 부재 | 부분 동의 | ⏭ 후속: 저장소에 DB 테스트 인프라 자체가 없음(0018도 동일). 라이브 파일럿+0018 대비 diff 증명(3+3곳)으로 대체 검증됨 |
| M1 | 투영 sha 비결정성(set 순서) | 동의 | ✅ 수정: 고정 tuple+sort_keys |
| M2 | bootstrap-apply SRP | 동의 | ⏭ 후속 |
| M3 | dev 기준선의 prod 상수 검증 | 동의 | ⏭ 후속 |
| M4 | applied→approved 정규화 위치 | 부분 동의(트레이드오프) | ⏭ 후속 검토 |
| M5 | 다중 증거 거부 계약 불명확 | 동의 | ⏭ 후속(계약 명문화) |
| M6 | CLI 도움말 문구 불일치 | 동의 | ✅ 수정 |

### Codex가 놓친 관점
- 파일럿에서 이미 잡아 수정된 결함들(실스키마 불일치, dedup 모순 등)이 이 PR의 핵심 가치라는 맥락 — 점수 5.5는 반영 전 기준.

---

## 3. 최종 권고 및 머지 결정

### 머지 결정: ⏸ 사용자 선택 대기 (Must-fix 잔존 0건)
- 동의한 Critical/High/Medium 중 즉시 수정 가능 항목 전부 반영 완료: 커밋 `672fcad`(수정) + `5b086e4`(회귀 테스트 4건+Bundle UUID 검증), 276 passed + mypy clean, push 완료.

### ⚠️ Should-fix (후속 PR — 이슈화 권장)
1. H4: prod 교차 조회 일괄화 (1,110 확장 전 필수)
2. H5: RPC 통합테스트 인프라 (별도 설계 필요)
3. M2/M3/M4/M5: SRP 분리, dev 기준선 상수, 정규화 위치, 다중 증거 계약

### 🚫 패스 처리 (비동의, 사유)
- C1의 auto_applicable=true 강제: metadata 무인 승인은 사용자의 명시적 거버넌스 결정(2026-07-23) — 해당 플래그는 legal 자동적용 경로용
- H2의 run 등가 비교 복원/provenance 컬럼 분리: evidence run-agnostic(0019)이 이 PR의 의도된 설계, 실데이터 dedup과 정합

---

## 4. 다음 단계
- [ ] 머지 방식 선택 후 머지 (사용자)
- [ ] 후속 이슈 등록: H4, H5, M2~M5, Dockerfile tests/ COPY, 1,110 확장 wrapper
- [ ] 머지 후 로컬 브랜치 정리

---

## 5. 메타데이터
- Codex 종료 코드: 0 / PR diff 크기: 2,309줄
- 리뷰 반영 검증: 276 passed, 4 skipped(linux 전용) / mypy 신규 코드 clean
- 이 리포트: docs/review/pr-review-102-20260723-182220.md
