# Dual PR Review Report (재리뷰): #7 — Slice 0 data pipeline
> Generated: 2026-07-15 (2차, 수정 커밋 a20641d 반영)
> PR URL: https://github.com/jojo3420/fontagit/pull/7
> Reviewers: Codex (gpt-5.5, xhigh) + agy (Gemini 3.5 Flash High)
> Mode: **Degraded — Codex 단일** (agy 인증 만료 재실패, exit=1)

---

## 0. 1차 대비 변화
- 1차 Must-fix(라이선스 실패 → Supabase published 전량 draft 덮음): **해결** — Codex 긍정 관찰에 명시. license_ok=False 시 upload `return 3`으로 기존 데이터 보존.

## 1. 모델 상태
- Codex: ✅ 완료 (exit 0) — `docs/review/pr-review-7-codex-20260715-091549.md`
- agy: ❌ 인증 실패 (exit 1) — Google OAuth 미로그인. Degraded 유지.

## 2. Claude 크로스 리뷰 (Codex 단독, diff 대조)

| # | 지적 | 위치 | Codex 심각도 | Claude 판정 | 근거 |
|---|------|------|--------------|-------------|------|
| 1 | 라이선스 실패 시 로컬 JSON도 전부 draft로 덮임 | `__main__.py` write_output | Critical | **Should-fix** | `output/tier-a.json`은 gitignore 파생 산출물, 원본은 Supabase(업로드는 이미 차단됨), 매 실행 재생성. 일관성 위해 실패 시 return 3 권장하나 데이터 손실 아님 |
| 2 | 업로드 비원자적(fonts 후 aliases 실패 시 불일치) | `uploader.py` | Critical | **Should-fix** | 멱등 upsert라 재실행 수렴. Codex도 "완전 치명적 아님". 트랜잭션(RPC)은 다음 iteration |
| 3 | status가 str(Literal 아님), 테스트에 status="active"/license_type="OFL 1.1" | `models.py:36`, `test_models.py:93,95` | High | **동의 → Should-fix** | 실제 확인: 모델-DB enum 불일치, 테스트가 비표준 값을 정상 케이스로. 방어 강화 필요 |
| 4 | stale alias 미삭제 | `uploader.py` | High | Should-fix (다음 iteration) | 폰트명 변경 시 과거 alias 잔존. 동기화 정책 필요 |
| 5 | licenses.py entry["path"] KeyError 취약 / license-license_type 중복 / draft도 업로드 | 여러 | Medium | 다음 iteration | 방어/정리 권장, 비차단 |

### 환각 점검
- Codex 인용 라인 모두 실제 diff/파일에 존재. #1-#2는 심각도만 하향(파생 산출물/멱등), 환각 아님. #3 테스트 결함은 grep으로 실증.

## 3. 머지 결정: MERGE_POSSIBLE (Must-fix 0)
핵심 데이터 손실(Supabase)은 해결. 남은 것은 전부 Should-fix/Medium.

### 🟡 빠른 Should-fix (반영 권장, 저비용)
- 라이선스 실패 시 로컬 JSON도 안 만들고 return 3 (일관성)
- status를 Literal["draft","published","archived"]로 제한 + 테스트 비표준 값(active, OFL 1.1) 정정

### 💡 다음 iteration (별도 이슈 권장, 큰 변경)
- 업로드 원자성(Supabase RPC 트랜잭션), stale alias 동기화, license/license_type 필드 통합, licenses.py JSON 구조 방어

## 4. 메타데이터
- Codex exit 0 / agy exit 1(인증) / 코드 diff 1327줄
- Codex 리뷰: `docs/review/pr-review-7-codex-20260715-091549.md`
