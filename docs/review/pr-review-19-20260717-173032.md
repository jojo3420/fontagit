# PR Review Report: #19 — feat: chosung search DB engine + SSG build stabilization
> PR: https://github.com/jojo3420/fontagit/pull/19
> Base <- Head: develop <- feat/search-autocomplete
> Codex: codex-cli 0.144.5, reasoning effort xhigh

---

## 0. PR 요약
- 변경 규모: +513 -4 (11 files). 핵심: 초성 검색 DB 엔진(0009), 설계 문서, `getAllFonts` aliases in-list 제거(fonts.ts), `staticGenerationRetryCount`(next.config), fontagit REST 노출(0010).
- 내 세션 커밋(48dcf6d)은 fonts.ts + next.config.ts 2파일. 나머지는 선행 커밋(de85784 등).

## 1. Codex 리뷰 요약
- 점수 5.5/10, 머지 권고: Must-fix 해결 전 불가(Critical 3건 주장).

## 2. Claude 교차 검증 (증거 기반)

| # | Codex 지적 | Codex 심각도 | Claude 판정 | 근거 |
|---|-----------|-------------|------------|------|
| C1 | 0010이 db_schemas를 고정목록으로 덮어씀(타 환경 스키마 유실 위험) | Critical | 동의(강등)→Should | prod는 기존 목록이 정확히 `public,storage,graphql_public`임을 실측 후 fontagit만 추가 → 실제 유실 없음. 단 멀티환경엔 취약, 견고화 권장 |
| C2 | 스키마 노출 + PUBLIC 함수 → anon이 내부/쓰기 함수 호출 | Critical | **비동의(반증)→Nice** | prod pg_proc 실측: upsert_font/sync_tier_a는 service_role 전용, anon 미부여. PUBLIC은 normalize_search(순수 텍스트 헬퍼)뿐 = 쓰기-유출 없음. anon은 이미 search_fonts로 normalize 도달 |
| C3 | 설계문서 /api/suggest가 output:export와 비호환 | Critical | 동의(강등)→Should(문서/후속) | 사실이나 **문서 상의 설계**일 뿐 이 PR에 구현 코드 없음(미커밋 WIP). 이미 handoff로 분리됨 |
| H1 | aliases `.select("*")` 무제한 조회 → max-rows 초과시 무음 절단 | High | 동의→Should | 현재 alias 319건(안전)이나 성장 시 silent truncation 위험. published id 청크 `.in()`로 URL-절단 동시 해결 권장 |
| H2 | service_role 전환 시 RLS 우회로 draft 유출 | High | 부분 비동의→Nice | fonts 쿼리가 이미 `.eq(status,published)` + 그룹핑이 published 폰트에만 alias 부착 → draft가 결과에 안 실림. 가정 명시는 개선사항 |
| H3 | 0009: normalize_search가 100자 검사보다 먼저 실행(prep 비용) | High | 동의→Should(경미) | 원본 q 길이 선검사로 방어. 0009(선행 커밋) |
| H4 | 테스트가 핵심 미검증(C6 assertion 없음 등) + fonts.ts 회귀 테스트 없음 | High | 동의→Should | 초성 해피패스/draft 미노출/lim 경계 + getAllFonts 그룹핑 테스트 필요 |
| M1 | 설계문서 0008 vs 실제 0009 파일명 불일치 | Medium | 동의→Medium(문서) | 사소, 문서 수정 |
| M2 | retryCount가 지속장애 시 부하 증폭 + 적용 검증 | Medium | 부분동의→Nice | 빌드 배너/재시도 로그로 **적용은 확인됨**. 증폭은 경미 |
| M3 | text_pattern_ops 인덱스 실사용 EXPLAIN 미확인 | Medium | 동의→Nice | 운영 데이터량 EXPLAIN 권장 |

### 핵심 결론
- **동의 Must-fix(머지 차단): 0건.** Codex Critical 3건은 검증 후 전부 강등: C1(prod 실측 정확), C2(반증-쓰기함수 anon 미노출), C3(문서/후속).
- Should-fix 4건(H1 aliases 절단, H3 입력검증 순서, H4 테스트, C3 문서 재설계), Nice 다수.

## 3. 최종 권고
### Must-fix (머지 차단)
- 없음.

### Should-fix (후속 권장)
- H1: `getAllFonts` aliases 조회를 published id 25~50개 청크 `.in()`로 변경(무음 절단 방지). **내 fonts.ts 변경이라 지금 반영 가능**.
- H4: fonts.ts 회귀 테스트 + 0009 테스트 assertion 보강.
- C3: 자동완성 설계를 클라이언트 직접 RPC 호출로 재설계(= /api/suggest handoff의 핵심).
- C1: 0010을 기존 목록 병합 방식으로 견고화 또는 검증 목록 문서화.

### Nice-to-have
- normalize_search PUBLIC EXECUTE 회수, H3 입력순서, M1 문서 파일명, M3 인덱스 EXPLAIN.

### 머지 결정: 사용자 승인 대기 (Must-fix 0 → 머지 가능)

## 4. 메타
- Codex 종료: 정상. diff 587줄. Claude 교차검증: 증거 기반(prod pg_proc 실측 포함).
