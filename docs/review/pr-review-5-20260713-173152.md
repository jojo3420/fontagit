# PR Review Report: #5 — feat(web): Phase 3-4 확장 화면 + 마감
> Generated: 2026-07-13 17:35
> PR URL: https://github.com/jojo3420/fontagit/pull/5
> Base ← Head: main ← develop
> Codex Model: gpt-5.6-sol (xhigh), 코드 diff(tsx/ts/css 1146줄) 리뷰

---

## 0. PR 요약
- 제목: feat(web): Phase 3-4 확장 화면 + 마감 (캔버스/비교/컬렉션/등록/모바일/다크/OG)
- 작성자: jojo3420
- 변경 규모: 코드 33파일(tsx/ts/css) + md 문서 + 스냅샷 PNG. 코드 diff 1146줄.
- 핵심 변경: 확장 화면 5(playground/compare/collections/submit/EmptyState) + 마감 3(MobileTabBar/ThemeToggle/OG)

---

## 1. Codex 리뷰 (원문 요약)
- 점수 7/10, Critical(Must-fix) 0, High 2, Medium 3, 머지 권고 "High 수정 후"
- High 1: /compare가 Header-MobileTabBar 어디에도 링크 없어 고립. /playground는 모바일 탭에만.
- High 2: layout에 viewportFit:"cover" 없어 safe-area 무효 + 탭 높이 56px에 safe-area 패딩 포함(border-box)돼 notch 기기서 탭 축소.
- Medium: smoke가 "모든 견본"인데 .first()만 검사 / ThemeToggle localStorage 빈 catch(silent) / 컬렉션 내부 중복 fontSlug 미검사(상세 key 충돌 가능).
- 잘된 부분: 클라이언트 경계-정적 라우트-OG 정적 생성-컬렉션 참조 검증 구조적 적절, XSS/외부링크 이슈 없음, 의도된 목업/OG 인라인 예외는 결함 처리 안 함. tsc/eslint 독립 통과.

---

## 2. Claude 크로스 리뷰

### 종합 소견
Codex 지적 5건 전부 diff에서 사실 확인(환각 0). Must-fix(Critical) 없음 → 머지 가능. High 2건은 실제 결함이나 비치명적(화면 동작, 탭 클릭 가능). High 1(진입점)은 계획 범위 밖 갭, High 2(safe-area)와 Medium 3(중복 fontSlug)은 간단-저위험 실결함.

### 항목별 판정
| # | Codex 지적 | 파일 | 심각도 | 판정 | 근거(grep 확인) |
|---|-----------|------|--------|------|------|
| 1 | /compare-/playground 진입점 고립 | Header.tsx, MobileTabBar.tsx | High | 동의 → Should-fix | Header nav=/,/collections,/fonts,/submit,/trends. MobileTabBar=/,/fonts,/trends,/playground. /compare 어디에도 없음. 계획 갭(Header 4nav 브리프대로, 9a 진입배너 Phase3-4 미포함) |
| 2 | viewport-fit=cover 없음 + 탭 높이 safe-area 포함 | layout.tsx, MobileTabBar.module.css | High | 동의 → Should-fix | layout viewport export 0건. `.bar height:56px + padding-bottom:env(safe-area)` border-box. iOS서 safe-area 무효 + notch 탭 축소 |
| 3 | 컬렉션 내부 중복 fontSlug 미검사 → key 충돌 | lib/data.ts, [slug]/page.tsx | Medium | 동의 → Should-fix | 상세 `key={it.fontSlug}`. assertDataIntegrity가 컬렉션 내 중복 fontSlug 미검사. 현재 데이터 안전, 미래 방어 갭 |
| 4 | smoke "모든 견본"인데 .first()만 | smoke.spec.ts | Medium | 동의 → Nice | playground/compare 테스트가 첫 견본만 확인. 한 칸 회귀 못 잡음 |
| 5 | ThemeToggle localStorage 빈 catch | ThemeToggle.tsx | Medium | 부분 동의 → Nice | 빈 catch는 접근 불가 환경(private 모드) 방어로 의도적. 실패해도 화면은 바뀜, 새로고침 시 FOUC 스크립트 fallback. 경고 로그 추가는 개선 여지지만 낮은 우선순위 |

### 비동의/패스 항목
없음. Codex 지적 전부 유효(환각 0).

### Codex가 놓친 관점
없음(이번 PR 코드 범위 내). metadataBase(배포 시점)는 프롬프트 컨텍스트로 이미 공유돼 Codex도 지적 안 함.

---

## 3. 최종 권고 및 머지 결정

### 머지 결정: ⏸ 사용자 보류 (Must-fix 0 → 머지 가능하나 High 2건 처리 방침 확인 필요)

### ❌ Must-fix (머지 차단): 없음

### ⚠️ Should-fix (머지 전 수정 권장 — 간단-저위험)
- High 2: layout에 `viewport: { viewportFit: "cover" }` 추가 + MobileTabBar 탭바 높이를 `calc(56px + env(safe-area-inset-bottom))`로. (safe-area 코드가 실제 효과 내도록)
- Medium 3: `assertDataIntegrity`에 컬렉션 내부 중복 fontSlug 검사 추가(상세 key 충돌 방어).

### 💡 Should-fix (디자인 결정 필요 — 후속 권장)
- High 1: /compare-/playground 진입점 추가. 계획 범위 밖(스펙 9a "홈→캔버스 진입 배너"가 Phase 3-4에서 생략됨). 어디에 링크를 둘지(Header nav 확장 vs 홈 배너) 디자인 결정 후 별도 작업 권장.

### 💡 Nice-to-have
- Medium 4: smoke 테스트가 3개 견본/컬럼 전부 갱신을 확인하도록 강화.
- Medium 5: ThemeToggle localStorage 실패 시 경고 로그.

---

## 4. 다음 단계
- Must-fix 0 → 머지 자체는 가능.
- 권장: safe-area(High2) + 중복 fontSlug 무결성(Med3)은 간단하니 머지 전 수정. 진입점(High1)은 디자인 결정이라 이 PR 후속.

---

## 5. 메타데이터
- Codex 종료: 정상(1-2차 xhigh 타임아웃 후 3차 read-only/diff-only로 완료)
- PR diff: 전체 3469줄 중 코드 1146줄 리뷰(md 문서-PNG 스냅샷 제외)
- 리포트: docs/review/pr-review-5-20260713-173152.md
