# Dual PR Review Report: #154 — 눈누 official_url 오염 172종 정정

> Generated: 2026-07-30 21:48:39
> PR: https://github.com/jojo3420/fontagit/pull/154
> Base <- Head: main <- feat/150-noonnu-url-finding-ingest
> Reviewers: Codex (gpt-5.6-sol, max) + Antigravity agy (gemini-3.6-flash-high)
> Mode: Dual (Codex 1차 8분 타임아웃 → 20분 재시도 exit 0, agy exit 0)

---

## 0. PR 요약

- 눈누(noonnu.cc) 상세 페이지 본문 앵커를 근거로 `official_url` / `license_source_url` 오염을 정정하는 스캔-적재-승인-적용 경로
- 리뷰 대상 diff: 코드 파일만 추림 (apps/pipeline 9개 + supabase/migrations/0027 + scripts/verify SQL), 1132줄 추가
- 나머지 변경(문서-목업-리뷰 리포트 3600여 줄)은 리뷰 대상에서 제외
- dev/prod 양쪽 적용 완료(각 175행) 후의 사후 리뷰

## 1. 모델별 리뷰 원문

- Codex: `docs/review/pr-review-154-codex-20260730-214839.md` (5/10, 머지 불가 판정)
- agy: `docs/review/pr-review-154-agy-20260730-214839.md` (9/10, 즉시 머지 가능 판정)

두 모델의 판정이 극단적으로 갈렸다. Codex는 SQL 게이트의 근거 검증을 조목조목 파고들어
Must-fix 4건을 냈고, agy는 순수 함수 분리와 회귀 테스트 확충을 높이 사 Must-fix 0건을 냈다.

## 2. Claude 통합 크로스 리뷰

### 종합 소견

Codex의 Must-fix 4건 중 **실제로 데이터를 오염시킬 수 있는 것은 1건**(리다이렉트)이었고,
나머지 3건은 실재하는 결함이지만 사람 승인 manifest라는 상위 게이트가 있어 단독으로는
손상을 만들지 못한다. agy가 Must-fix 0건을 낸 것은 그 상위 게이트를 신뢰한 결과이고,
Codex가 4건을 낸 것은 "게이트 자체가 제 역할을 하는가"를 물은 결과다. 후자의 질문이 더 유용했다.

두 모델이 함께 잡은 지적은 2건(근거 역할 교차 허용, run 상태 고립)이며 둘 다 실재한다.

### 항목별 판정

| # | 지적 | 파일:라인 | 출처 | 모델 심각도 | Claude 판정 |
|---|------|---------|------|-----------|------------|
| 1 | 리다이렉트로 다른 폰트 페이지에 닿아도 원본 ID로 적재 | `noonnu_url_scan.py:249`, `noonnu_url_ingest.py:39` | Codex | Critical | **동의 → 이 PR에서 수정** (`fe08c0c`) |
| 2 | 근거 역할과 허용 필드가 교차 오염 | `0027:191` | 합의 (Codex Critical / agy Medium) | Critical/Medium | 동의 → High, #157로 분리 |
| 3 | 스냅샷이 proposed_value를 증명하지 않음 | `0027:147` | Codex | Critical | 동의 → High, #157로 통합 |
| 4 | run이 `running`으로 고립 / 구조 검사 전에 완료 처리 | `__main__.py:300,327` | 합의 (Codex Critical / agy High) | Critical/High | 동의 → High, #158로 분리 |
| 5 | `--include-manual-review`에 검토 증거 없음 | `__main__.py:1534` | Codex | High | 동의 → #158 |
| 6 | `--limit`이 actionable 필터보다 먼저 | `__main__.py:244` | Codex | High | 동의 → #158 |
| 7 | 검증 SQL이 이번 run 결과를 증명 못 함 | `verify-noonnu-url-fix.sql:25` | Codex | High | 동의 → #158 |
| 8 | 거부 케이스 테스트 부재 | `test_noonnu_url_ingest.py` | Codex | High | 동의 → 일부 이 PR에서 추가, 나머지 #157 |
| 9 | #69 설계 문서 커밋이 섞임 | 커밋 `2e91fa8`, `5612c0b` | Codex | High | 동의 → 사용자 판단 대기 |
| 10 | `needs_review_count`가 nullify 누락 | `__main__.py:320` | Codex | Medium | 동의 → #158 |
| 11 | manifest가 행을 임의 순서로 하나씩 잠금 | `0027:101` | Codex | Medium | 부분 동의 — 이론상 교착 가능하나 적용은 단일 운영자 수동 실행이라 동시성 없음 |
| 12 | `license_verified`가 finding 없이 변경 가능 | `0027:223` | Codex | Medium | 동의 — #155(license_verified 정책)와 같은 뿌리, 그쪽에서 처리 |
| 13 | `scan_targets`의 책임 과다 | `noonnu_url_scan.py:421` | Codex | Medium | 동의 → 리팩토링 후보, 이슈 미등록 |
| 14 | 더미 `uuid4()`로 expected_findings 계산 | `__main__.py:315` | agy | Medium | 동의 → Nice-to-have, #158의 실제 건수 조회로 함께 해소 |

**비동의/패스: 없음.** 두 모델의 지적 중 환각으로 판정한 항목은 없었다. 심각도만 재조정했다.

### 모델 합의도

- 합의 지적: 2건 (#2 근거 역할 교차, #4 run 상태 고립)
- Codex 단독: 11건
- agy 단독: 1건
- Claude 자체 발견: 0건

Codex가 압도적으로 많이 잡았다. `model_reasoning_effort = max` 설정과, SQL 게이트 로직을
줄 단위로 따라간 접근 차이로 보인다. agy는 구조-테스트 품질 평가에 무게를 뒀다.

## 3. 최종 권고 및 머지 결정

### Must-fix: 0건 (수정 완료)

`fe08c0c` — 리다이렉트로 다른 폰트 페이지에 닿은 근거의 적재를 차단:
- `build_snapshot_draft`가 요청 URL과 `final_url`의 페이지 ID를 대조해 다르면 `ValueError`
- `provider_record_id_from_source_url`이 `/font_page/<숫자>` 형식을 강제
- 회귀 테스트 3건 추가 (리다이렉트 거부, 비숫자 경로 거부)
- 검증: `pytest -q` 492 passed 4 skipped / `ruff check` 통과 / `mypy src` 신규 오류 0(기준선 70건 유지)

### 후속 이슈

- #157 (high) — 0027 근거 게이트 정밀화. 필드-역할 1:1 허용, proposed_value 값 대조, 0028 보정 마이그레이션
- #158 (medium) — 스캐너 run 수명주기, `--limit` 순서, 검증 SQL 정확도, `--include-manual-review` 검토 증거
- #155 (기존) — `license_verified` 정책. Codex #12가 같은 뿌리
- #156 (기존) — 사람 검수 8종

### 미결: PR 범위 혼재

커밋 `2e91fa8`, `5612c0b`는 다른 세션의 #69 플레이그라운드 설계 문서다. 코드 변경은 없지만
머지하면 함께 main에 들어간다. 분리 여부는 사용자 판단.

## 4. 메타데이터

- Codex 종료 코드: 124(1차 8분 타임아웃) → 0(20분 재시도)
- agy 종료 코드: 0
- 리뷰 대상 코드 diff: 1132줄 (PR 전체 diff는 4726줄)
- 프롬프트: `scratchpad/pr-dual-prompt.txt` (1652줄)
