# Dual PR Review Report: #4 — feat(web): FontAgit 웹 토대 + 핵심 화면 (Phase 1-2)

> Generated: 2026-07-13 12:15
> PR URL: https://github.com/jojo3420/fontagit/pull/4
> Base ← Head: main ← develop
> Reviewers: Codex (gpt-5.6-sol, xhigh) + Antigravity agy (Gemini 3.5 Flash High)
> Mode: **Degraded — 단일 모델(Codex)만 유효**. agy는 두 차례 모두 stdin 프롬프트를 리뷰로 처리하지 못하고 자기 CLI 플래그(`--dangerously-skip-permissions`)만 파고들어 유효 리뷰 미생성. Codex 단독 + Claude diff 대조로 진행.

## 0. PR 요약
- 웹 앱(apps/web)을 처음 도입하는 PR. 리뷰 대상은 apps/web 소스 62파일 1778줄(전체 17,064줄 중 lockfile/스냅샷/설계번들 제외).
- Codex 종합: 5.8/10, "Must-fix 해결 전 머지 불가".

## 1. 모델별 리뷰 원문
- Codex: `docs/review/pr-review-4-codex-20260713-120348.md` (120줄, 정상)
- agy: `docs/review/pr-review-4-agy-20260713-120348.md` (실패 — 리뷰 아님)

## 2. Claude 통합 크로스 리뷰 (Codex 지적 diff 대조)

| # | Codex 지적 | 파일 | Codex 심각도 | Claude 판정 | 근거 |
|---|-----------|------|------|------------|------|
| C1 | FontCard가 상세로 이동 Link 없음(목록-무료대안 클릭 불가) | components/FontCard.tsx | Critical | **동의 → Must-fix (확인)** | `<article>`만 있고 Link 없음. 핵심 탐색 끊김. 실제 버그 |
| C2 | Header /collections-/submit 링크 클릭 시 404 | components/Header.tsx | Critical | 부분동의 → Should-fix | Phase 3 미구현 페이지. 브랜드 404가 뜨므로 치명적 아님. 의도된 범위 |
| H1 | 검색-필터 무동작 + 트렌드 양쪽 표 표시 | Hero/FilterChip/trends | High | 비동의 → 패스(의도) | 스펙: 필터/검색은 비동작 목업. 의도된 Phase 1-2 |
| H2 | E2E가 콘솔에러만 확인, 404 통과 가능 | e2e/smoke.spec.ts | High | 부분동의(대부분 해결됨) | 404는 이미 브랜드내용 단언으로 수정됨(Task13). 상태코드/제목 단언은 개선 여지 |
| H3 | start=next start(export와 불일치) + e2e가 out/ 가정 | package.json | High | **동의 → Should-fix (수정)** | 실제 불일치. start 깨짐, e2e 빌드 선행 필요 |
| H4 | next/font/google 빌드 네트워크 의존 | lib/fonts.ts | High | 비동의 → 패스(설계) | self-host를 next/font로 하는 의도적 선택. 트레이드오프 |
| H5 | verifiedAt 데이터 있으나 화면 미표시 | 상세 페이지 | High | **동의 → Minor (수정)** | 상세에 확인일 미표시였음. 표시 추가 |
| M1 | 상세 페이지 max-width/여백 없음 | [slug]/page.module.css | Medium | **동의 → Should-fix (수정)** | .wrap에 gutter 없어 가장자리 붙음 |
| M2 | 트렌드 slug find()!, 빈 weights, URL 미검증 | lib/data.ts 등 | Medium | 부분동의 → Nice | 무결성검사가 slug/fontKey/freeAlt는 검증. 빈 weights/URL은 미검증(목업이라 저위험) |
| M3 | 인라인 테마 스크립트 CSP 저해 | app/layout.tsx | Medium | 부분동의 → Nice | 현재 XSS 아님(Codex도 인정). CSP 강화 시 해시 등록 필요 |
| M4 | fontKey를 타입/배열/매핑 3곳 관리(중복) | lib/data, lib/fonts, types | Medium | 부분동의 → Nice | 알려진 트레이드오프. 단일소스화 여지 |
| M6 | README가 create-next-app 템플릿 상태 | README.md | Medium | 동의 → Nice | 실제 미갱신. 문서 정리 필요 |

### 모델 합의도
- Degraded(단일 모델): Codex 단독 12건. agy 무효. Claude가 diff로 각 항목 재판정.
- Claude 자체 추가 발견: 없음(Codex가 C1 포함 핵심을 커버).

## 3. 최종 권고 및 머지 결정

### 반영 완료 (commit 148d770, 148d770 재검증 전부 그린)
- **C1(Must-fix)**: FontCard를 `<Link href=/fonts/[slug]>`로 → 목록-무료대안 클릭 이동 복구.
- M1: 상세 `.wrap`에 max-width 900 + margin auto + `var(--pad-page)`.
- H3: `start`=`serve out`, `e2e`=`next build && playwright test`.
- H5: 상세에 "확인일 {verifiedAt}" 표시.
- 검증: tsc 0, pnpm test 7/7, build 16페이지, playwright 26/0, eslint clean.

### 미반영 (판정: 의도/설계/후속)
- C2(nav 404): Phase 3 페이지 완성 시 해소. 현재 브랜드 404로 처리 — 후속.
- H1(목업 필터/트렌드 표): 스펙상 의도된 비동작 목업.
- H2(E2E 상태/제목 단언): 개선 여지, 후속.
- H4(next/font 네트워크): 설계 선택. 재현빌드 이슈화되면 next/font/local 검토.
- M2/M3/M4/M6: Nice-to-have. README 갱신은 다음 정리 때.

### 머지 결정: Must-fix 0건(해소됨) → **머지 가능**. 사용자 승인 대기.

## 4. 메타데이터
- Codex exit 0 / agy exit 0(단 무효 출력) / 리뷰 diff 1778줄(스코프)
- 픽스 commit: 148d770 (develop, PR #4 갱신됨)
