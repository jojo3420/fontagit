# PR Review Report: #17 — feat: Top10 이동 클릭 집계 (슬라이스3, F-03)

> Generated: 2026-07-17 08:56 KST
> PR URL: https://github.com/jojo3420/fontagit/pull/17
> Base ← Head: main ← feat/slice3-click-tracking
> Codex Model: gpt-5.6-sol (reasoning_effort=xhigh)
> 교차 리뷰: Codex(gpt-5.6-sol) + Claude(Opus 4.8), 앞서 code-reviewer(Opus) 별도 1회

---

## 0. PR 요약
- 익명 공식링크 이동 클릭 기록(fire-and-forget) + 홈/트렌드 Top10 실측 클릭 랭킹, 0건이면 "최신 등록" 폴백
- 변경 규모: +1655 / -41, 22개 파일
- DB 마이그레이션 0007(font_clicks/RPC/RLS), apps/web(clicks.ts/trends.ts/UI)

---

## 1. Codex 리뷰 (원문 요지)
- 전체 6/10, "Must-fix 해결 전 머지 불가"
- Must-fix 2건: 폴백 정직성 문구(trends/page.tsx), 익명 RPC 남용(rate limit 부재)
- Should-fix 3건: data=null 오인(trends.ts), 동기 예외(clicks.ts), 랭킹 순서 미검증(SQL C5)
- Medium 5건: clamp 표현, C7 p_days 미검증, 롤업 미연결, authenticated 역할, 계획 체크박스
- 긍정: IP/식별자 컬럼 없음, REVOKE+RLS, SECURITY DEFINER+search_path 고정, RPC 오류 throw, CTA 책임 분리, SQL 테스트 rollback 래핑

---

## 2. Claude 크로스 리뷰 (실코드 검증 판정)

| # | Codex 지적 | 파일:라인 | Codex | Claude 판정 | 근거(실코드 확인) |
|---|-----------|---------|-------|------------|------|
| 1 | 폴백에 인기/이동 클릭 문구 | trends/page.tsx:17 | Must-fix | **Should-fix** | h1="최신 등록 폰트"-showMoves=false로 사칭 아님. 단 lead에 "이동 클릭 기준 인기 순위로 전환됩니다" 잔존 → 확정 설계 문구 정합상 다듬을 가치. 사용자 판단 |
| 2 | 익명 RPC 무제한 호출 | 0007:65 | Must-fix | **후속/배포조건** | `grant execute record_click to anon`+rate limit 없음 확인. 단 rate limit은 앱코드 아닌 Supabase 게이트웨이/인프라. prod 적용 전 필수 조건 |
| 3 | data=null 0건 오인 | trends.ts:35 | Should-fix | **동의 Should-fix** | `(data ?? [])` null-[] 미구분. "이상은 throw" 설계의도와 모순. Array.isArray 방어 |
| 4 | 동기 예외 계약 파괴 | clicks.ts:12 | Should-fix | **동의 Should-fix** | `Promise.resolve(rpc(...))` rpc 선평가 → 동기 throw 시 catch 미포착. `.then(()=>rpc())`로 수정 |
| 5 | 랭킹 순서 미검증 | font_clicks_test.sql:82 | Should-fix | **동의 Should-fix** | C5가 top slug 일치 미검증, clicks<3만 확인. 기존 데이터 오염 시 위양성 |
| 6 | clamp가 실제 reset | 0007:80 | Medium | **동의 Medium** | 범위 초과 시 clamp 아닌 7-10 reset. "clamp" 표현 부정확(방어 자체는 유효) |
| 7 | C7 p_days 미검증 | font_clicks_test.sql:142 | Medium | **동의 Medium** | limit만 검증, p_days 경계/오래된 클릭 제외 미검증 |
| 8 | 롤업 미연결 | 0007:88 | Medium | **후속(계획됨)** | raw 전체 집계. 롤업 cron 후속 예정 |
| 9 | authenticated 역할 | 0007:65 | Medium | **후속(로그인 도입 시)** | 현재 앱 로그인 없음 |
| 10 | 계획 체크박스 미완료 | plan.md | Medium | **비동의/패스** | 프로젝트 규칙: 체크박스 신뢰 금지, SDD 원장이 SSoT. 의도적 분리 |

### code-reviewer(Opus)와의 차이
- code-reviewer는 홈 `WeeklyRankPanel`(hint="최근 등록순-클릭 데이터 수집 중")만 보고 "정직성 통과" 판정 → 트렌드 페이지 lead 문구는 검증에서 누락
- Codex가 trends/page.tsx:17 잔존 문구 포착 → 교차 리뷰의 실소득. 단 Must-fix는 과함(사칭 아닌 회색지대)

---

## 3. 최종 권고 및 머지 결정

### 머지 결정: ⏸ 사용자 보류 (직전 "아직 머지 안 함" 선택)
- Must-fix(동의): **0건** → 코드 블로커 없음, 기술적으로 머지 가능

### ⚠️ Should-fix (머지 전 반영 권장, 모두 저비용)
1. `clicks.ts:12` 동기 예외 방어 — `Promise.resolve().then(()=>supabaseClient.rpc(...))` + 동기 throw 테스트
2. `trends.ts:35` — `Array.isArray(data)` 아니면 throw(설계의도 정합)
3. `font_clicks_test.sql` C5 — 기존 클릭 격리 후 두 폰트 상대순서 검증
4. `trends/page.tsx:17` 폴백 lead 문구 — "인기 순위" 표현 제거 여부(사용자 판단)

### 🔭 후속/배포조건
- **rate limiting**: prod 0007 적용 전 Supabase 게이트웨이 호출 제한 + 폭증 모니터링 (배포 조건)
- 일별 롤업 cron(계획됨), authenticated 역할 지원(로그인 도입 시)

### 🚫 패스
- 계획 문서 체크박스(프로젝트 규칙상 SDD 원장이 SSoT)

---

## 4. 다음 단계
- [ ] Should-fix 1~3 반영(코드 저비용) 여부 결정 → 반영 시 default-worker 위임, 테스트 재실행
- [ ] 정직성 lead 문구(4) 다듬을지 사용자 판단
- [ ] 머지 방식 결정(squash 관례) 또는 후속 작업 우선
- [ ] prod 0007 적용 시 rate limit/모니터링 배포조건 선반영

---

## 5. 메타데이터
- Codex 종료: 정상, gpt-5.6-sol xhigh
- 머지: 미실행(보류)
- PR diff: 521줄
- 이 리포트: docs/review/pr-review-17-20260717-085623.md
