### 종합 평가
- 전체 점수: 6.5/10
- 한줄 총평: 구조는 좋지만, 라이선스 조회 실패 시 기존 공개 데이터를 전부 draft로 덮을 수 있는 치명적 실패 경로가 있습니다.
- 머지 권고: Must-fix 해결 전 불가

### 관점별 리뷰 (표)
| # | 관점 | 평가 | 핵심 지적 |
|---|---|---|---|
| 1 | 정합성 | 위험 | PR 설명의 “license-gated published” 보장이 실패 경로에서 깨짐 |
| 2 | 영향도 | 주의 | Supabase 업로드가 기존 공개 상태를 덮어쓰는 영향 있음 |
| 3 | SOLID/SRP | 양호 | license/transform/uploader 분리는 적절 |
| 4 | 선제적 위험 | 위험 | GitHub 일시 장애가 DB 공개 상태 대량 변경으로 이어짐 |
| 5 | 방어적 입력 처리 | 부족 | Supabase 응답, GitHub JSON 구조 검증이 약함 |
| 6 | 보안 | 주의 | RLS는 있으나 published 조건이 라이선스 검증과 묶여 있지 않음 |
| 7 | Silent Failure | 위험 | Supabase env 일부 누락 시 조용히 업로드 스킵 |
| 8 | 테스트 | 부족 | 핵심 실패 경로 테스트가 없음 |
| 9 | 코드 스타일 | 양호 | 대체로 읽기 쉬움 |
| 10 | 머피의 법칙 | 위험 | 외부 API 실패, 부분 업로드, 오래된 alias 처리 약함 |

### Critical 이슈 - Must-fix (머지 차단)

- `apps/pipeline/src/fontagit_pipeline/licenses.py:96`, `apps/pipeline/src/fontagit_pipeline/__main__.py:84` / 라이선스 조회 실패 시 기존 공개 데이터를 전부 draft로 덮을 수 있음 / 근거(diff 인용): `except httpx.HTTPError ... return {}` 이후 `upload_records(doc.fonts, ...)` 실행. 빈 `license_map`이면 모든 레코드가 `status="draft"`가 되고, `upsert`가 기존 `published`를 덮음 / 제안 수정: 라이선스 조회 실패와 “정상적으로 빈 결과”를 구분하세요. Supabase 업로드가 켜진 상태에서 라이선스 조회 실패면 업로드하지 말고 exit 3 처리하세요.

- `apps/pipeline/src/fontagit_pipeline/models.py:41`, `supabase/migrations/0001_fontagit_schema.sql:26,63` / DB와 모델이 “published는 검증된 라이선스만 가능”이라는 핵심 규칙을 강제하지 않음 / 근거(diff 인용): SQL은 `license_type is null or license_verified = true`만 검사하고, RLS는 `using (status = 'published')`만 봄. 즉 `status='published', license_verified=false, license_type=null`도 공개 가능 / 제안 수정: DB에 `status <> 'published' or (license_verified = true and license_type in ('OFL','Apache-2.0','UFL') and is_commercial_free = true)` 제약을 추가하고, `FontRecord` validator에도 같은 규칙을 넣으세요.

### High 이슈 - Should-fix

- `apps/pipeline/src/fontagit_pipeline/__main__.py:84` / Supabase 설정이 하나만 빠져도 조용히 업로드를 건너뜀 / 근거(diff 인용): `if settings.supabase_url and settings.supabase_secret_key: ... else: 업로드 건너뜀` / 제안 수정: 둘 중 하나만 있으면 설정 오류로 보고 exit 3 처리하세요.

- `apps/pipeline/src/fontagit_pipeline/uploader.py:63` / 업로드가 원자적이지 않음. fonts 성공 후 aliases 실패 시 중간 상태가 DB에 남음 / 근거(diff 인용): font upsert 후 alias upsert를 별도 요청으로 실행 / 제안 수정: Supabase RPC로 트랜잭션 처리하거나, 최소한 실패 시 명확히 재시도 가능한 상태와 로그를 남기세요.

- `apps/pipeline/src/fontagit_pipeline/uploader.py:69` / Supabase 응답을 무조건 신뢰함 / 근거(diff 인용): `font_id = str(data[0]["id"])` / 제안 수정: `data`가 비었거나 `id`가 없으면 명시적 예외를 내고 어떤 slug에서 실패했는지 기록하세요.

- `supabase/migrations/0001_fontagit_schema.sql:54,63` / `authenticated`에 select grant를 줬지만 RLS policy는 `anon`만 있음 / 근거(diff 인용): `grant select ... to anon, authenticated;` + `for select to anon` / 제안 수정: 인증 사용자도 읽어야 하면 policy에 `authenticated`를 포함하고, 아니면 grant를 제거하세요.

### Medium 이슈

- `supabase/migrations/0001_fontagit_schema.sql:22` / `updated_at`이 upsert 때 갱신되지 않음 / 제안 수정: trigger를 추가하거나 uploader에서 갱신하세요.

- `supabase/migrations/0001_fontagit_schema.sql:72` / 공개 collection이 draft font의 `font_id`를 노출할 수 있음 / 제안 수정: collection_items policy에 font도 `published`인지 확인하는 조건을 추가하세요.

- `apps/pipeline/src/fontagit_pipeline/uploader.py:45` / alias가 source에서 사라져도 DB에서는 삭제되지 않음 / 제안 수정: 해당 font의 alias를 동기화 기준으로 맞추거나 stale alias 삭제 정책을 정하세요.

- `apps/pipeline/src/fontagit_pipeline/uploader.py:33` / alias 정규화가 공백만 제거함 / 제안 수정: 하이픈, 구두점, 유니코드 정규화까지 처리할지 결정하세요.

- `apps/pipeline/src/fontagit_pipeline/models.py:31` / `status`, `source_tier`, `license_type`이 문자열 자유 입력임 / 제안 수정: `Literal` 타입으로 제한해 DB 전에 실패시키세요.

### 긍정적 관찰

- 라이선스 판별, 변환, 업로드 책임을 파일별로 나눈 점은 좋습니다.
- `license_verified` 기반으로 공개 상태를 정하려는 방향은 맞습니다.
- RLS를 처음부터 넣은 점은 좋습니다.
- `httpx` 로그에서 API 키 노출을 줄인 점은 적절합니다.
- `build_font_row`, `build_alias_rows`처럼 업로드 전 데이터를 작게 분리한 점은 테스트하기 좋습니다.