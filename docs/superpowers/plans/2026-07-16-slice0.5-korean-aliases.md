# Slice 0.5: 한글 이름-별칭 적재 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** korean subset 38종에 근거 있는 한글 이름(name_ko)-통용 별칭을 큐레이션해 파이프라인 경유로 dev DB에 적재한다.

**Architecture:** 큐레이션 매핑 JSON(패키지 내장) → 로더(pydantic 검증 + NFC) → transform 통합(fail-fast) → 기존 `upsert_font` RPC 재적재. 파이프라인이 SSoT, DB 직접 삽입 금지.

**Tech Stack:** Python 3.12 + pydantic v2 + pytest + uv / Supabase Postgres (dev만)

**Spec:** `docs/superpowers/specs/2026-07-16-slice0.5-korean-aliases-design.md` (v1.1)

## Global Constraints

- **prod DB 쓰기 금지** — 적재는 dev(`zgxtfcpiokhkcrywlxmc`)만. `apps/pipeline/.env`는 dev를 가리킴(확인됨).
- **네트워크 필요 작업(파이프라인 실행, psql 검증)은 메인 세션이 직접 실행** — 서브에이전트 위임 금지. 예외: Task 1 공개정보 웹조사는 병렬 서브에이전트 허용(사용자 확정).
- **정직성**: 근거 출처(URL) 없는 name_ko/별칭 금지. 확인 불가 항목은 null/빈 배열.
- name_ko 작성 기준: 공식 한글 표기(제작사-구글폰트 표기)만. 음역 창작 금지.
- 정규화 순서: **NFC → 공백제거 → lower** (Python `normalize_alias`가 기준, 슬라이스2 SQL도 동일 순서 예정).
- 라틴 92종 무변경(내용 기준): 재적재 전후 alias_norm 집합-name_ko(null) 동일.
- 테스트 실행: `cd apps/pipeline && uv run pytest` (uv.lock 존재).

## File Structure

| 파일 | 역할 |
|---|---|
| `apps/pipeline/src/fontagit_pipeline/data/korean_names.json` | Create — 큐레이션 매핑(38 slug). 패키지 내장으로 모듈 기준 접근 |
| `apps/pipeline/src/fontagit_pipeline/korean_names.py` | Create — 매핑 로더: pydantic 검증, NFC 정규화, fail-fast 대조 |
| `apps/pipeline/src/fontagit_pipeline/models.py` | Modify — `KoreanNameEntry` 모델 추가 |
| `apps/pipeline/src/fontagit_pipeline/transform.py` | Modify — `build_aliases` extra_aliases, `to_record`/`build_records` 매핑 통합 |
| `apps/pipeline/src/fontagit_pipeline/uploader.py` | Modify — `normalize_alias`에 NFC 추가 |
| `apps/pipeline/src/fontagit_pipeline/__main__.py` | Modify — 로더 호출 + build_document 연결 |
| `apps/pipeline/tests/test_korean_names.py` | Create — 로더-검증 테스트 |
| `apps/pipeline/tests/test_transform.py` | Modify — 별칭 병합-fail-fast 테스트 추가 |
| `apps/pipeline/tests/test_uploader.py` | Modify — NFC 정규화 테스트 추가 |

주: 스펙 4.1의 `apps/pipeline/data/` 경로는 Should 7(모듈 기준 경로) 반영으로 `src/fontagit_pipeline/data/`(패키지 내장)로 확정. hatchling `packages = ["src/fontagit_pipeline"]`이 데이터 파일을 wheel에 포함한다.

---

### Task 1: 한글 이름-별칭 웹조사 큐레이션 (병렬 서브에이전트)

**Files:**
- 산출(임시): 조사 결과 마크다운(스크래치패드) — 메인이 검수 후 Task 2에서 JSON 확정

**Interfaces:**
- Produces: 38종 각각에 대해 `{ slug, name_ko(또는 null), aliases[], sources[](URL), 확인 요약 }`

**Steps:**

- [ ] **Step 1: 38종을 4개 그룹으로 분할해 general-purpose 서브에이전트 4개 병렬 디스패치** (WebSearch/WebFetch 사용)

그룹:
- G1: noto-sans-kr, noto-serif-kr, nanum-gothic, nanum-myeongjo, nanum-gothic-coding, nanum-pen-script, nanum-brush-script, ibm-plex-sans-kr, gothic-a1, hahmlet
- G2: jua, do-hyeon, black-han-sans, gaegu, hi-melody, dongle, gamja-flower, poor-story, cute-font, yeon-sung
- G3: gowun-batang, gowun-dodum, sunflower, song-myung, gugi, stylish, dokdo, east-sea-dokdo, single-day, kirang-haerang
- G4: asta-sans, bagel-fat-one, gasoek-one, orbit, diphylleia, black-and-white-picture, moirai-one, grandiflora-one

각 서브에이전트 프롬프트 골자:

```
다음 구글폰트 폰트들의 "한글 공식 이름"과 "실제 통용되는 한글 별칭"을 조사하라: [slug: name_en 목록]

각 폰트에 대해 반환:
- name_ko: 공식 한글 표기(제작사 공식 페이지, 구글폰트 specimen의 한글 표기, 배포처 공식 문서에서 확인된 것만). 확인 불가면 null.
- aliases: 실제 통용 별칭(예: Noto Sans KR의 "본고딕"). 웹에서 실사용이 확인되는 것만. 없으면 [].
- sources: 각 판단의 근거 URL 1개 이상. URL 없으면 그 항목을 넣지 마라.
- note: 확인 과정 한 줄.

규칙(위반 시 산출물 폐기):
1. 음역 창작 금지 — "Diphylleia"를 근거 없이 "디필레이아"로 만들지 마라.
2. 추측 별칭 금지 — 출처에서 그 표기를 실제로 확인한 것만.
3. 확인 불가는 정직하게 null/[]로.

참고 힌트: 나눔 계열=네이버(한글꼴 모음), 주아/도현/한나/기랑해랑/을지로 계열=우아한형제들(배달의민족 글꼴), 고운바탕/고운돋움=류양희, 눈누(noonnu.cc)에 한글명 페이지가 있는 경우가 많다.

반환 형식: slug별 JSON 배열(마크다운 코드블록).
```

- [ ] **Step 2: 4개 결과 종합 — 메인이 검수** (sources URL 실재성 표본 확인, 음역 창작 여부 점검, 중복-표기 충돌 정리)

- [ ] **Step 3: 검수본을 스크래치패드에 `korean-names-curation.md`로 저장** (Task 2 입력)

검수 기준: 38 slug 전원 존재(값이 null/[]이어도 항목은 존재), sources 없는 name_ko/alias 0건.

---

### Task 2: KoreanNameEntry 모델 + 매핑 JSON + 로더 (TDD)

**Files:**
- Create: `apps/pipeline/src/fontagit_pipeline/data/korean_names.json`
- Create: `apps/pipeline/src/fontagit_pipeline/korean_names.py`
- Modify: `apps/pipeline/src/fontagit_pipeline/models.py` (파일 끝에 모델 추가)
- Test: `apps/pipeline/tests/test_korean_names.py`

**Interfaces:**
- Consumes: Task 1 검수본(큐레이션 데이터)
- Produces:
  - `KoreanNameEntry(name_ko: str | None, aliases: list[str], sources: list[str])` (models.py)
  - `load_korean_names() -> dict[str, KoreanNameEntry]` (korean_names.py) — 반환 키는 slug, 값은 NFC 정규화 완료
  - `validate_coverage(mapping: dict[str, KoreanNameEntry], published_korean_slugs: set[str]) -> None` — 불일치 시 `KoreanNamesError` raise

**Steps:**

- [ ] **Step 1: 실패 테스트 작성** (`tests/test_korean_names.py`)

```python
import unicodedata

import pytest

from fontagit_pipeline.korean_names import (
    KoreanNamesError,
    load_korean_names,
    validate_coverage,
)


def test_load_korean_names_returns_38_nfc_normalized_entries():
    mapping = load_korean_names()
    assert len(mapping) == 38
    entry = mapping["noto-sans-kr"]
    assert entry.name_ko is not None
    assert "본고딕" in entry.aliases
    assert entry.sources  # 근거 URL 1개 이상
    # NFC 보장: 모든 문자열이 NFC와 동일
    for slug, e in mapping.items():
        for s in ([e.name_ko] if e.name_ko else []) + e.aliases:
            assert s == unicodedata.normalize("NFC", s), f"{slug}: NFD 혼입"


def test_validate_coverage_fails_on_missing_and_surplus():
    mapping = load_korean_names()
    published = set(mapping.keys())
    validate_coverage(mapping, published)  # 일치 → 통과

    with pytest.raises(KoreanNamesError, match="누락"):
        validate_coverage(mapping, published | {"new-korean-font"})

    with pytest.raises(KoreanNamesError, match="잉여"):
        validate_coverage(mapping, published - {"noto-sans-kr"})
```

- [ ] **Step 2: 실패 확인** — `cd apps/pipeline && uv run pytest tests/test_korean_names.py -v` → Expected: FAIL (ModuleNotFoundError)

- [ ] **Step 3: models.py에 KoreanNameEntry 추가** (파일 끝)

```python
class KoreanNameEntry(BaseModel):
    """큐레이션된 한글 이름-별칭. sources는 근거 URL(DB로 유입되지 않음)."""

    name_ko: str | None = None
    aliases: list[str] = []
    sources: list[str]

    @model_validator(mode="after")
    def validate_evidence(self) -> "KoreanNameEntry":
        """근거 없는 큐레이션 금지: 이름/별칭이 있으면 출처 필수, 빈 문자열 금지."""
        has_content = bool(self.name_ko) or bool(self.aliases)
        if has_content and not self.sources:
            raise ValueError("name_ko/aliases가 있으면 sources가 1개 이상 필요합니다")
        if any(not a.strip() for a in self.aliases):
            raise ValueError("빈 별칭은 허용되지 않습니다")
        if self.name_ko is not None and not self.name_ko.strip():
            raise ValueError("name_ko는 빈 문자열일 수 없습니다(없으면 null)")
        return self
```

(models.py 상단 import에 `model_validator`가 이미 있는지 확인, 없으면 추가)

- [ ] **Step 4: korean_names.py 작성**

```python
"""한글 이름-별칭 큐레이션 매핑 로더."""

import json
import unicodedata
from pathlib import Path

from fontagit_pipeline.models import KoreanNameEntry

_DATA_PATH = Path(__file__).parent / "data" / "korean_names.json"


class KoreanNamesError(Exception):
    """매핑 파일 검증 실패."""


def _nfc(entry: KoreanNameEntry) -> KoreanNameEntry:
    """큐레이션 입력의 NFD(자모 분리) 혼입 방지 — 저장 원문부터 NFC로 통일."""
    return entry.model_copy(
        update={
            "name_ko": unicodedata.normalize("NFC", entry.name_ko) if entry.name_ko else None,
            "aliases": [unicodedata.normalize("NFC", a) for a in entry.aliases],
        }
    )


def load_korean_names(path: Path = _DATA_PATH) -> dict[str, KoreanNameEntry]:
    """매핑 JSON을 로드-검증한다. 파일 부재/파싱 실패/스키마 위반 시 KoreanNamesError."""
    try:
        raw = json.loads(path.read_text(encoding="utf-8"))
    except (OSError, json.JSONDecodeError) as exc:
        raise KoreanNamesError(f"매핑 파일 로드 실패({path}): {exc}") from exc
    try:
        return {slug: _nfc(KoreanNameEntry(**data)) for slug, data in raw.items()}
    except (TypeError, ValueError) as exc:
        raise KoreanNamesError(f"매핑 스키마 위반: {exc}") from exc


def validate_coverage(
    mapping: dict[str, KoreanNameEntry], published_korean_slugs: set[str]
) -> None:
    """published korean subset 폰트와 매핑 키의 완전 일치를 강제한다(침묵 실패 금지)."""
    missing = published_korean_slugs - mapping.keys()
    surplus = mapping.keys() - published_korean_slugs
    if missing:
        raise KoreanNamesError(f"매핑 누락 slug: {sorted(missing)}")
    if surplus:
        raise KoreanNamesError(f"매핑 잉여 slug: {sorted(surplus)}")
```

- [ ] **Step 5: korean_names.json 작성** — Task 1 검수본을 다음 형식으로 변환(38 slug 전원):

```json
{
  "noto-sans-kr": {
    "name_ko": "노토 산스 코리안",
    "aliases": ["본고딕"],
    "sources": ["https://fonts.google.com/noto/specimen/Noto+Sans+KR", "..."]
  },
  "diphylleia": {
    "name_ko": null,
    "aliases": [],
    "sources": []
  }
}
```

(위 값은 형식 예시 — 실제 값은 Task 1 검수본만 사용. null/[] 항목은 sources도 [] 허용)

- [ ] **Step 6: 통과 확인** — `uv run pytest tests/test_korean_names.py -v` → Expected: PASS (2개)

- [ ] **Step 7: Commit** — `git add`(data JSON, korean_names.py, models.py, 테스트) 후 `feat: 한글 이름-별칭 큐레이션 매핑 + 로더 (fail-fast 검증)`

---

### Task 3: transform 통합 — extra_aliases + fail-fast (TDD)

**Files:**
- Modify: `apps/pipeline/src/fontagit_pipeline/transform.py` (build_aliases 43-60행, to_record, build_records)
- Modify: `apps/pipeline/src/fontagit_pipeline/__main__.py` (build_document, main)
- Test: `apps/pipeline/tests/test_transform.py` (추가)

**Interfaces:**
- Consumes: `load_korean_names`, `validate_coverage`, `KoreanNamesError`, `KoreanNameEntry` (Task 2)
- Produces:
  - `build_aliases(name_en: str, name_ko: str | None = None, extra_aliases: list[str] | None = None) -> list[str]`
  - `to_record(raw, license_map, korean_names: dict[str, KoreanNameEntry] | None = None) -> FontRecord`
  - `build_records(fonts, license_map, latin_limit=100, korean_names=None) -> list[FontRecord]` — published korean 레코드에 대해 coverage 검증(불일치 시 KoreanNamesError 전파)

**Steps:**

- [ ] **Step 1: 실패 테스트 3개 추가** (`tests/test_transform.py` 끝에)

```python
def test_build_aliases_merges_name_ko_and_extra_aliases():
    result = build_aliases("Noto Sans KR", "노토 산스 코리안", ["본고딕"])
    assert result == [
        "Noto Sans KR",
        "noto sans kr",
        "notosanskr",
        "noto sans kr ttf",
        "노토 산스 코리안",
        "노토산스코리안",
        "본고딕",
    ]


def test_to_record_applies_korean_names_mapping(webfonts_sample, license_map):
    from fontagit_pipeline.models import KoreanNameEntry

    korean = next(f for f in webfonts_sample if "korean" in f.subsets)
    mapping = {
        build_slug(korean.family): KoreanNameEntry(
            name_ko="테스트한글명", aliases=["통용별칭"], sources=["https://example.com/evidence"]
        )
    }
    record = to_record(korean, license_map, korean_names=mapping)
    assert record.name_ko == "테스트한글명"
    assert "테스트한글명" in record.aliases
    assert "통용별칭" in record.aliases


def test_build_records_fails_fast_on_missing_korean_mapping(webfonts_sample, license_map):
    from fontagit_pipeline.korean_names import KoreanNamesError

    # korean subset published 폰트가 존재하는데 매핑이 비어 있음 → 침묵 실패 금지
    with pytest.raises(KoreanNamesError):
        build_records(webfonts_sample, license_map, korean_names={})
```

(`build_slug` import가 테스트 상단에 없으면 추가. `korean_names=None`일 때는 기존 동작 유지 — 기존 테스트 전원 그린이 그 증거)

- [ ] **Step 2: 실패 확인** — `uv run pytest tests/test_transform.py -v -k "korean or extra"` → Expected: FAIL (TypeError: unexpected keyword)

- [ ] **Step 3: transform.py 수정**

build_aliases (기존 43-60행 교체):

```python
def build_aliases(
    name_en: str,
    name_ko: str | None = None,
    extra_aliases: list[str] | None = None,
) -> list[str]:
    """검색용 기본 별칭 목록을 만든다(동일 문자열 기준 중복 제거, 순서 유지)."""
    candidates = [
        name_en,
        name_en.lower(),
        name_en.lower().replace(" ", ""),
        f"{name_en.lower()} ttf",
    ]
    if name_ko:
        candidates += [name_ko, name_ko.replace(" ", "")]
    candidates += extra_aliases or []
    seen: set[str] = set()
    result: list[str] = []
    for c in candidates:
        if c not in seen:
            seen.add(c)
            result.append(c)
    return result
```

to_record: 시그니처에 `korean_names: dict[str, KoreanNameEntry] | None = None` 추가(import: `from fontagit_pipeline.models import GoogleFontRaw, FontRecord, KoreanNameEntry`). 본문 변경:

```python
    slug = build_slug(raw.family)
    entry = (korean_names or {}).get(slug)
    return FontRecord(
        slug=slug,
        ...(기존 그대로)...
        name_ko=entry.name_ko if entry else None,
        aliases=build_aliases(
            raw.family,
            entry.name_ko if entry else None,
            entry.aliases if entry else None,
        ),
        ...(기존 그대로)...
    )
```

build_records: 시그니처에 `korean_names: dict[str, KoreanNameEntry] | None = None` 추가, 루프에서 `to_record(raw, license_map, korean_names)` 전달. 루프 종료 후(개별 건너뜀 try/except 밖):

```python
    if korean_names is not None:
        published_korean = {
            r.slug for r in records if r.status == "published" and "korean" in r.subsets
        }
        validate_coverage(korean_names, published_korean)
```

(import: `from fontagit_pipeline.korean_names import validate_coverage`)

- [ ] **Step 4: __main__.py 연결**

- `build_document(..., korean_names: dict[str, KoreanNameEntry] | None = None)` 파라미터 추가 → `build_records(fonts, license_map, latin_limit, korean_names=korean_names)` 전달.
- `main()`에서 `license_map` 확보 직후:

```python
    try:
        korean_names = load_korean_names()
    except KoreanNamesError as exc:
        logger.error("한글 매핑 로드 실패: %s", exc)
        return 3
```

- `build_document(fonts, license_map, generated_at, korean_names=korean_names)` 호출.
- `KoreanNamesError`(coverage 불일치)가 build_document에서 전파되면 main에서 잡아 return 3:

```python
    try:
        doc = build_document(fonts, license_map, generated_at, korean_names=korean_names)
    except KoreanNamesError as exc:
        logger.error("한글 매핑 coverage 불일치: %s", exc)
        return 3
```

(import: `from fontagit_pipeline.korean_names import KoreanNamesError, load_korean_names`)

- [ ] **Step 5: 전체 통과 확인** — `uv run pytest -q` → Expected: 전원 PASS (기존 테스트 그린 = korean_names=None 하위 호환 증거. test_main.py의 build_document 호출부가 깨지면 korean_names=None 기본값으로 통과해야 함)

- [ ] **Step 6: Commit** — `feat: transform에 한글 이름-별칭 통합 (fail-fast coverage 검증)`

---

### Task 4: normalize_alias NFC 정규화 (TDD)

**Files:**
- Modify: `apps/pipeline/src/fontagit_pipeline/uploader.py:33-35`
- Test: `apps/pipeline/tests/test_uploader.py` (추가)

**Interfaces:**
- Produces: `normalize_alias(alias: str) -> str` — NFC → 공백제거 → lower (기존 시그니처 유지, 슬라이스2 SQL `normalize_search`가 이 규칙을 따라감)

**Steps:**

- [ ] **Step 1: 실패 테스트 추가** (`tests/test_uploader.py`)

```python
def test_normalize_alias_converts_nfd_to_nfc():
    import unicodedata

    nfd = unicodedata.normalize("NFD", "본 고딕")  # 자모 분리형
    assert normalize_alias(nfd) == "본고딕"
    assert normalize_alias(nfd) == normalize_alias("본 고딕")  # NFC/NFD 입력 동치
```

- [ ] **Step 2: 실패 확인** — `uv run pytest tests/test_uploader.py -v -k nfd` → Expected: FAIL (NFD 문자열 ≠ NFC 문자열)

- [ ] **Step 3: uploader.py 수정** (33-35행)

```python
def normalize_alias(alias: str) -> str:
    """별칭을 정규화한다(NFC → 공백 제거 → 소문자). 한글은 유지."""
    return re.sub(r"\s+", "", unicodedata.normalize("NFC", alias).lower())
```

(상단 import에 `import unicodedata` 추가)

- [ ] **Step 4: 통과 확인** — `uv run pytest tests/test_uploader.py -v` → Expected: 전원 PASS

- [ ] **Step 5: Commit** — `feat: normalize_alias에 NFC 정규화 추가 (정규화 SSoT 순서 확정)`

---

### Task 5: dev 재적재 + 전수 검증 (메인 세션 직접 — 네트워크)

**Files:** 없음(실행-검증만). 산출: 검증 로그.

**Interfaces:**
- Consumes: Task 2~4 완료된 파이프라인, `apps/pipeline/.env`(dev), 루트 `.env.sandbox`(psql 비번)

**Steps:**

- [ ] **Step 1: 재적재 전 라틴 스냅샷 저장** (psql → 스크래치패드 `latin-before.txt`)

```sql
select f.slug, coalesce(f.name_ko,'∅'), array_to_string(array_agg(a.alias_norm order by a.alias_norm), ',')
from fontagit.fonts f left join fontagit.aliases a on a.font_id = f.id
where f.status = 'published' and not ('korean' = any(f.subsets))
group by f.slug, f.name_ko order by f.slug;
```

- [ ] **Step 2: 전체 테스트 최종 그린 확인** — `cd apps/pipeline && uv run pytest -q` → PASS 로그 확보

- [ ] **Step 3: 파이프라인 실행(적재)** — `cd apps/pipeline && uv run python -m fontagit_pipeline` → Expected: `업로드 130개(공개 130개)` 유사 로그, exit 0. (전량 검증 후 쓰기: coverage fail-fast가 build_document 단계 = 업로드 전에 발생)

- [ ] **Step 4: dev 전수 검증** (psql, 매핑 기대값과 대조)

```sql
-- (a) name_ko 반영 수 = 매핑의 name_ko 비-null 수
select count(*) from fontagit.fonts where status='published' and name_ko is not null;
-- (b) 한글 별칭 보유 폰트 수 = 매핑에서 (name_ko 또는 aliases 보유) 폰트 수
select count(distinct f.id) from fontagit.fonts f join fontagit.aliases a on a.font_id=f.id
where f.status='published' and a.alias_norm ~ '[가-힣]';
-- (c) 대표 샘플
select f.slug, f.name_ko, array_agg(a.alias order by a.alias) from fontagit.fonts f
join fontagit.aliases a on a.font_id=f.id
where f.slug in ('noto-sans-kr','nanum-gothic','jua') group by f.slug, f.name_ko;
```

기대값은 korean_names.json에서 산출: `(a) = name_ko 비-null 항목 수`, `(b) = name_ko 또는 aliases가 있는 항목 수`. 불일치 시 원인 규명 전 완료 선언 금지.

- [ ] **Step 5: 라틴 무변경 확인** — Step 1과 동일 쿼리 → `latin-after.txt` → `diff latin-before.txt latin-after.txt` → Expected: 차이 0줄

- [ ] **Step 6: 완료 보고 + Commit** — 검증 로그(테스트-적재-전수쿼리-diff) 첨부, `docs/progress.md`는 세션 마무리에서. 커밋: `feat: 슬라이스0.5 - dev 한글 이름-별칭 재적재 검증 완료` (문서/로그 변경분 있을 때만)

---

## Self-Review 결과

- Spec 커버리지: 2장(38종-기준) → Task 1-2 / 4.1(sources 배열-노출 경계) → Task 2 / 4.2(로더-fail-fast-모듈 경로) → Task 2-3 / 4.3(NFC SSoT) → Task 4 / 4.4(전량 검증 후 쓰기-재적재) → Task 5 / 4.5(테스트 4종) → Task 2-3-4 / 5장(완료 기준 전수 검증) → Task 5. 갭 없음.
- 타입 일관성: `KoreanNameEntry`/`load_korean_names`/`validate_coverage`/`KoreanNamesError` 명칭 Task 2 정의 = Task 3-5 사용 일치.
- 6장(슬라이스2 테스트 실데이터 교체-SQL 버그 B/C)은 이 계획 범위 밖 — 슬라이스2 복귀 시 반영.

**Plan Version**: 1.0 | **Status**: Ready for Implementation | **Date**: 2026-07-16
