# 세션 핸드오프 — 2026-07-13 13:21 KST

> 모드: superpowers-plan (docs/superpowers/plans 사용)
> Feature: FontAgit 웹 Phase 3-4 화면
> 이전 세션 종결 사유: 인계 (다음 세션에서 Phase 3-4 계획 작성)

## 한 줄 요약

Phase 1-2 웹 토대+핵심 4화면이 완성돼 main에 머지됨(PR #4). 다음 세션은 **Phase 3-4 화면 구현 계획서를 새로 작성**한다(브레인스토밍은 스펙에 이미 반영됨 → 바로 writing-plans 가능).

---

## 다음 세션이 가장 먼저 할 일

반드시(must) — 컨텍스트 복원 순서:

1. 이 핸드오프 파일을 읽는다.
2. 스펙과 기존 계획서를 읽는다:
   - `docs/superpowers/specs/2026-07-12-fontagit-web-screens-design.md` (13화면 전체 확정, 절 4 라우트맵, 절 11 리스크-OG)
   - `docs/superpowers/plans/2026-07-12-fontagit-web-foundation-and-core.md` (Phase 1-2 완료본, 맨 끝 "후속 계획" 섹션에 Phase 3-4 목록)
   - `docs/progress.md` + `docs/progress-001.md` (현재 apps/web 구조-결정-검증 명령어)
3. 현재 상태 확인: `git status && git log --oneline -6`. apps/web 실제 구조는 `apps/web/{app,components,lib,data,types,styles}` 참조.
4. 아래 "다음 단계 → MUST"부터 시작: superpowers:writing-plans로 Phase 3-4 계획서 작성.

---

## 작업 컨텍스트

### 사용자 원본 요청
> "@docs/design/fontagit-v2/README.md 디자인 문서 확인 후 디자인과 동일하게(95%) 화면 구성하기. 목업파일 참고 @docs/design" → 이후 정식 Next.js 앱으로, 13화면 확정, Phase 1-2 먼저 구현-머지 완료. 이번 인계 목적: **Phase 3-4 화면 계획을 새로 작성**.

### 추가 합의-변경 사항
- 결과물: 정식 Next.js 앱(폐기 없는 제품 토대). 범위: 확정 13화면, 4단계 phasing.
- `/web-setup`이 만든 Tailwind 스캐폴드를 세션 결정(CSS Modules) 우선으로 재정비함.
- Phase 1-2를 develop→main PR #4로 머지(merge 커밋 `4fe1da5`).

### 사용자 제약-금지사항 (반드시 준수)
- 스타일: CSS Modules + CSS 변수 토큰만. Tailwind 재도입 금지. 인라인 스타일은 데이터 기반 `fontFamily`만 예외.
- 모든 UI 텍스트 한국어. UI는 Pretendard, 견본만 견본 폰트.
- 정적 export(`output: 'export'`) 유지. 동적 라우트는 async `params`(`await params`) + `generateStaticParams` + `dynamicParams=false` + `notFound()`.
- 데이터는 목업(파이프라인 실연동은 범위 밖). 필드명은 파이프라인 `FontRecord`와 정합 유지.
- 오케스트레이션: 메인은 판단, 구현은 서브에이전트 위임(SDD).

---

## Plan / Design 인덱스

| 단계 | 문서 | 상태 |
|------|------|------|
| Design(스펙) | `docs/superpowers/specs/2026-07-12-fontagit-web-screens-design.md` | 확정 (13화면 전체 + 자체-Codex 리뷰 반영) |
| Plan(Phase 1-2) | `docs/superpowers/plans/2026-07-12-fontagit-web-foundation-and-core.md` | 완료 (13/13 태스크, 머지됨) |
| Plan(Phase 3-4) | (미작성) | 다음 세션 MUST |

---

## 코드 변경 상태 (git)

- 브랜치: `develop` (main보다 progress 커밋 `cb8db39` 앞섬). main = `4fe1da5`(PR #4 머지).
- Uncommitted(untracked): `docs/review/pr-review-4-{codex,agy,dual}-20260713-120348.md` — PR 리뷰 산출물. 커밋할지 다음 세션 판단(무해).
- Phase 1-2 웹 커밋 범위: `3634686`..`148d770` (main 반영됨).

---

## 결정 사항 (뒤집지 말 것 — 변경 시 사용자 확인)

| # | 결정 | 근거 |
|---|------|------|
| 1 | 스타일 CSS Modules + tokens.css 변수 | 세션 결정, 픽셀 정밀도 |
| 2 | 정적 export + async params + generateStaticParams + dynamicParams=false | Next 16 정적 배포 |
| 3 | 폰트 self-host: Pretendard(npm) + 견본 next/font/google | CWV. Song_Myung은 Next 타입상 subsets 미지원이 정상 |
| 4 | 데이터 목업(FontKey 유니언, lib/data 헬퍼, assertDataIntegrity 빌드시 호출) | 95% 재현 우선 |
| 5 | 인터랙션 핵심만 동작(미리보기-다크토글). 필터/검색/트렌드탭은 의도적 비동작 목업 | 스펙 범위 |
| 6 | OG(7): output:export라 전 slug 빌드시 사전생성 + ImageResponse에 한글 폰트 바이트 직접 로드(불안정시 public/og PNG 폴백) | 스펙 절 11 |

---

## 블로커 - 미해결 이슈

| # | 이슈 | 영향 | 다음 시도 |
|---|------|------|----------|
| 1 | 헤더 `/collections`-`/submit` 링크가 클릭 시 404 | UX(브랜드 404로 처리 중) | Phase 3에서 페이지 생성 후 Header의 `prefetch={false}` 제거-링크 정상화 |
| 2 | agy CLI가 request-pr-dual에서 stdin 프롬프트를 리뷰로 처리 못함(플래그만 조사) | dual 리뷰가 Codex 단독(Degraded) | agy 대안 모델 검토 또는 codex 단일 리뷰 사용 |
| 3 | Collections 데이터 추가 시 assertDataIntegrity에 `Collection.items[].fontSlug` 검증 추가 필요 | 빌드 무결성 | 컬렉션 태스크에서 검증 로직 확장 |
| 4 | README가 create-next-app 템플릿 상태 + 루트/apps/web 중복 pnpm-workspace.yaml 빌드 경고 | Nice | 정리 |

---

## 다음 단계 (Next)

MUST (진행 핵심):
- [ ] superpowers:writing-plans로 **Phase 3-4 계획서** 작성. 대상 화면(디자인 소스 라인):
  - 타입 캔버스 `/playground` (3a, 라인 461-) + 모바일(4c, 231-)
  - 비교 `/compare` 최대 3종-입력 연동 (5a, 258-)
  - 컬렉션 목록 `/collections` + 상세 `/collections/[slug]` (8a, 371-). Collection 목업 데이터-타입-무결성 검증 추가
  - 창작자 등록 `/submit` 폼 UI만 (8b, 387-)
  - 빈 상태 EmptyState 컴포넌트 (시스템 1i)
  - 모바일 반응형 정밀화: 홈/상세/캔버스 (4a 179-, 4b 203- 하단고정입력, 4c 231-). safe-area/키보드/탭바
  - 다크모드 토글 UI (9b, 423-). 토큰-FOUC 스크립트는 이미 있음 → 헤더 토글 버튼 + localStorage 저장 연결
  - 런칭 자산: 파비콘 세트(7a, 330-), 기본 OG(7b, 346-), 동적 OG `app/fonts/[slug]/opengraph-image`(7b, 355-)

SHOULD:
- [ ] 계획 작성 시 기존 Phase 1-2 계획서의 태스크 구조-검증 방식(Vitest 로직 테스트 + Playwright 스모크) 재사용
- [ ] Header nav 링크 정상화(블로커 #1), assertDataIntegrity 컬렉션 검증(#3)

NICE-TO-DO:
- [ ] README 갱신, pnpm-workspace 중복 경고 정리(#4)
- [ ] docs/review 미커밋 리뷰 파일 커밋 여부 결정

---

## SDD 실행 교훈 (Phase 3-4 구현 시 반복 방지)

- 서브에이전트가 "정확 전사" 태스크에서도 임의 생성함(디자인 토큰, 테스트, next/font 옵션). controller가 **실제 파일/테스트를 직접 대조** 검증 필수.
- 화면 태스크는 **네비게이션 링크 존재**까지 확인(FontCard가 `<article>`로 링크 누락됐던 버그를 controller가 놓치고 PR 리뷰가 잡음).
- 테스트 파일은 vitest 통과 개수만 믿지 말고 `tsc --noEmit`와 실제 단언 내용 확인(가짜 통과 사례 있었음).

---

## 핵심 파일 경로 (Refs)

| 카테고리 | 경로 |
|---------|------|
| 스펙 | `docs/superpowers/specs/2026-07-12-fontagit-web-screens-design.md` |
| Phase 1-2 계획 | `docs/superpowers/plans/2026-07-12-fontagit-web-foundation-and-core.md` |
| 디자인 소스(시각 SSOT) | `docs/design/fontagit-v2/project/FontAgit 화면 세트.dc.html` |
| 진행 일지 | `docs/progress.md`, `docs/progress-001.md` |
| 앱 코드 | `apps/web/{app,components,lib,data,types,styles}` |
| 이 핸드오프 | `docs/superpowers/handoff/2026-07-13-1321-phase3-4-plan.md` |

---

## 검증 상태 (Phase 1-2, main 반영본 기준)

| 항목 | 상태 |
|------|------|
| 빌드(pnpm build, SSG) | 통과 (out/ 16페이지) |
| 단위 테스트(pnpm test) | 통과 (7/7) |
| E2E(Playwright 스모크) | 통과 (26/0) |
| tsc / eslint | 통과 (에러 0, 억제 0) |
| PR #4 머지 | 완료 (`4fe1da5`) |

---

## 재개 프롬프트 (다음 세션에 그대로 붙여넣기)

```
이전 세션의 작업을 이어받습니다. 다음 핸드오프 파일을 먼저 읽고 컨텍스트를 복원해주세요:

/Users/joel.silver/Workspace/gitroom/python/fontagit/docs/superpowers/handoff/2026-07-13-1321-phase3-4-plan.md

복원 순서:
1. 위 핸드오프 파일 전체를 읽는다
2. Plan/Design 인덱스의 스펙과 Phase 1-2 계획서, progress.md/progress-001.md를 읽는다
3. git status / git log --oneline -6로 현재 상태 확인 (브랜치 develop)
4. "다음 단계 → MUST"부터 시작: superpowers:writing-plans로 Phase 3-4 계획서 작성
5. 결정 사항 표(CSS Modules, 정적 export, 목업 데이터, 인터랙션 범위, OG 방식)는 뒤집지 않는다 (변경 시 사용자 확인)
6. 블로커(헤더 nav 404, agy 리뷰 실패, 컬렉션 무결성 검증)를 계획에 반영

진행 전에 핸드오프 파일을 읽었음을 확인하고, Phase 3-4 계획서 작성을 어떤 화면부터 슬라이싱할지 한 줄로 보고해주세요.
```

---

핸드오프 메모 작성 완료. 다음 세션은 위 재개 프롬프트로 시작.
